# CHANGELOG

格式遵循 Keep a Changelog，并采用语义化版本。

## [1.2.0] - 2026-09-09

### 暂撤 Mac 客户端，保留源码工作台

- 以已发布 v1.1.0 为基础移除 Mac 原生窗口、打包/签名工具、专属启动与静态网关；保留 React 浏览器 UI、源码 setup/start、Codex 引擎与所有已发布研究和模型接入能力。
- 暂以源码 + 本地浏览器工作台交付，便于使用者的 Agent 改造开发。中英文 README 和发布清单同步调整；原有客户端数据不删除、不自动迁移。
- 本次为源码更新，不构建新安装包；GitHub 历史 v1.1.0 Release、安装包及官网入口不变。详见 [源码交付说明](docs/source-delivery.md)。

### Issue / PR 增量修复

- 排查 Issue #48 时修复自定义 API 的预设恢复：返回设置页时按来源和模型共同恢复，不再把用户编辑的模型名称当成下拉选项，也不因模型同名切换服务商。保存后回读浏览器存储，静默写入失败时明确报错；不声称复现了旧 Mac 客户端的全部存储故障。
- 适配 PR #42（感谢 @WenhuaXia）：已引用计算记录中的 inputs/details 数字允许受限的舍入、百分比和单位换算，避免误拦合法中间值；保留 output.value 的 display 约束，不整体放宽派生结果校验。
- PR #45 的缺字段崩溃已由现有契约校验覆盖；PR #47 的报告表格与证据链接已有现行实现，不覆盖回旧方案。其余新增功能及风险项见 [Issue / PR 处理记录](docs/issue-pr-triage-v1.2.0.md)。
- 独立审查后排除 details 中结果形子对象的 value，防止嵌套派生结果绕过 display 规则；真实中间量仍可绑定。
- MCP 间接依赖 Hono 从 4.13.3 更新到 4.13.5，处理本轮依赖审计报告的安全公告，不改变 MCP SDK 或 Codex 引擎版本。

## [1.1.0] - 2026-09-07

### 多模型订阅接入与 Mac 独立客户端

- 统一前后端及依赖锁文件版本为 1.1.0，Mac 构建号递增至 40；旧 M38 的 1.0.4 安装包保留，不能改名当作新版。
- 新版重点：Codex / Claude Code / WorkBuddy 订阅和 API 接入、默认普通对话与按需开启 Agent、聊天优先首页和五类功能导航、截图／表格转写，以及独立 Mac 客户端。
- M40 隐私加固：截图/表格转写只开放本次上传清单的受控读取，文本与图片都要求实际读取完成；只产待确认草稿，不自动写持仓。成功、失败和取消后清理本次临时副本，不删除原文件或历史资料。
- 本地订阅进程改为必要环境变量白名单，避免继承无关服务密钥；Mac/POSIX 在退出成功前也确认所属进程组已停止，失败或取消仍保留真实状态。Windows 正常退出后的孤儿进程清理尚缺 Job Object 级保证，不能套用 Mac 的通过结论。
- Mac 打包仅接收精确公开根文件与目录，大小写变体的私密备份也排除；前端按锁文件重新安装后构建。安装版始终使用受控研究执行模式，避免安装路径改变工具边界。
- Mac 交付为已签名、公证的 Apple Silicon App 和签名 DMG，构建号40、引擎0.153.4；从最终DMG复制后的实际计算及退出/重启保持验证通过。安装包与更新说明见 [v1.1.0 Release](https://github.com/simonlin1212/Vibe-Research/releases/tag/v1.1.0)。
- 以下里程碑为开发历史；旧版本号、提交状态和测试数量仅代表各次记录。本轮结果见 [M40 发布候选与隐私验收](docs/发布候选与隐私验收_M40_2026-09-07.md)，实际公开状态以Release页面为准。


- M24：左上角 AI 来源旁增加“开启Agent”，默认关闭，与设置页同步。订阅和 API 均支持无研究工具的普通对话，
  开启后恢复完整 Agent；旧自动开启值迁移为关闭，明确选择跨刷新保留，同来源重测不重置。
  切换中止在途聊天（含连接预检），不影响已启动的独立研究任务。详见 [M24 验收](docs/Agent开关与普通对话_M24_2026-09-06.md)。

- M23：首页以 Agent 对话为首屏，未接入时弹出三种订阅快捷测试和其他接入方式；左上角显示实际已保存来源。
  测试失败、取消或配置被其他页面更改时不覆盖配置。交流框下用五个并排分类框展示 12 个一级栏目小按钮，保留原侧栏。
  本地 Claude 真实接入、浏览器交互与验收边界见 [M23 验收](docs/首页快捷接入与功能导航_M23_2026-09-06.md)。

- M22：普通 Agent 聊天接通联网搜索、网页读取、数据取数、确定性计算/回测及研究记录等 13 个工具入口，
  显示真实调用记录，不再把正常聊天固定为无工具模式。长研究通过界面一次性确认后启动，保持所选 AI 来源；
  计算目录补真实参数契约，取数失败不标成成功。Codex 与 Claude Code 本机实测及四轮增量审查记录见
  [聊天工具开放验收](docs/聊天工具开放验收_M22_2026-09-06.md)。本地开发，不代表所有平台/来源已验收或发布。

- 同步上游截至 `09e8404` 的 Issue #44 与 PR #34（原贡献 @WenhuaXia），保留未提交双引擎开发成果。
  MCP 发现与执行共用产品 CODEX_HOME；新增默认关闭的 `VRA_LAN=1` 并接入两种启动器，API 仍绑回环。
  本地适配在代理补 token 前增加同源检查，保留远程 HTTPS/本机 HTTP 边界及全站一次 AI 接入；
  自托管模板未实测，不内置转换网关。回测、页面分析/反思与资料任务不依赖安全上下文的 randomUUID。
  包版本同步上游基线 1.0.4，不代表未提交双引擎改动已发布。
- M12–M14 本地开发：Claude Code / WorkBuddy 辩论、Agent 回测与受控资料转写补齐，持仓页新增截图／表格
  人工核对入口；不会上传后自动写台账，不暗换 AI 来源。真实路径与失败轮次边界见 [M12](docs/跨来源业务验收_M12_2026-09-05.md)。
- 剩余已确认审计问题完成分批整改：Sortino、初始等权持有对照、权重约束、路径及诊断错误、行情跨期对照、
  PE 来源披露、行业校验契约、Windows 就绪探针与目录漂移，见 [M13](docs/剩余审计整改_M13_2026-09-05.md)。
- Codex 同会话在途保护、可取消的分批资料检索及 mootdx 安装/体检依赖补齐；串行后端 795 项（794 通过、
  1 Windows ACL 跳过），前端 51/51、Python 699/699。当前仍是未提交开发版，不是发布就绪；
  full 实跑、端点失败与 Windows 边界见 [M14](docs/完整版本机验收_M14_2026-09-05.md)。
- 辩论进程树停止未确认时，保留明确失败原因与此前已完成内容，不误报“已取消”或“全场未启动”，
  不自动归档为完成报告；跨模块审查及后继两轮修复复审闭环。
- 根路由增加中文异常恢复页，区分找不到页面和运行异常，提供重新加载与返回首页；不展示原始错误正文，
  不清空本机配置，提醒未保存输入可能丢失。渲染回归、真实浏览器和独立复审通过。
- 受控计算工具复用现有引用契约，在执行前拒绝错误的证据/计算 id 格式，避免到整阶段结束才反馈；
  MCP/直连同源校验，不改变真实计算失败记录的保留规则。红绿回归、全量后端测试与独立复审通过。
- 报告、附录及内嵌报告的查看器只在运行结束且最终状态一致时开放；未校验草稿保留本地，但不经 HTTP/MCP
  当作正式报告展示。资料不完整的合格报告仍可阅读并提示缺口；不再承诺固定十几分钟或对单标的取全部端点。
- 变化提醒沿用当前数据目录，页面查询不再写比较缓存，CLI 保留显式归档能力；补数据根隔离、只读与路径回归。
  两项经真实浏览器验证、独立 Codex 审查及后继复审，无可行动回归。
- WorkBuddy 六阶段 full 实跑 complete / exit 0，1,011 条证据、23 条计算、最终错误与 gate 命中均为零；
  约 112 分钟且画像/风险/报告各三次尝试才通过。Claude 同范围因额度限制失败，保留记录，不推广为所有来源成功。

- M11 深度研究取消本机闭环：取消与最终归档以原子决策互斥，等待取数、Codex SDK 和订阅 CLI 子进程树
  退出后确认取消；刷新后可从历史继续跟踪，已完成阶段保留。三轮实际 Codex 审查修复五项，后端 744 通过、
  1 Windows ACL 跳过，前端 44/44、Python 648/648，类型/构建通过。生产 HTTP/React 加合成 worker 浏览器
  验收通过，真实模型与 Windows 不在本批结论内。未提交，见 [深度研究取消验收](docs/深度研究取消验收_2026-09-05.md)。

- 2026-09-05 本地检查点：M8 与原 14 项优先 P2 整改已按 Simon 授权提交为 `2ed80ac`，提交前 Gitleaks
  扫描无泄漏；未 push、tag 或 Release。下方“未提交”保留为当时的历史状态，不覆盖本条。
- 后继批次修复跨数据根快照缓存、普通工具继承多余模型凭据、同名辩论覆盖，以及辩论请求取消传递；
  暂未提交，详细验证与复审状态见 [验收记录](docs/运行隔离与辩论中止验收_2026-09-05.md)。
- 后续三项修复：资料检索改按同页窗口的不同查询词覆盖选择片段，避免封面重复词抢占且不串页；
  今日复盘只发送一份页面摘要；计算 JSON 工具保留确定性展示串及嵌套情景展示，不改变原值与失败状态。
  独立 Codex 复审无可行动回归；后端 730 项（729 通过、1 Windows ACL 跳过）、前端 40/40、Python 648/648，
  类型检查/构建通过。仍未提交，边界见 [检索与计算展示验收](docs/检索与计算展示验收_2026-09-05.md)。

- 2026-09-05：整仓审计原 14 项优先 P2 完成本地修复、回归与独立复审。以下为接续 COV-F1–F4 的十项收口，
  不是客户完整版或发布就绪声明；M8 与整改仍未提交、未推送、未发布。
- PE 分位序列现在绑定实际文件、列、过滤、日期列与选中期间；报价时效未知时要求同日 K 线和正成交量佐证。
  回测补齐已接受号段的板块/历史边界，并强制披露价格来源、复权、未模拟现金分红及历史重述风险；特殊交易
  情形未建模的边界保留，不承诺全市场微观规则或总回报精度。
- 合并取数按调用方独立取消；查看器通过固定脚本哈希恢复交互、保留 CSP 与无同源 sandbox；API 地址允许
  字面 loopback HTTP，仍拒绝远程明文连接，不绕过模型直连能力验证。
- 首页提供今日复盘、公司研究、已有研报三个受控页面入口，普通聊天不承诺自动取数或全网收集；页面独立
  分析与反思使用各自会话；自选行情显式重新取数，显示最早价格证据的快照时间，不再标成“每 3 秒实时”。
- 十项分三个范围由实际 Codex 独立复审，均无可行动 P1/P2。最终 orchestrator **721 项**（720 通过、
  1 Windows ACL 跳过）、desktop **37/37**、Python **640/640**，类型检查及构建通过；实际 Chrome 合成数据
  验证首页路由、快照和查看器交互/安全共 15 项。构建包大小警告保留，订阅六阶段修后重跑、全端点、Windows
  真机、其余 P3 与跨来源业务缺口不在本次闭环结论内。下方旧数字保留为各阶段历史检查点。

- M8 完成 WorkBuddy 订阅引擎在核心数据范围（`endpoint_scope=core`）的真实六阶段验收：
  `m8-workbuddy-full-3` 用 102 分 54 秒完成 profile / financials / estimates / valuation / risk / report，
  最终 `complete / exit 0`，产出 119 条证据、29 条计算记录（22 成功、7 失败尝试留痕）、0 处跨源冲突；
  财务阶段一次修复错误计算参数，报告阶段一次超时后重试成功，六阶段
  最终均由 validator 判定通过。
- 两次先行实跑暴露并修复报告数字忠实度问题：被忽略区段现在保持原始字符位置，不再把证据 id 内的数字
  错认成正文主张；重试反馈会按原行汇总**全部**不匹配数字，不再只展示前三条或截断为 12 行。报告补跑
  优先修正既有文件，逐行对齐同段证据 id 或 `output.display`，避免每次从头生成。
- 本机 Agent 的确定性前置错误现在立即终止当前阶段，不再把“未安装 / 未登录 / 版本过旧 / 配额不足”
  重试 18 次；运行入口即使探针失败也会先建立 manifest，再通过真实运行路径写出可恢复的失败终态。
  Claude Code OAuth 过期时，`m8-claude-auth-terminal` 在 5 秒内形成 `failed / exit 3` 的完整 manifest，
  并给出重新登录提示。Simon 随后完成登录，本机 Claude Code 2.1.261 的 `m8-claude-full-2` 用 27 分 43 秒
  跑完核心数据范围六阶段：119 条证据、25 条计算记录（24 成功、1 失败尝试留痕）；最终各阶段 validator
  通过，报告首轮 17 处数字引用问题修订后通过。仍保留早期历史财务期不足等 gaps，状态为
  `incomplete / exit 2`，不能写成 complete。两种订阅的全端点 `full` 范围均未在本轮验收。
- 订阅 Agent 单阶段默认上限由 20 分钟校准为 30 分钟，显式用户配置仍原样尊重。WorkBuddy 最终报告首轮
  在 30 分钟上限退出，第二轮随后正常完成；此前也发生过 20 分钟超时。该记录支持增加长报告运行余量，
  但不能仅凭超时证明 CLI 当时仍在有效生成，速度和超时诊断仍需改善。
- 干净 macOS 临时副本完成从零 `scripts/setup` 与 `scripts/start --no-open`：初始化、依赖安装、API 8765、
  UI 5930、鉴权健康检查与退出后端口释放均通过。Windows 契约 / 路径 / PowerShell 模拟为 114 项中的
  113 通过、1 项真实 ACL 因当前为 macOS 跳过；没有 Windows 主机或允许 push 的远端 runner，故不宣称
  Windows 真机干净安装已经验收。
- M8 独立 Codex 五轮审计依次发现并修复“重试反馈仍会截断”“探针失败早于 manifest”“旧 CLI 错误仍会
  重试”等真实问题；最终两轮均为 `No actionable P1/P2 findings`。最终回归：orchestrator **714 项**
  （713 通过、1 项 Windows ACL 在 macOS 跳过）、desktop **34/34**、Python **577/577**；两端类型检查、
  Vite 生产构建、Core 纯净度 0（63/63）与 diff 格式检查通过。M8 改动仍未提交、未推送、未发布。
- 登录恢复后，按 Simon 要求另由真实 Claude Code 完成四轮只读整仓范围审计，主审已复核并用隔离诊断
  复现关键问题。新确认 14 项优先 P2，包括首页任务未接执行入口、跨功能会话共享、行情快照新鲜度、
  并发取消耦合、证据查看器 CSP、回测规则与披露、序列输入溯源、报价二次验证及错误分类/脱敏等。
  这些是更大范围新发现，审计结束时尚未修复；此前 M8 增量审计通过不等于整仓已可发布。详细覆盖、误报与待验证项
  保存在本地审计交接材料；部分数据源族仅抽查，Windows 真机和扩展端点全范围仍待验收。
- 整仓审计首批关闭 COV-F1–F4，原 14 项优先 P2 仍有 10 项待修：运行期回测限制进入服务端强制披露，
  不截断长提示来规避契约；取数/信号失败保留可读 JSON 原因，部分取数失败与样本不足同时说明。
  Windows 单/双反斜杠用户名路径完成脱敏；通用 HTTP 404 与模糊 403 不再冒充业务无数据，FINRA/SEC
  保留有界日期/形式回退，FINRA 空结果不再推断小票/覆盖范围。合法文件校验兼容当前小数成交量，
  真实官方 2026-09-03 文件 12,266 条解析通过，未丢弃分数股数值。
- 首批修复经过 3 轮独立 Codex 复审：r1 两个真实错误链遗漏均修复，r2 闭环；其后实连发现的新整数
  校验误伤已修复，r3 专项复审通过。最终 Python **623/623**、orchestrator **717 项**（716 通过、
  1 Windows ACL 跳过）、desktop **34/34**，两端类型检查与构建通过；仍有主 JS 包大小警告。
  本批未重跑订阅六阶段、全端点或 Windows 真机；M8 与整改仍未提交、未推送、未发布。
- M7 把 Claude Code / WorkBuddy 订阅接入正式六阶段研究：新增独立 `local_agent` 引擎与逐阶段 CLI 运行器，
  不继承 OpenAI provider、Codex 路径、`CODEX_HOME`、hooks 或模型名，也不把它们冒充 Codex Harness。
- 本机订阅 Agent 每个阶段关闭内建工具，只开放既有五个 VRA MCP 工具；工具 schema、阶段所有权、确定性计算、
  文件边界与最终 validator 继续共用一份实现，没有复制第二套研究状态机。自然语言回答或退出码 0 不构成完成。
- WorkBuddy Deep 首轮显式等待 MCP，并按官方加载机制设置 `alwaysLoad / defer_loading=false` 与
  `NoDefer(mcp__vra__*)`。本机 WorkBuddy 5.2.6 / CodeBuddy Code 2.106.4 已真实完成 profile 阶段：
  23 条证据、一次尝试、阶段 `complete`、validator 通过；刻意只跑一个阶段时整次仍如实为
  `incomplete / exit 2`。这不是完整六阶段真实运行验收；Claude Code 路径已由机器测试覆盖，但本轮没有冒充
  做过真实 Claude 订阅运行。
- M7 独立 Codex 首轮审计确认 4 个 P2：阶段超时被错误压到 10 分钟、父进程退出可能遗留订阅 CLI / MCP
  进程树、订阅运行错误继承默认模型名，以及任务路由把本机 Agent 冒充 Codex Harness。四项均已修复并加回归；
  收敛复审结论为 `No actionable P1/P2 findings`。
- M7 最终验证：orchestrator **708 项**（707 通过、1 项 Windows ACL 在 macOS 跳过）、desktop **34/34**、
  Python **577/577**；两端类型检查、Vite 生产构建与 diff 格式检查通过。M7 已本地提交为
  `b18799b feat: run subscription agents through six-stage research`，未推送、未发布。
- M6 新增 WorkBuddy / CodeBuddy 订阅适配器：设置页把它作为第三个真实订阅来源，
  已安装并登录 WorkBuddy 桌面版时直接复用应用内置 CodeBuddy Code CLI；独立安装 CLI 的用户也可沿用
  原有登录，不再强迫这类用户重复安装、重复登录或填写 API key。
- CodeBuddy 探针通过官方 stream-json initialize 控制请求判定登录，只向界面返回版本和布尔状态；
  账号与 token 不返回、不落日志。执行时强制关闭工具、MCP、用户设置、自动记忆、后台任务与子 Agent，
  并移除 API key、自定义端点和模型覆盖环境变量。WorkBuddy 内置旧 CLI 缺少无会话参数时，订阅凭据只在
  内存中转给一次性临时用户目录中的回答进程，完成后删除，防止写入真实用户目录。
- M6 当时的边界如实保持在对话与有界材料任务；完整六阶段研究由后续 M7 独立实现。旧的
  “CodeBuddy 适配器尚未完成”引导已移除。
- M6 使用腾讯官方 CodeBuddy Code 2.143.1 完成干净环境实装：版本探测、未登录识别及退出码为 0 的纯文本
  登录错误均被稳定归类；又使用本机 WorkBuddy 5.2.6 内置 CodeBuddy Code 2.106.4 的真实订阅登录完成
  受限回答，验证桌面端自动发现、内存凭据桥与临时用户目录清理。同时补齐 Windows 常见安装位置的内置 CLI 发现，
  无扩展名入口由 Node 安全启动，旧版隔离运行同时改写 HOME / USERPROFILE / APPDATA / LOCALAPPDATA
  及 XDG 目录，不留下从 Windows 绕回真实用户目录的口子。最终回归：orchestrator **699 项**（698 通过、1 项 macOS 跳过）、
  desktop **34/34**、Python **577/577**，两端类型检查与 Vite 生产构建通过。独立 Codex 首轮审计对
  `--setting-sources none` 的唯一意见经官方源码与真实命令证伪，收敛复审为
  `No actionable P1/P2 findings`，未按错误建议放松隔离。补齐 Windows 发现与用户目录隔离后再做一轮独立收敛复审，
  结论同样为 `No actionable P1/P2 findings`。
- M5 增加 macOS / Linux `scripts/setup` 与 `scripts/start`：普通用户不再手装两套依赖、全局安装 Codex
  或打开两个终端。启动器会检查安装状态与固定端口，同时管理 API / 界面进程，读取实际数据根完成带鉴权
  健康检查后才打开浏览器；任一进程退出会同步收口。
- 干净目录实装发现 npm 11 在依赖目录不存在时不能可靠处理绝对 `--prefix`；POSIX 安装器改为进入两个包目录
  后执行 `npm ci`，不再让已有 `node_modules` 掩盖首次安装失败；两端安装器也不再让 npm 审计网络阻塞
  已按锁文件完成的首次安装。
- Vite 固定使用 5930，端口被占用时明确失败，不再静默漂到 5931；首屏连不上后端时直接显示各平台的一键
  启动命令和“重新连接”按钮；初始化与代理错误也不再把内部 Node / CODEX_HOME 命令交给普通用户。
- M5 独立 Codex 审计发现并修复 1 个真实 P2：启动健康检查完成前或关闭终端时，尚未注册停止信号会让
  detached 的 API / 界面成为残留进程；现于首个异步等待前监听 Ctrl+C、终止与关闭终端信号，真实启动期
  中断验证两端口均释放。末轮复审为 `No actionable P1/P2 findings`。
- 重构 AI 接入与执行方式：第一次打开只选择订阅或 API，连接成功后默认开启 Vibe Research Agent；
  设置页用第二张独立卡控制 Agent / 模型直连，全站共用同一份来源与执行方式。
- 只有通过真实能力探针的 API provider 可以关闭 Agent。订阅登录与未验证 API 不会被伪装成裸模型直连；
  直连普通对话和标题翻译走受限 API 传输层，研究、辩论、资料转写和工具任务在副作用前返回 `agent_required`。
- 「我的研报」移除 Auto / Quick / Deep 三选一，系统继续在内部按任务语义路由；路由指纹同时绑定 AI 来源
  与 Agent 开关，不允许路由与执行阶段偷换 provider 或模式。全新浏览器无明确配置时进入“接入 AI”，
  不再借后端默认值替用户做选择。
- 设置页、README 与官网如实区分 Codex Harness、Claude Code Agent、WorkBuddy / CodeBuddy 和模型 API；
  M6 完成前的 API 引导已由真实本地适配器取代。
- 本轮独立 Codex 审计发现并修复 3 个真实 P2：原始工具入口绕过执行方式、Claude 订阅进入尚不支持的
  辩论 / 转写 / 引导工具流程，以及默认模型来源指纹没有绑定后端实际配置；末轮复审为
  `No actionable P1/P2 findings`。
- 最终验证：orchestrator **691 项**（690 通过、1 项 Windows ACL 在 macOS 跳过）、desktop **34/34**、
  Python **577/577**；两端类型检查、Vite 生产构建、Core 纯净度 0（60/60）与真实浏览器首次接入 / Agent
  默认开启 / 直连开关 / 官网入口断言全部通过。

- 完成双引擎任务架构 M4 收口：公开产品固定为 Browse / Quick / Deep，Deep 仍由 Codex 六阶段承载；
  既有 Direct 六阶段只保留为显式 `--experimental-direct-deep` 开发者入口，公共 `/research` 与 MCP 均拒绝它。
- 把通用 `AgentRunner / EventsLog` 从 Codex 专属 runner 抽离，Codex SDK 只在 Codex 分支动态加载；阻断全部
  `@openai/codex*` import 的子进程探针仍可加载 Direct 路径，Direct 也不再探测 Codex 二进制。
- 运行清单如实区分能力：Codex 为 `constitution_and_skills`，Direct 为 `stage_prompt_only`；Direct 记录真实
  provider / model 并把 Codex 路径与二进制置空，Codex 使用 provider 默认模型时也记录实际解析值。
- 修复 MCP 静默剥掉 `engine=direct` 后启动默认 Codex、调用方注入环境的 Direct 密钥可能漏入事件账本，
  以及 `user_reports` 16 份产品上限与 schema 不一致。真实 Direct + MiMo 单阶段运行得到 23 条证据、
  profile 完成且 validator 通过；整次因刻意只跑单阶段如实为 `incomplete / exit 2`。
- M4 独立 Codex 审计确认并修复 3 个 P2，自查修复 1 个密钥脱敏问题；一条 hooks 意见经真实构造链与
  运行清单证伪，最终复审为 `No actionable P1/P2 findings`。
- M4 最终验证：orchestrator 670 项（669 通过、1 项 Windows ACL 在 macOS 跳过）、desktop 30/30、
  Python 577/577；两端类型检查、Vite 生产构建与 Core 纯净度 0（60/60）全部通过。
- 完成双引擎任务架构 M3：统一 `/tasks` 现可把 Deep 路由交给既有 Codex 六阶段研究，并通过
  `/tasks/resume` 按运行 ID 与路由指纹恢复；没有新增第二套研究状态机，旧 `/research` 保持兼容。
- Deep 任务把关注点、唯一 A 股标的和最多 16 份圈选资料的版本绑定到运行，实际读取时再次核对版本；缺失、
  变化、多标的或其它市场资料均在启动前拒绝。16 份材料各自至少有一个片段进入 40K 专用上下文，未勾选资料
  不会因同代码被扩入。
- 页面会持续读取六阶段进度；运行中 manifest 的占位退出码不再误判为失败，只有 `complete + exit 0` 才显示完成；
  启动目录一分钟仍未出现时明确失败，不再永久轮询。独立 Codex 三轮审计共发现 6 个真实 P1/P2 并全部修复，
  最终复审为 `No actionable P1/P2 findings`。
- M3 全量验证：orchestrator 663 项（662 通过、1 项 Windows ACL 在 macOS 跳过）、desktop 30/30、
  Python 577/577；两端类型检查、Vite 生产构建与真实浏览器功能断言全部通过。
- M3 真实 Deep 运行经统一 `/tasks` 启动并由 `/tasks/resume` 收口：六阶段全部实际执行，生成 1,099 条证据、
  20 项计算和正式报告，圈选资料引用进入报告。因一个外部财报日源失败，底层状态如实为 `incomplete / exit 2`，
  任务层准确映射为 `failed` 而未冒充完成；临时测试资料已删除并复核资料库恢复为空。
- 完成双引擎任务架构 M2：新增统一 `/tasks` 两段式任务入口、真实研报材料与确定性操作适配器，以及
  「我的研报」Auto / Quick / Deep 任务面板。执行必须携带路由指纹，材料变化时拒绝而不是透明换路线；
  M2 的 Deep 只显示路由结果并明确未启动长流程，旧 `/research` 保持兼容。
- Quick v1 只用一次直连模型请求从服务端预切段落中定位原文；材料版本、完整段落引用、取消、合规 gate、
  超时和输出上限均在服务端校验。确定性任务已接真实页面查询、数据源刷新与计算注册表。
- M2 阶段验证：orchestrator 653 项（652 通过、1 项 Windows ACL 在 macOS 跳过）、desktop 29/29、
  Python 577/577；两端类型检查、前端生产构建、真实浏览器 Deep 路由与临时资料清理通过；最终 Codex 复审
  `No actionable P1/P2 findings`。

## [1.0.4] - 2026-09-05

上游发布记录：Issue #44 修复 Codex MCP 隔离发现时使用了用户配置目录的问题。
来源 `9633210`，发布提交 `500f0f9`；本地通过适配补丁同步，未移动开发分支 HEAD 或创建发布。
PR #34 在该 Release 之后进入 main，不属于 v1.0.4 标签内容。

## [1.0.3] - 2026-09-02

V1.0.3 收口 Issue #38 / #39 / #40：普通对话不再被资料库泛词误召回，设置页连接检测改走专用探针，
Node 运行时要求写准并在 CI 补齐 Node 22。

- 修复 #40：「接入 AI」的「测试并保存」不再借用普通 `/chat`。新增 `/llm-probe` 专用探针：后端生成
  一次性令牌、固定 developer 指令、不召回资料库、不复用线程，成功判据是模型
  回复里原样出现本次令牌。除不召回资料外与业务对话走同一条路（同一份开场白、developer 层与合规 gate、
  自由文本回复），仍是仓库规则要求的「真实对话测试」。此前探针语句会被「连接 / 检测 / 成功」等二字片段
  命中用户资料，资料片段随之发给正在测试、尚未验证的 provider；而模型只回一句「连接成功」又会因缺少
  `[资料:…]` 引用被判成测试失败。
- 修复 #39：`/chat` 不再对每条消息全库检索。新增 `reportRecallPlan`：只在用户明确提到资料库 / 上传 /
  附件 / 原文、消息命中某份报告的证券代码、或命中报告文件名主体词时召回，后两种只在命中的报告内检索；
  「今日」「市场」「数据」等泛词不再触发。个股研究按代码强制召回的路径不变。
- 修复 #38：`engines.node` 从 `>=22.6` 改为 `>=22.18`（与 README 一致，原生 TypeScript 类型剥离默认
  开启的最低版本），desktop 补同样声明；`npm test` / `npm run run` 前先跑 `scripts/check-node.mjs`，
  Node 版本不够或构建未启用 TypeScript 支持（`process.features.typescript` 为 false，见于部分 Linux
  发行版打包的 Node）时给出明确指引，而不是 `ERR_UNKNOWN_FILE_EXTENSION`；`scripts/init`、`scripts/doctor`、
  `start.ps1`、`setup-windows.ps1` 等直接执行 `.ts` 的入口同样先过预检；CI 矩阵新增 Node 22。
- 新增回归：探针令牌 / 一次性线程 / 无资料注入 / 错误回填判失败；召回计划的泛词、明确意图、代码边界、
  标题主体四类用例。
- Codex 首轮审计 4 条（1 P1 / 3 P2）全部核实为真并修复：英文主体词改整词匹配（`cat` 不再命中 `education`，
  否则一份无关私有资料会被送去模型）；连字符文件名拆词（`apple-annual-report` → `apple`）；中文只剥首尾停用词、
  剩余不足 2 字即停（公司名里含通用词不再被剥空）；`probe_bad_output` 进入前端可原样显示的错误码白名单，
  不再被吞成「连接失败」。
- Codex 第二轮 4 条：英文虚词与公司后缀（for / the / inc…）不再构成文件名主体；点名带扩展名的完整文件名
  （`summarize notes.txt`）即视为显式指定该资料；预检接入全部直接启动入口；探针改走业务对话同一路径（见上）。
- Codex 第三轮 2 条：按页问答的消息由前端以「【问题】」分隔页面上下文与用户问题，召回意图与检索打分只看用户
  问题（页面上下文里的「近期公告」、代码不再触发）；明确要资料且停在某主体页面时只召回该主体的资料；
  公司全称后缀（股份有限公司 / 集团…）计入通用停用词，「贵州茅台股份有限公司…年度报告」剥出「贵州茅台」。
- Codex 第四轮 3 条：问题里明确指到的目标（完整文件名 > 代码 > 文件名主体词）优先于页面主体，「资料里怎么说 300750」
  停在 600519 页面时召回的是 300750 那份；港股写法 `700.HK` / `HK:700` 规范成入库的五位码再比对；两个
  package-lock 根包的 `engines` 与 package.json 同步（`npm version` 不会更新它）。
- Codex 第五轮 1 条：明确要资料但问题无目标时，页面上下文里列出的**完整文件名**也算页面主体（「我的资料」页
  的建议问句对没有代码的主题类资料不再漏召回）。
- Codex 第六轮 2 条：字母代码只认**大写整词**（`ON` / `$ON` / `ON.US`），文件名里本就大写的词同样要求大写匹配，
  「focus on margins」「my cat is sick」不再命中 onsemi / CAT 的资料；「这两份报告」「所有报告」「these reports」
  这类量词式指称计入明确意图。
- Codex 第七轮 4 条：点名文件按边界比对（`meeting-notes.txt` 不再连带 `notes.txt`）；代码分支把入库形式的代码并入
  打分查询（问 `700.HK` 选中了 `00700` 那份却打 0 分）；「this / that / the report」英文单数指称计入明确意图；
  页面上下文列出 ≥3 个文件名视为列表页（前端只显示最新 60 个），不按它限定召回范围，但文件名仍进检索键。
- Codex 第八轮 1 条：常驻控制台的「（我正在看「某页」这一页）」前缀也算页面上下文，「我的研报」页标题不再触发召回。
- Codex 第九轮 3 条：页面范围内代码命中与文件名命中取并集（「比较这两份」不再丢掉无代码的那份）；页面上下文里的
  港股写法同样规范成入库码；中文文件名按体裁词（报告 / 点评 / 分析 / 深度…）切段取候选主体，「贵州茅台深度报告
  消费税影响分析」也能剥出「贵州茅台」。
- Codex 第十至十四轮（召回细节）：问题里点了数量（「比较这三份报告」）且页面恰好列了那么多份时按页面那几份来；一句话里
  混用文件名 / 代码 / 主体词时三类目标取并集；打分查询键在前、问题在后（长问题不再把键挤出检索窗口）；点名文件名按
  Unicode 边界整段匹配、且被已知更长文件名覆盖的出现不算（「年度报告.md」不再连带「报告.md」，「meeting notes.txt」
  不再连带「notes.txt」，库里没有的长文件名也不会命中它的短后缀；中文句子里文件名要与前后文以空格 / 标点隔开，紧贴着写的
  仍可经文件名主体词召回）；意图门槛的数量词与
  `quantifiedCount` 对齐（中文到十、英文到 ten、阿拉伯数字）。
- **不做「跟进沿用上一轮召回范围」**：第十轮曾按会话记住上一轮命中的资料让不带目标的跟进句沿用，随后四轮审计连续指出
  沿用条件过宽（换页 / 换主体 / 换凭据 / 非承接句 / 普通疑问词仍沿用、指纹不稳），最终移除——服务端猜「这句是不是
  跟进」不是可靠的资料边界。跟进句要用资料请点名（代码 / 文件名 / 「这份报告」）；否则不召回，线程按既有设计随召回
  集合变化重开。
- Codex 第十五至二十九轮：文件名边界与覆盖规则如上；金融垂类意图补「这份年报 / 财报里」这类带明确指称的说法
  （「年报季到了」仍不触发）；英文复数指称补 attached / uploaded；明确选中 N 份时检索给 N 个位置并按份数放宽
  上下文字数（此前固定 5 份 / 12k 字，「比较这六份报告」会静默少一份），仍放不下时在上下文末尾明示「比较不完整」；
  查询组装给用户问题保留窗口（列表页 60 个文件名不再把问题挤出检索的 500 字 / 80 词）；点名文件后，文件名自带的代码 /
  公司名不再把同名兄弟文件并进来（遮罩按空白灵活匹配）；选中份数超过检索 20 份硬上限或有几份没打到分时，同样在
  上下文末尾明示不完整；`AI` / `EV` 这类两字母全大写主体保留；键优先时先给键留预算、单键截到 160 字，超长文件名不再被整体丢弃；
  明确要资料且停在没有列文件名的主体页时，页面上下文里的公司名 / 主题名也算页面主体（没有解析出代码的资料不再漏）；
  明确意图的措辞收紧：裸的「资料 / 文档 / 报告中」「the report」不算，须带资料库 / 上传 / 附件 / 我的 / 指示词等限定
  （「如何给技术文档添加目录」不再触发），「这一份报告」计入；裸的 PDF / DOCX 同样须带指示词；document / file 这类
  通用名词计入英文停用词；「所有报告 / 总结全部文件 / all reports」圈定整个资料库、选中的不再按相关性过滤（检索
  20 份硬上限或字数上限放不下时提示不完整）；4 / 9 开头的六位代码
  （北交所老号段 / 新号段）入库时可识别（资料索引 schema 升到 3，旧索引读取时按新规则重算），问题里的裸代码即使没登记也
  只按代码本身打分兜底（仅限合法号段，日期 / 金额式六位数不算）；「every report」计入全库请求；垂类可经
  `Plugin.reportRecall.documentNouns` 注入自己的资料名词（金融：研报 / 年报 / 财报…），「总结所有研报」「比较这六份财报」
  同样参与全库 / 数量判定（含初始意图判定）；「所有的报告」「all (of) the reports」这类带助词 / 冠词的写法也认；
  裸「上传」不算意图（须为「已上传 / 上传的 / 上传了」）；点名多个文件时遮罩按长度降序，短名不再把长名剩下半截；
  「我的资料」页内置建议改为「帮我给这些资料排个阅读顺序」以便触发召回；页面列了文件名时只按文件名圈定（代码 / 主体词
  只在没有文件名可认时兜底，没列出的同代码旧文件不再被并进来）；资料索引版本要求整数，畸形值判损坏而不是静默迁移；
  「总结 300750 的所有报告」这类带目标的「所有」只作用于该目标（这几份全部注入），没有目标时才是整个库；≥6 字母的全大写文件名词
  （NVIDIA）按公司名不分大小写，只有 ≤5 字母才当 ticker。
- 最终验证：orchestrator **571 项**（570 通过、1 跳过）、desktop **26/26**、Python calc 201 + backtest 139；
  Node 22.23 官方二进制下 chat / report_library / core_purity / plugin / release_version **113/113**；
  两端类型检查与前端生产构建通过；Core 纯净度棘轮保持 0。

## [1.0.2] - 2026-08-31

V1.0.2 修复 Windows 用户在 V1.0.1 接入 API 后，Agent 对话可能全部报“本地 Agent 暂时没有连接成功”的问题。
故障发生在模型请求之前，与 DeepSeek 等 API key 是否有效无关。

- 修复 MCP 隔离配置生成：不再递归扫描并提升未启用 profile 或 Apps 运行时中的 MCP 名称，避免生成只有
  `enabled = false`、却缺少 `command` / `url` transport 的根配置，触发 Codex 0.149
  `invalid transport in mcp_servers.<name>` 启动错误。
- MCP 隔离改为以产品捆绑的官方 Codex 在相同 `CODEX_HOME` 与工作目录下解析出的当前有效集合为准；
  预检阶段同步关闭 Apps、MCP Apps 与 plugins，只禁用该引擎入口实际报告的有效 MCP，无法核验时继续
  失败关闭。
- 新增真实引擎回归：构造“根配置有有效 MCP、未启用 profile 含 `codex_app`”的配置，要求产品生成的
  覆盖能被捆绑 Codex 直接解析，并且只禁用当前有效 MCP，不再把休眠配置投影到根层。该测试会在
  Windows、macOS 与 Ubuntu CI 中随平台契约运行。
- 最终验证：orchestrator **540 项**（539 通过、1 项 Windows ACL 专项在 macOS 跳过）、desktop
  **25/25**、Python **575/575**；两端类型检查与前端生产构建通过，联网体检 **20/20**。

## [1.0.1] - 2026-08-30

V1.0.1 是 Windows 与公开 Issue 收口版本：补齐 Windows 原生 PowerShell/CMD 接入和受控研究执行层，
合并 Claude Code 对开源版的审计整改，并复核仓库全部未关闭 Issue。

- 新增 Windows 11 原生支持，不依赖 WSL：提供 PowerShell/CMD 初始化、体检和一键启动入口，补齐
  Windows venv、PATH/PATHEXT、用户目录、临时目录、DNS 探针、进程树终止和文件打开语义。
- Windows 六阶段研究采用受控 MCP 执行层：关闭 Shell、unified exec、图片读取、子代理、插件、应用和
  联网搜索，线程只读，只能读取净化后的运行产物、调用确定性 calc、写当前阶段 JSON 与最终报告。
  该设计绕开上游 native Windows PowerShell lifecycle hook 尚不完整的问题，同时保留编排器与 validator
  的机器校验边界；macOS/Linux 继续走既有 hooks 链路。
- 新增 `windows-latest` / `macos-latest` / `ubuntu-latest` CI 矩阵；orchestrator 新增 Windows 路径、
  环境、只读线程、受控工具读写、NTFS 私密 ACL、JSON 账本路径与真实 calc 回归，当前 **539 项**
  （本机 538 通过 + 1 项 Windows 专项跳过），Core 行业词仍为 0。Windows 还会以两个真实 Python
  进程验证东财请求锁确实互斥；Unix 跑完整 orchestrator 套件，Windows 跑平台契约与全量 Python 回归。
  Windows CI 统一以 UTF-8 解码 Python 的 JSON 管道，Node 动态加载的测试路径改为标准 `file://` URI。
  PowerShell 入口保留 UTF-8 BOM，兼容 Windows PowerShell 5.1 对中文脚本的编码识别。
- 开源版审计修复：研报索引升级到 v2，收紧 A 股代码识别并支持公司名召回；删除板块页写死的
  `300308`；缺失行情不再伪装成 0，二十日资金流只有数据完整时才合计，官网 Star 获取失败显示未知。
- 合规 gate 补齐常见动作建议与个股评级赋予语境，同时保留机构评级篇数统计；回测工具把自标的基准和
  换手不足口径作为服务端强制可见披露，不能由模型省略或藏进 HTML 注释。
- 最终验证：orchestrator **539 项**（538 通过、1 项平台跳过）、desktop **25/25**、Python **575/575**；
  两端类型检查与前端生产构建通过，联网体检 **20/20**，Codex 收敛复审无可操作 P1/P2。
- 重建 [viberesearch.wiki](https://viberesearch.wiki) 官方网站，主叙事升级为“基于 OpenAI Codex Harness
  打造的本地金融研究 Agent”，并展示当前工作台、模型接入、回测 Agent、研报库与证据纪律。
- 官网源码迁入 `website/`，中英文 README 恢复官网入口；Release 链接使用稳定的 `releases/latest`，
  具体版本号由 GitHub API 动态同步，避免后续发布时出现新旧版本并存。
- Issue #26：当前全球行情链在搜索接口返回空结构时会自动逐市场直查，美股与港股代码不再因搜索端点
  异常整块失效；新增搜索空结果和港股短码回归测试。
- Issue #21：事件概率已接入 Kalshi + Polymarket 实时公开定价；修复 `Borussia` 命中 `russia` 导致
  体育比赛被误分为地缘政治的问题，结构化体育元数据现在优先于标题子串。
- Issue #20：API 与 MCP 继续从 `orchestrator/package.json` 读取唯一产品版本，并新增发布元数据一致性测试，
  强制同步 orchestrator、desktop、两个 lockfile、中英文 README 徽章与 CHANGELOG。
- Issue #16：旧版直接调用 PATH 中 Codex CLI 的链路已由产品隔离运行时替代；设置页可检测版本与登录状态、
  启动官方 Codex 授权并执行真实“测试并保存”，失败返回受控且可行动的错误，不再只显示裸退出码。

## [1.0.0] - 2026-08-28

Vibe Research 从直接调用模型 API 的个人投研看板，升级为基于 Codex Harness 的本地金融研究 Agent。
V1.0.0 以源码 + 本地浏览器 UI 发布，包含首页 Agent、每日复盘、资讯雷达、产业信号、个股研究、
本地研报库、Agent 驱动回测、多空辩论、三市场自选与持仓、报告归档，以及 Codex 订阅与模型 API 双通道。

发布验证：orchestrator **525/525**、desktop **22/22**、Python **569/569**；两端类型检查和前端生产
构建通过。Codex 订阅与 MiMo API 均完成从空配置到真实业务报告的端到端验证。

## 1.0.0 开发记录 · 发布前模型接入闭环(2026-08-28)

- “接入 AI”新增 Codex 订阅登录入口：从产品专用 `CODEX_HOME` 未登录状态点击“登录 Codex”，
  自动打开 OpenAI 官方授权页；授权完成后设置页自动轮询并切换为可用，不要求用户打开终端或复制认证文件。
- 订阅登录任务单实例运行，具备明确的等待、失败与超时状态；超时时清理整个登录进程组，避免残留回调进程
  继续占用端口或让下一次登录永久卡在等待状态。
- 订阅与 API 两种接入统一改为“测试并保存”：先通过本机后端发起真实模型对话，成功才覆盖当前配置；
  失败不再出现“配置存下来了、到业务页才发现不能用”的假成功。
- **全新 Codex 订阅实测通过**：产品隔离登录态从未登录 → 官方浏览器授权 → 自动检测为可用 →
  测试保存 → “每日复盘”真实生成完整报告；最终配置不含 API key，也不读取 `~/.codex`。
- **全新 MiMo API 实测通过**：清空浏览器模型配置，从 API 接入重新填写官方地址、
  `mimo-v2.5-pro` 与用户 key → 测试保存 → “每日复盘”真实生成完整报告；结束后默认配置恢复为 Codex 订阅。
- Codex 独立审计三轮：前两轮共抓出 4 个 P2（设置页并发刷新覆盖新状态、登录超时只杀父进程、
  其他刷新入口仍可竞态、父进程退出早于回调子进程）；全部修复并加回归测试，第三轮结论为
  **无可操作 P1/P2**。
- 验证：orchestrator **525/525**、类型检查通过；desktop **22/22**、类型检查与 Vite 生产构建通过；
  数据脚本 **231/231**。

## 1.0.0 开发记录 · 本机订阅运行时检测与 Claude Code 接入(2026-08-28)

- 对照 `nexu-io/open-design` 0.21.0 的 runtime registry / CLI detection，补回原先只移植了模型清单、
  没有移植真实运行适配器的缺口。
- 新增本机 Agent 状态探针：设置页实时显示 Codex / Claude Code 的安装、版本和登录状态；未安装或未登录
  就禁用，不再把“已登录”或“开发中”写死在卡片上。
- Claude Code 订阅已接通真实对话链：用户正文走 stdin，强制 `safe-mode`、空工具集、空 MCP、无联网工具、
  无插件与无会话落盘；同时清除可能静默改走 API / Bedrock / Vertex / Foundry 的环境变量。
- Qwen Code 与 DeepSeek CLI 不再冒充免 key 订阅：前者旧免费 OAuth 已停止，后者使用 API key；两者继续
  通过现有 API 接入区配置。
- 实机验证：本机 Claude Code 2.1.226 已登录，并经产品适配层返回 `CLAUDE_RUNTIME_OK`；orchestrator
  517/517、desktop 16/16、两端类型检查与前端生产构建通过。

## 1.0.0 开发记录 · 从 API 分析升级为 Codex 本地金融 Agent(2026-08-28)

- 上一版 Vibe Research 由应用直接调用模型 API 完成一次次分析；当前版本改造为基于
  **OpenAI Codex Harness** 的本地金融 Agent，Agent 运行时与模型供应商正式分离。
- Codex Harness 负责在本机维持上下文、读取工具目录、连续调用工具、推进多步任务、展示进度并处理失败；
  金融层继续提供数据源、研究 SOP、确定性计算、证据校验和合规边界。
- 首页对话、个股研究、我的研报、回测与报告归档开始共用同一套 Agent 能力，不再是彼此独立的 API 问答。
- 模型层支持已接通的 Codex / Claude Code 订阅登录和大多数 Responses API；本机状态由后端实时检测，
  不把预留接口写成已可用。
- 相比旧版单次 API 分析，当前版本在长任务执行、工具调用、上下文保持、失败恢复与推理质量上均有显著提升。

## 1.0.0 开发记录 · 「我的研报」真正接入 Agent 底座(2026-08-28)

- `我的研报` 不再是占位页：支持上传 PDF / DOCX / TXT / MD / CSV，本机保存原文件与抽取文本，按行业、公司和代码归档；同一文件按 SHA-256 去重，删除采用事务式落盘。
- 新增本地资料检索：关键词与中文二元词召回匹配片段；PDF 保留页码，Agent 收到的上下文统一标为不可信资料，并以 `[资料:<id> p.<页码>]` 回引。
- 对话输出新增机器校验：只要使用了召回片段，回答就必须保留本轮真实资料 id 与页码；错引、漏引、引用未知资料都会拒绝返回。
- A 股六阶段研究会按代码自动召回相关资料，实际注入清单写入运行 manifest，最终报告同样必须保留真实引用；港股、美股资料仍可归档并在 Agent 对话中检索，但完整六阶段取数链目前只开放 A 股，后端会拒绝空跑。
- 对话执行改用专用无工具入口：关闭 Shell、图片读取、子代理、插件、应用和联网能力，防止不可信文档诱导模型绕过服务端检索读取本机文件；真引擎探针确认工具调用为 0。
- 新增资料列表、上传、下载、删除 API 与真实浏览器界面；扫描版 PDF 因无文本层会明确提示需要 OCR，不会伪装成导入成功。
- 验证：orchestrator 500 项测试全绿、Core 行业词 0、类型检查通过；desktop 9 项测试、类型检查和生产构建通过；Codex 四轮审计收口，末轮无可操作 P1/P2。

## 1.0.0 开发记录 · 发行形态回退为源代码 + 浏览器 UI(2026-08-28)

- 删除 `shell/` 下的 Electron 主进程、`app://` 协议、载荷装配、DMG 打包配置与客户端测试。
- 删除 `build/` 下只服务于 macOS App 的图标资产；保留 `desktop/` React 浏览器 UI 与本机 HTTP API。
- 编排器不再透传 `ELECTRON_RUN_AS_NODE`，源码纯净度测试新增“不得恢复 `shell/` 客户端”的棘轮。
- 中英文 README 明确当前交付是**源码 + 本地浏览器 UI**，补齐浏览器 UI 的安装与启动步骤。

## 历史 · 已移除的 macOS 安装包:Electron 桌面外壳(2026-08-26)

> 本节只保留当时的开发记录。当前工作树已移除 Electron / DMG 客户端，不再按这一形态发行。

`shell/` —— 用户拿到手的形态。Electron 主进程拉起编排器、`app://` 协议服务界面、
`/api` 转发到本地后端;`scripts/assemble.mjs` 装配载荷(引擎二进制 + 可重定位 Python +
界面构建产物 + 编排器源码),`electron-builder.yml` 出 dmg。
**一条命令出片** `npm run dist`(外壳重编 → 界面重构建 → 载荷重装配 → 验证 → 打包)。

产物 `VibeResearch-0.1.0-arm64.dmg` **331 MB**,已装到 `/Applications`、
**清空数据根当全新用户**实测:0.6 秒起后端 → 界面加载 → 真实行情,退出无孤儿进程。

🔴 **五条实测才定下来的**(与最初方案不同):

1. **`.app` 名字不能带空格** —— 产品根就在 App 包里,而编排器对"产品根 / 数据根含空格"是硬拒绝
   ⇒ `productName: VibeResearch`,显示名走 `CFBundleDisplayName: Vibe Research`。
2. **数据根 = `~/.vibe-research-agent`**,不是 `~/.vibe-research` —— 后者是**开源版看板的**
   (里面有它的 portfolio.json / myreports / monitor),第一次装机跑起来才发现。
3. **编排器发 `.ts` 源码不编译** —— 编译反而更脆(它自己按硬编码路径拉起 `orchestrator/src/run.ts`);
   宿主 Node 24 默认能剥类型。
4. **引擎打整个 npm 平台包**不抠二进制 —— 给 SDK 显式路径会让它把 `codex-path/` 那份 PATH 清空。
5. **字节码要预编进发行版** —— akshare 冷 import 带缓存 1.15 秒 / 不带 **21.9 秒**,
   而 App 包在别人机器上很可能只读(+55 MB,值)。

底座六处配套修复:`--port 0`(0 是 falsy,原来被两个 `||` 各吃一次)· `VRA_DATA_ROOT` 可改数据根 ·
`ELECTRON_RUN_AS_NODE` 透传(装机版的 node 就是 Electron 二进制,丢了它子进程会去开窗口)·
doctor 跳过 `payload` / `release`(不跳会把密钥扫描的额度吃光、真源码反而没扫)· 纯净度棘轮覆盖 `shell/src`。

🔴 **两次"成功地打进了上一版"**:一次是没重跑 esbuild 就打包,一次是 `beforePack` 只检查三个载荷文件在不在。
⇒ `beforePack` 改成**无条件重做**,不判新鲜度。
🔴 **`ps` 按 locale 转义非 ASCII** —— 中文安装路径下收尸静默匹配不上,已改成 ASCII 片段有序匹配 + `LC_ALL=C`。

外壳过 **Codex 8 轮审计**(4 初审 + 4 复审 + 打包侧 1 轮,20 条全部处置),每条修复做过变异测试。
测试 shell **77** · orchestrator **433**。

**还差**:应用图标(现在是 Electron 默认)· 签名 + 公证 + 自动更新(等 `Developer ID Application` 证书)·
🔴 **装机版怎么拿到模型 API key**(从访达双击启动的 App 没有 shell 环境,而密钥只从环境变量读)——
**这是产品决策,不是 bug**。

## 五个功能逐个实测:两个真 bug,一个功能确实没做(2026-08-27)

Simon 让把回测 / 自选股 / 持仓 / 我的研报 / 研究记录逐个跑一遍。**都在界面上真跑**,数字逐条手算核对。

### 🔴 两个 bug,都是我做回测页时留下的

1. **「回测」这个分类没注册**。我写了 `addNote("回测", …)`,却没在垂类包的
   `note.category` 枚举里加它 ⇒ 点「存进研究记录」抛错
   「分类「回测」没有对应的台账枚举」。
2. **而这个错用户看不见** —— 我在回测页**手搓了一个按钮**、用 `void addNote(...)`,
   `void` 把 rejection 吞掉了:**既不报成功也不报失败,错只进控制台**。
   ⚠️ 仓库里本来就有 `SaveNoteButton`,它的注释开头就写着
   「写失败照样显示成功…这种谎比报错难查得多」——**我把它警告的坑又踩了一遍**。
   ⇒ 回测页改用 `SaveNoteButton`;分类在垂类枚举 + 前端本地映射两处都注册。

### 实测结果

| | 结果 |
|---|---|
| **回测** | ✅ 两只票(茅台+五粮液)跑通:−17.40% / 回撤 −29.59% / 18 笔;对照 −35.53% 并标注「不是指数」;限制 6 条 + 口径 3 条 + 数据来源(各 968 根、起止日期)随结果列出 |
| **自选股** | ✅ 加两只,实时行情正常(旭创 877.01 +3.07% / 三花 36.88 +2.05%,PE/PB/换手齐全) |
| **持仓** | ✅ 加两笔,**逐项手算吻合**:茅台 (1291.16−1500)×100 = −20,884 / −13.92%;旭创 (877.01−600)×500 = +138,505 / +46.17%;合计 567,621 / 450,000 / +117,621 / +26.14% 全对 |
| **我的研报** | ⚠️ **确实没做** —— 页面明说「还没接到底座上(不是没有数据,是这条链路还没做)」。后端虽有 `ingestFiles`,但那是**把文档读成台账记录草稿**,与这页要的「文件归档 + 行业分类」是两个功能,不是没接线 |
| **研究记录** | ✅ 两条(回测 + 问 AI),展开后指标 / 限制 / 数据来源全在;落盘 `.local/ledger/note.json` |

顺带验了「问 AI」:它读到了本页真实数据(总市值约 56.8 万、浮盈 +26.14%),与手算一致,并成功存入。

⚠️ 台账里现在有 **2 笔持仓 + 2 只自选 + 2 条记录**,都是**测试数据**,要删随时说。

测试 orchestrator 454 · Python 555 · desktop tsc 0 错。

## 辩论产出的数字校验(2026-08-27)

补上上一轮敞着的那个口子:**引用来的数字能跟资料包比对,算出来的数字比不了** ——
后者成了唯一一类「谁都没在看」的数字,而它就摆在核对过的数字旁边,看着一样可信。

### 三档,必须分开报

新增 Core 模块 `arithmetic.ts`(算术是领域无关的,**不带任何行业词**),把一段产出里的数字分三档:

- **对得上资料包** —— 可溯源
- **写了算式且重算通过** —— 没有对应的已知值,但它把算式写出来了,就地重算过
- **两样都不是** —— **不等于错**,但这一档没经过核对,要让人知道有多少

外加 `badMath`:**算式是它自己写的、结果对不上** —— 唯一可以断言「这里错了」的一类。
界面上每段产出下面挂一条,算错的用红块单独列出并给出重算值。

⚠️ 只标不改。重算对不上不代表结论一定错(也可能是算式写得不规整),但**必须让人看见**。
⚠️ 这一层**只查它自己写下的算式,不判断算式选得对不对** ——
拿错了年份的两个数相除,算式本身可以是自洽的。能查的和不能查的要分清楚。

### 三次「以为做好了、其实没有」

1. **不认正号**:真实文本写的是 `= +0.2%`,而第一版 NUM 只认负号 ⇒
   **那句错话整条识别不出来**,安然过关。
2. **不剥标注**:`2026H1 444.64 亿 [ev-x] ÷ 2025H1 453.90 亿 [ev-y] − 1 = −2.04%` ——
   这**正是我们要求模型写的形状**,而带着期间标签与证据引用时一条都匹配不上,
   等于这层校验对规范写法完全失效。⇒ 先剥 `[ev-…]` 与 `2026H1`/`2025 年度`(**整体剥**,
   只剥字母会留下 `2026` 被当成运算数)。
3. 🔴 **只支持 `A ÷ B − 1` 与 `− + ×`,漏了纯除法**。而实测里模型写的大多是
   `453.90 ÷ 910.94 = 49.83%` 这种 ⇒ 一段**写满算式**的产出只认出 **1 条**。
   **「识别不到」和「没有算式可查」在结果上长得一模一样**,不测就发现不了。
   补上纯除法与「运算符和数字之间的标签」后,同一段正文识别 **1 → 6 条,全部重算通过**。

### 一条测试的自我更正

原本写了「标签间隙不能宽到造成误报」并配了四句反例。**变异测试把间隙从 8 放宽到 40,
四句照样一条不匹配** —— 说明挡住误报的是**字符类**(排除数字 / 运算符 / 逗号),不是宽度,
断言与真正承重的机制对不上。已按实情改写,并改为直接钉字符类:
去掉「排除数字」这一条会红 8 条,含那句具体断言。

### 顺带

五条角色提示词加了「算式要写出来 + 注明两个数各自的资料期 + 跨期比较用同一口径的相邻期」。
实测有效:同一只票再跑一场,多方把六条算式全写出来了(旧产出一条都没写),且逐条重算通过。
⚠️ **提示词不保证正确** —— 它只是提高了「有算式可查」的比例;真正兜底的是上面这层重算。

⚠️ Core 纯净度棘轮当场抓到我把 `营收` / `净利` 写进了 `arithmetic.ts` 的注释举例里,已换成中性例子。

测试 orchestrator **454**(新增 11 条,用例全部抄自两次真实辩论的产出)· Python 555 · shell 77 · doctor 18 ok。

## 多空辩论实测:深度档位是装饰性的 + 算出来的数没校验(2026-08-27)

真跑了一场(600519,选「一轮」)。**结论:引用是可信的,算出来的数不是。**

### 🔴 深度档位选了没用

界面上那个「一轮 / 两轮」下拉框**是装饰性的**:适配层把参数命名成 `_rounds`
(下划线 = 声明「我不用它」),**整条链**(界面 → 适配层 → API → service → debate)都把它丢了,
永远跑完整五阶段。而旁边的耗时提示还跟着档位变 ——
**告诉你 100 秒 / 3 次调用,实际跑了 6 分多钟 / 5 次。**
⚠️ 控件说一套、系统做一套,比没有这个控件更糟。

修法:契约加 `Plugin.debate.depths`(档位 → 跑哪几个阶段;**档位怎么分是垂类的事**,
Core 不知道「轮」是什么),沿链透传。实测 depth=1 → 3 阶段、depth=2 → 5 阶段、
**不给 depth → 回落到全部**(认不出的档位少跑,等于悄悄换了一个更弱的产物)。
`sees` 本来就按「这一场里存不存在且已完成」过滤,少几个阶段不用另外处理。

### ✅ 证据引用是真的(逐个核过)

正文引用的 30 个 ev-id 里,12 个属于财务端点(值不随行情变、可比对)——
**全部真实存在,且数值与正文逐个吻合**(445.17 / 444.64 / 453.90 / 822.93 / 862.41 /
1720.54 / 1741.44 亿)。没有编造引用。

### 🔴 但**算出来的数**没有任何校验

同一场里抓到两处,都出在没有 ev-id 的派生数字上:

1. 「2026H1 扣非 444.64 亿,**同比仅增 +0.2%**(2025H1 为 453.90 亿)」——
   实为 **−2.04%**,**方向反了**;而它自己在反驳段又写了 −2.04%,**前后矛盾**。
2. 「2025 全年**同比 +7.2%**(822.93 vs 747.53)」—— 747.53 确实在资料包里,
   但那是 **2023** 年的数(**取错年份**);且 822.93/747.53 = **+10.1%**,
   连它自己那两个数都对不上;用真实的 2024 基数 862.41 应是 **−4.6%**,方向也反了。

⚠️ **这两个百分比就摆在正确引用的数字旁边,看着一样可信。**
研究流程有 `judgeNumberBinding` 做数字绑定校验,**辩论一处都没有**(`debate.ts` 里 0 处),
提示词里也从没要求「算出来的数要能复算」。

**这一轮只做了两件够得着的**:① 五条角色提示词都加了「算式要写出来 + 注明两个数各自的资料期
+ 跨期比较要用同一口径的相邻期」;② **界面上把这条限制说出来** ——
「带 [ev-…] 的数字可溯源;同比、差额这类算出来的数没有经过校验」。
⚠️ 提示词不保证正确 —— **真正的解法是给辩论也加数字绑定校验,那是另一件事,没做。**

### 顺带证实的两条(注释属实,不是许愿)

- 每个阶段**一条全新会话**(`debate-<id>-<stage>`)—— 同一会话里连着扮演多空 = 一个人写辩论稿
- 产出**照常过合规 gate**(经 `chatSend` → `applyGate`,无条件执行);
  `debate.ts:260` 那个三元的另一分支是测试注入用的,生产走不到

### 另有一处未修

模型的**草稿泄漏进了正文**:「2025 全年营收同比仅 +1.1%(1741.44/1720.54−1**...不对,
这里要用 2024 全年营收**:…)」—— 自我更正的过程被原样输出。属表达层问题,未处理。

测试 orchestrator **443**(新增 3 条钉深度档位,变异验证过)· Python 555 · shell 77 · doctor 18 ok。

## 个股研究页实测:六处「不报错但结果是错的」(2026-08-27)

Simon:「个股研究的模块你测试过吗?没测试过的话跑一遍,这个还是挺关键的。」
—— **没测过**(它来自整套 UI 搬入那批 `8c91116`,只跑过 tsc)。真跑了 A 股与美股各一遍。

⚠️ **排查过程里我先误判了一次**:直接读 `/fetch` 响应的 `d.evidence`,恒为 0,
一度以为 8 个 legacy 端点全挂了。实际证据在 **`d.envelope`** 里,端点全是好的
(`fetch_financials` 48 条 / `fetch_announcements` 30 条 …)。**差点报一个假的严重故障。**

### 修掉的六处

🔴 **`noteKV` 白名单漏键 —— 同一个坑的第三次**(前两次:研报的 `industry`、GPU 现货卡的可租张数)。
`买方 / 卖方 / 当日涨跌 / 龙头 / 板块代码 / 概念` 都没登记 → 被安静丢掉 → 上层 `?? r.note` 回退
→ **界面上直接显示 `板块代码=BK0438;当日涨跌=-0.38%;龙头=五芳斋` 这种内部字符串**。
⚠️ 回退到原始 note 比显示空更糟:它看着像内容,用户不知道那是调试文本。

🔴 **note 末尾没有键的尾注被上一个值吞掉**:大宗交易的 note 以 `;单位按东财数据中心口径` 收尾,
于是卖方显示成「广发证券…营业部;单位按东财数据中心口径」。终止符加了第三条
(`;<末尾一段不含等号的文字>$`),只在字符串末尾且那段不含 `=` 时匹配。

🔴 **板块名取错来源**:板块名是**证据的 value**(食品饮料 / 白酒Ⅲ),代码却在找不存在的 `name=` 键。
同处的涨跌与龙头也一直是空的(它们在 note 里)。

🔴 **大宗交易的买卖方取的是不存在的字段**(`block_trade_buyer/seller`),
端点只给 price / premium / volume / amount / count,买卖方在 note 里 →
界面上显示成「买 · 卖」,像渲染坏了而不是没数据。

🔴 **美股现金流量表两个轴写反了**:`record_key` 实际是 `<报告期>|<科目>`,代码写成 `[item, period]`。
于是表头那句 `report_date.slice(0,7)`(为日期写的)套在科目名上,
把「投资活动产生的现金流量净额」截成「投资活动产生的」;
**数值因为转置一致所以还对得上,只有表头是错的** —— 最难看出来的那种。
顺带:期间标签改用**证据自己的资料期**(真实日期),不用 record_key 里的
`2026/Q9` —— 没有第 9 季度,那是把一个编出来的期间摆到界面上。

🔴 **财务块的数据来源标错**:写着「同花顺财务摘要」,实际走的是**新浪**
(akshare `stock_financial_abstract`,落盘 raw 的文件名就是 `extracted_sina_abstract_…`)。
一个卖溯源的产品把来源标错,比不标更糟。

### 顺带改了两处表达

- 财务块原本摆着 ROE / 毛利率 / 净利率 / 每股净资产 / 每股经营现金流 **五个写死的「—」**。
  用户分不清是**这只票没有**还是**这个源不提供**(实际是后者:该端点只给四项)。
  ⇒ 去掉五个空格子,换成一句说清楚的话。
- 页面说明写「输入一个 6 位股票代码」,而输入框提示写着支持美股 / 港股 / 韩股 —— 两处对齐。

### 还没做

美股的**总市值**显示「—」:`em_global_quote` 确实不返回市值(不是 bug),
但和上面那五项同一个问题 —— 光一个破折号看不出是"没有"还是"这个源不给"。要接得另找端点。

测试:orchestrator **440**(新增 3 条钉 `noteKV`,均做过变异验证)· Python 555 · shell 77 · doctor 18 ok。

## 大盘指数换成 A股 + 美股 + 港股(2026-08-27)

Simon:「把下面那排去掉,换成美股的道指跟纳斯达克,还有恒生指数、恒生科技」。
第二排原来是 科创50 / 上证50ETF / 沪深300ETF —— 两个 ETF 与第一排的宽基重复,
换成 **道琼斯 · 纳斯达克 · 恒生指数 · 恒生科技**,变成干净的 4(A股)+ 4(海外)两排。
⚠️ **科创50 也一并去掉了**(它在那一排里)——要留的话说一声,加回来是一行的事。

腾讯 `qt.gtimg.cn` 本来就能取这四个,两处要改:

🔴 **代码大小写敏感**:`usDJI` 有数据,`usdji` 返回 `v_usdji="1";`(20 字节空壳)。
   而 `tencent_prefix()` 里有个 `.lower()` —— 转了小写不会报错,那一条**静默地没有了**,
   界面上表现为"少一张卡片"。⇒ `us` / `hk` 前缀**原样透传,不转小写**。

🔴 **市场要逐行判,不能沿用端点级的 CN**:不然道指 53463.88 会被标成 `CNY`。
   数字对、标签错,而**界面上不显示币种所以看不出来** —— 错的那份会随证据进 AI 上下文。
   ⇒ mapper 里按代码前缀给每行自己的 market(市场决定币种,见 `ev()`)。
   实测:道指 → US/USD、恒生 → HK/HKD、上证 → CN/CNY。

解析正确性用**自校验**验过(涨跌额 ÷ 昨收 ≈ 涨跌幅),八条全对 ——
腾讯对 A股短式(11 字段)与长式(60+ 字段)的字段位置完全不同,只看数字"像不像"是看不出错位的。

## 侧栏默认收起 + Agent 按钮改配色(2026-08-27)

Simon:「把那些小三角都默认收起来,让大家一打开就是一个一级 Tag 的全览」
「左下角那个 Agent 对话,橙色加白色有点傻,能不能弄成高级点的、灵动一点的」。

**侧栏分组默认收起**。🔴 顺带把存储键升了版(`vr-*-open` → `vr-*-open2`):
默认值从"展开"改成"收起"时,老键里存着的 `open` 会让已经用过的人照旧全展开 ——
那不是 bug(它在记住你的选择),但**新默认就等于没生效**。换个键 = 旧记忆不再适用,
所有人重新从收起开始;之后手动展开的仍然会被记住。
(Simon 当场就撞上了这个:改完他那边"产业信号还是打开的"。)

**Agent 按钮**从实心橙 + 纯白字改成**橙作强调、不作底色**:
很淡的橙色渐变底 + 细橙环 + 橙图标 + 常规前景色文字;悬停时有一道掠过的高光(走一次,不常驻),
打开时环变亮、加一层柔光、图标极缓呼吸。右侧「对话 / 收起」小标做成淡色胶囊 ——
原来是压透明度的白字,在浅色主题下几乎看不见。

实测对比度(连底色一起合成后量):
暗色 文字 13.16 / 图标 5.97 / 小标 6.21;亮色 12.3 / 3.59 / 4.69 —— 两套主题全部达标。
⚠️ 中途量到过"文字对比度 1.11",查下来是**我自己连点主题切换弄出的不一致状态**,
不是真问题 —— 刷新到干净状态复量才是对的。差一点就去修一个自己搅出来的现象。

## 回测接进界面 · Core 新增「垂类工具」机制(2026-08-27)

Simon:「你的回测研究应该是需要加入到一个模块里面吧?就是左侧的,我没有看到呀。」——
上一轮**只做了能力层**:回测能跑,但只能在命令行跑,侧栏里根本没有入口。这一轮补上产品层。

### Core:一个通用的「跑垂类工具」机制

🔴 **Core 里不许出现行业词**(棘轮零容忍)⇒ Core 只提供
「起进程、把 stdin 喂进去、把 stdout 当 JSON 读回来」这件事,**不知道这些工具各自是干什么的**。
「回测」这个概念只存在于垂类包里。

- 契约槽位 `Plugin.tools`(可选):`{ label, module, timeoutMs? }`,注册期按安全模块名校验
- `service.runTool()` / `listTools()`;`GET /tools`、`POST /tool/:name`
- 复用既有的**异步** spawn(`spawnSync` 会阻塞整个事件循环),给它加了可选的 stdin ——
  stdin 只在真要喂东西时才开管道,无条件 `pipe` 会让不读 stdin 的脚本在管道满时挂住

🔴 **加一个可选槽位要动四处**:schema properties / 键集白名单 / 注册期 `decl` 投影 /
`currentPlugin()` 返回投影。这次前两处改了、后两处漏了 —— **注册通过、437 条测试全绿**,
但 `currentPlugin().tools` 是 `undefined`,界面上表现为「工具清单是空的」,**没有任何一层报错**。
⇒ ① 键集白名单改为**从 schema 派生**(以前是手抄的第二份,加槽位要同时改两处);
② 新增测试:schema 里声明、垂类也设了的槽位,必须都活到 `currentPlugin()`。

### 垂类:`backtest.cli`(JSON 进 / JSON 出)

三种结果**分得清**:`{ok:true, result}` / `{ok:false, refused:{reason, remedy}}` /
`{ok:false, error}`。🔴 **「被闸口拦住」与「出错了」必须分开** ——
混成一个 error,界面就只能显示「失败了」,而用户真正需要看到的是那句
「为什么不成立、怎么才能跑」。
⚠️ 引擎会往 stdout 打整份 metrics JSON,CLI 里用 `redirect_stdout` 接住,
不接的话我们这份 JSON 前面会多出一坨,调用方 parse 直接失败。

### 界面:`/backtest`(侧栏「回测」,在多空辩论之后)

选项(口径 / 策略 / 参数 / 市场规则)**由后端下发,前端不写死一份** ——
写死的那份迟早与真实实现对不上,而对不上的表现是「选了没反应」或
「真实存在的选项不在列表里」,两种都看不出是配置漂移。

- **限制与结果同屏**:回测最容易被误读的不是数字算错,而是读的人不知道它在什么约束下算出来的
- **被拦住 = 结论,不是错误**:橙色说明块,带原因 + 怎么才能跑 + 为什么要拦,不是红色报错
- **对照那一行说清楚是什么**:默认是「等权买入持有这几只标的本身,不是指数」
- **取不到的标的要点名**;数据来源(端点 + 根数 + 起止)随结果一起列
- 「问 AI」把整份结果**连同限制**一起带进上下文 —— 只给数字不给限制,等于替它打包票
- 跑一次几十秒(要先拉几年日线再逐根撮合),按钮旁边**明说**「通常十几到几十秒,别关页面」

实测:茅台 MA20/60 四年 → 总收益 −22.43% / 回撤 −30.64% / 11 笔(对照 −20.63%);
混市场(600519.SH + AAPL)→ 被闸口拦住并说清怎么办。

## 界面那批的 Codex 审计闭环(2026-08-27)

`01ee2e4` 是三个提交里唯一没审过的。七轮审计共 **17 条,逐条核实全部成立、零误报**,已全部修完。
产物在 `.local/reviews/ui-r{1..7}.md`。

### 🔴 源码里的裸 NUL 字节（我自己发现的,不在审计清单里）

heredoc 里写的转义序列被 shell 解释成**真正的 NUL 字节**写进了两个源文件
(`desktop/src/core/ai/pageContext.tsx`、`orchestrator/src/snapshot.ts`)。后果不是编译错:

- TypeScript **接受**字符串字面量里的裸 NUL ⇒ `tsc` 0 错
- Core 纯净度棘轮、前端边界棘轮**全绿**
- 但 **git 把文件判成二进制**(`Bin 0 -> 3047 bytes`):代码审查看不到内容、合并无法逐行,
  Codex 读到那里直接截断 —— 上一轮审 `pageContext.tsx` 其实**只审了一半**

⇒ 一个**谁都没报错**的失效。新增棘轮 `orchestrator/test/source_hygiene.test.ts`
(裸 NUL / 非法 UTF-8 / **逐个根**断言真的扫到了东西)。
换成转义写法后哈希实测不变,快照缓存不失效。

### 修掉的 17 条

**AI 对话状态机**:空回答(后端返回空串)被当完整回答落盘并进入下一轮 ·
`loading` 是陈旧闭包,同一事件里连点两次会留下永远转圈的空气泡 ·
**会话目录**:读不出来(损坏 / localStorage 不可用)与"真的空"混为一谈,
下一次写入会把目录压成只剩当前这条,**之前那些对话的内容还在但永远找不到入口** ·
删内容前没确认目录写成功,失败时内容已没、旧目录还留着条目(点开一片空白)。

**控制台 / 抽屉**:换对话那一帧 msgs 还是上一条的,却仍能发送 / 清空 ·
拖拽高度落盘的是上一帧的旧值 · 页面登记了但上下文为空时**发裸问题**,
模型会凭一般知识答一段看着正常的话 · 漏挂 Provider 时两端静默降级,
把"接线故障"伪装成"这页没有内容"。

**存档优先**:刷新锁是布尔且跨 run 共享 —— 切查询时新的那次被旧的在途请求锁死,
要么只显示旧存档且不报错,要么 `loading` **永远是 true**。

**行情信封**:核心字段走 `n0` 兜成 0 ⇒ 缺项显示成「0.00 元 / 0.00 倍」,与真实的 0 分不开。
⚠️ 这条规则**本来就写在那个文件头**,当时只在新加的两个字段上执行了。
改成 `number | null` 后类型系统逼出 12 处调用点,连 `Holding` 的行情派生四项一并改可空
(取不到行情的持仓原本整行显示「现价 0.00 / 市值 0.00 / 浮盈 0.00」)。

**中文输入法**:Enter 没查 `isComposing` ⇒ 选字期间按回车会把没成词的拼音直接发出去。

**GPU 取数层**:没有统计序列时发 `gpu_available_count = 0`、远期不可用时发
`gpu_forward_rung_count = 0` —— 上游只说了"没取到",没说"真的是 0"。
改用 status 型证据(与管制与准入层同一套写法),前端卡片过滤同步放行,
否则未覆盖的那张卡会**整张消失**(同一个坑的另一面)。
现货证据的资料期改用**曲线末点自己的日期**,不是取数日 —— 上游停更几天后
陈旧读数会看着像当天的,而这个温度计的用处正是判"还热不热"。

### 两条我自己引入的（第六轮抓出来）

① 提交锁用布尔 ⇒ **旧请求的 `finally` 会释放新请求刚拿到的锁** ——
   与我在存档 hook 里刚做对的 `busyRun` 是同一条规矩,在这里没应用。改成锁里存持有者。
② 目录把空串当"还没建过",且 `filter` 静默丢掉格式异常的条目再写回去 ——
   那些条目的入口就永久没了。改成只有 `null` 才是未创建,有一条不认识就整份当损坏。

第七轮:**无新 P1/P2**。

### ⚠️ 遗留:desktop 零测试

`desktop/` **没有任何测试**,只有 `tsc` 与一条读文件文本的边界棘轮。
而这一批里最危险的逻辑(对话状态机、会话目录)恰恰全在那儿。
试过用 orchestrator 现成的 runner 覆盖,卡在 desktop 用的是**无扩展名 import**(打包器风格),
Node 的 ESM 解析器认不了。本轮改用**在跑着的页面里实测**verify(损坏目录场景已验证:
目录原样未动、两条对话内容都还在),但那不是常驻测试。**要不要给 desktop 配测试跑器,等拍板。**

## 回测模块（2026-08-26）

从 [HKUDS/Vibe-Trading](https://github.com/HKUDS/Vibe-Trading)（MIT）移植回测引擎，
覆盖 **A股 / 美股 / 港股的日线**，长线与短线两种口径。归属与改动清单见 `backtest/NOTICE.md`。

**只搬引擎，数据走本产品自己的取数层** —— 上游那 25 个 loader 一个没用：
回测吃的每一根 bar 都带 `raw_ref`，与看板数字同源可复算。搬来 4,896 行（逻辑未改，
仅把两路可选增强的 import 改惰性、删掉 yfinance 兜底），新写 1,010 行，测试 137 条。

**闸口**（`gate.py`）在跑之前判这个回测成不成立：判市场 → 判口径 → 判限制，
只输出能跑的 `Plan` 或说得清的 `Refusal`。**运行期守卫**（`run.py`）补上闸口看不到的两件事：
实际 bar 数不够、策略发出了引擎会静默拒掉的信号 —— 两者都会产出看着完全正常的报告。

移植中挖出并修掉的静默错（共同形状：**不报错，给一个看着正常的结果**）：

- 传裸六位代码 → 要 `000001.SH`（上证综指）拿到 `sz.000001`（平安银行），回测照常跑完
- pandas 3 保留 `datetime64[s]`/`[us]`，而上游 `_align` 把 `asi8` 当纳秒 →
  所有 bar 掉到 1970 年 → **零成交但不抛异常**，产出"总收益 0.00%"的完整报告
- 形状守卫写宽 → `EURUSD` / `BTCUSDT` 被判成 A股（上游认不出时一律回落 `a_share`）
- ETF / 指数 / 可转债 → `bs_kline_qfq` 是个股接口，对它们返回空序列而不报错
- Yahoo 只 `dropna(close)` → 留下 `open=NaN` 的行，表现为"少成交一笔"而非数据有问题
- A股做空信号被引擎静默拒掉，报告里无任何字段说明 → 描述的是另一个策略

**Codex 审计 r1–r5 闭环**：13 条，逐条核实 = 11 条成立已修 · 1 条误报 · 1 条观察成立但建议的修法会误杀
（做空检查若按建议改成 `or`，会把"美股 + 调用方没声明做空"拦掉）。
r4 报的 2 条**都是我修前几轮时自己引入的**。每条修复均做变异测试，确认红在对应测试上。
r5 无新 P1/P2。

买入持有的收益经**独立手算逐基点核对**（滑点方向 / 印花税卖出单边 / 佣金最低 / 过户费双边 / 期末平仓成本）。

## 1.0.0 开发记录 · 全局 AI 入口 + 存档优先 + GPU 图表接齐(2026-08-26)

⚠️ **这批还没过 Codex 审计** —— 界面还在按反馈迭代,说好等定稿再一次性审。别把它当已审过的。

### 全局 AI 入口(两个,用途不同)

- **右上角固定按钮** —— 问**这一页**。对话按页分开存,换页就是另一份记录;
  只带这一页当前显示的数据。
- **侧栏「Agent」(高亮橙色) → 底部控制台** —— 一条**长期**对话,翻页不断,聊什么都行;
  打开时把主内容区**挤上去**而不是浮在上面(浮层会盖住正在看的表格,而这块面板的用处
  恰恰是"一边看着页面一边聊")。左侧一栏聊天记录,自己一条滚动轴,可新建 / 切换 / 删除。

不做写入预设 —— Simon:「客户有各种各样的需求,所以没办法去预测的」。

两个外壳共用一份状态机 `core/ai/useAiChat.ts`,因为这套逻辑里有一半是"看不出来但会出错"的:
**没答完的那一轮不落盘、也不进下一轮**(半截回答会被模型当成自己上一轮的完整发言);
**换 key / 关面板要中止在跑的请求**(迟到的回答会被写进另一份对话);
**session id 带哈希**(`/a/b` 与 `/a-b` 单纯替换非法字符会撞成同一个后端线程,而界面上看不出来)。

🔴 **换 key 之后有一帧 `msgs` 还是上一份的** —— 真踩过:点「新对话」之后,新的那条被写上了
**上一条的标题**。⇒ `AiChat` 暴露 `key`,调用方拿 msgs 做事之前先对一下。

### 存档优先(`core/data/useArchiveThenRefresh.ts`)

资讯 / 公开新闻 / A股公告 / 事件概率 / 产业信号:**打开先给上次的存档**,同时后台真取一次、
标「刷新中」。🔴 **刷新失败绝不清空已有内容** —— 把一屏存档换成一句"加载失败",
是拿一次网络抖动抹掉用户本来能看的东西,而且他不会知道刚才那些去哪了。
第一次打开没有存档时照实说在取,**不显示"暂无数据"**(那是把"还没取"说成"没有")。

### GPU 租金图表

按开源版 Vibe Research 的样子接齐:曲线走 500farm 近一年逐日中位,现货卡三张(B200 / H100 SXM /
A100 SXM4)。两处真 bug:**时间戳单位差 1000 倍**(秒当成毫秒);`noteKV` 白名单外的键被安静丢掉
⇒ 现货卡的"可租 X / 共 Y 张"与观测时间**在界面上就是不显示,而代码里明明取了**。

### 其它

行情信封补 `float_mcap_yi` / `amount_yuan`(**单位写进字段名**,换算只发生在展示层);
侧栏署名改 `https://phoenixtree.ai`。

## 1.0.0 开发记录 · 让 Codex 看真实页面 + session resolver + 界面查询契约(2026-08-26)

Simon:「你先把这个 UI 的页面告诉他地址,让他打开看一下……听听他的建议,然后再综合进行推荐」。

### 一、Codex 真的点了 8 个页面(不是读代码),抓到一个我没看见的数据 bug

产物 `.local/reviews/ui-r1.md`。它说「净流出最多」下面的数字**全是正的**。查下来是一串套娃:

1. 端点默认只取 50 个,`slice(-5)` 拿到的是"流入最少的五个" —— 仍是流入;
2. 我改成 200,以为够了 —— **上游 total 是 496**;
3. 改成 500 还是只回 200 ⇒ 挖到取数层:**代码请求每页 200,东财实际每页只给 100**,
   而循环里写着 `if len(more) < 200: break` —— **第 2 页就断了**。

修完取全 496 个,**净流出的板块有 273 个**,之前一个都看不到。
⇒ 新教训:**别假设上游认你请求的页大小**。(这是"取前 N 条之前先问源头能给多少"的第三次。)
顺带扫出板块中心那行 `api.fetch` 漏传 `refresh`(嵌套花括号让批量替换没匹配上),该页刷新对它无效。

它另外两条也成立、已列入后续:决策链没连起来(新闻不知道影响哪个主体、信号不知道在验证哪条假设);
核心功能要求用户先完成一套看不见结果的配置(空台账时几页就是空壳,没有示例与引导)。

### 二、`session.ts`(金融包):这一页该看哪一天

交易日收盘前 → 上一个交易日;收盘后 → 今天;非交易日 → 最近一个交易日。
**前端不许按本地时间猜**(用户机器的时区与时钟都不可信,而且"今天是不是交易日"要查日历)。
⚠️ 拿不到时段时**保守退回上一个交易日**并说明理由 —— 宁可显示一天前的完整数据,
也不要把半天的盘中数据当成"今天的复盘";后者从数字上看不出是错的。

实测:盘中打开复盘页 → `业务日期 2026-08-25 · 今天还没收盘,显示上一个交易日 · 盘中 true`。

### 三、界面查询契约(BFF):页面不再认识物理端点名

新增契约槽位 `Plugin.pageQueries` 与 `Plugin.pageContext`,Core 侧执行器 `service.pageQuery`,
路由 `GET /page/:query`。前端只说要哪一屏,端点 id / 参数 / 分页上限全在垂类声明里。

- **今日总览 = 盘中**(指数 · 情绪 · **成交额榜** · 人气 · 资金),**每日复盘 = 已收盘的那一天**。
- 新增端点 `em_turnover_rank`(全市场成交额榜,沪深京 A 股)—— 原来没有,是 Simon 点名要的。
- 返回带**页面级一致性**:`oldest_fetched_at` 与 `mixed_ages`(各块取数时刻跨天时出声)。
  只在每块标"X 分钟前"不够 —— 一屏并排的几块可能差好几天,而用户会把它们当成同一时刻的快照。
- 🔴 上下文注入是**按块**的。第一版无差别注入给每一块,结果不接受该参数的端点全被校验拒掉、
  一屏五块全 missing —— **错了会响、不会静默**,正是靠这一点当场发现的。
- ⚠️ 板块资金流那个源**只给当日**(没有"指定某一天"),复盘页盘中看到的是今天的进行时。
  如实写进该块的说明,不假装整页是同一天。

两处 Core 边界被棘轮当场挡下并已改(注释里写了行业词、Core 里 import 了 `finance/`);
`pageContext` 含函数 ⇒ 与 `quoteDecision` 那批一样走手查,不进 ajv。

测试 **392 TS · 417 Python** · Core 行业词 **0**(38/38 文件)。

### 四、前端 Core / 垂类边界

按 Codex 的建议把 `desktop/src/` 拆成 `core/`(通用外壳与组件)与 `verticals/finance/`(页面、导航、文案)。
新增 `core/lib/ui.ts` 注册点:**Core 的外壳不认识任何一个具体栏目**,只按注册进来的分组渲染;
组装根 `main.tsx` 是唯一把垂类接进 Core 的地方。依赖方向单向:垂类 → Core,永不反向。

顺手落地两条 Simon 提的:**看板改名「投研看板」**;分组从「经营闭环」改成 **盘面 / 研究 / 我的**
(对应"看市场 → 做研究 → 落到自己的决策",不是按技术模块摆一排)。

🔴 **新增边界棘轮 `test/ui_boundary.test.ts`,一上来就抓到 6 处** —— 和后端那条棘轮当初一样,
人眼看全都"很正常":
- 三处是**注释**里的行业词(改写即可);
- 三处是**用户可见文案**躺在通用组件里(对话框提示"写下标的代码"、导入示例"券商 App 的持仓页截图")
  —— 那不是注释问题,是真耦合:换个行业这句话就得改,而它在通用组件里没人会想起来。
  ⇒ 新增 `VerticalUi.copy`,由垂类提供自己的界面文案。

棘轮同时双向查导航与页面表(有导航没页面 = 点了白屏;有页面没导航 = 永远点不到),
两种失效都不报错、没人会发现。变异测试:把垂类 import 塞回 Core,4 条测试立刻红。

测试 **396 TS · 417 Python** · 前端构建通过 · Core 行业词 **0**。

### 五、三页接上界面查询:端点名从界面消失

`Today` / `Review` / `Sectors` 改用 `PageShell`(取数、资料期、刷新、缺块提示都在外壳里)。
每块的**标题与说明来自后端的垂类声明**,前端不再写死 —— "这一块在回答什么"只有一处真相。

真界面确认:
- 卡片右上角原来印着 `em_limit_up_sentiment` / `ths_hot_reason`,现在**没有了**;
- 复盘页顶部写明「今天还没收盘,显示上一个交易日」,数据是 **2026-08-25** 的;
- 今日总览补上了 **成交额榜**(新端点 `em_turnover_rank`,全市场 5,904 只里的前 20);
- 整页一个统一的资料期与刷新,不再每块一个。

顺手改对两处:
- 🔴 复盘页的龙虎榜我先前**删错了** —— 我看的是 `em_dragon_tiger`(单只主体,需要 symbol),
  而这一页要的是 `em_daily_dragon_tiger`(市场级日榜,`symbol_kind=none`)。转换页面时才发现拿错了端点。
- 缺块**统一在顶部列出并说明原因**,不再让页面少画一块了事 ——
  少画一块时用户会以为"这里本来就没有东西",而真相是取数失败,两者处置完全不同。

⚠️ 还没接的:`Signals` / `Radar` 仍在用 `EndpointPanel`;从 Vibe Research 搬 12 页尚未开始。

## 1.0.0 开发记录 · 取数快照层 + 一致性契约(2026-08-26)

Simon:「每日复盘每次点开都要读取一遍,这个其实是不对的……所有的这些页面,都应该有一个存档的功能,
就是上次打开是什么就是什么。就不能每次都跑,那太浪费 Token 了」。

### 一、快照层(`src/snapshot.ts`,Core)

取数结果按 `端点+主体+参数` 落盘 `<dataRoot>/snapshots/`,默认读上次的、不重取。
**照搬 Vibe Research 原版已经跑通的三层**(内存 TTL → 磁盘快照 → 显式刷新),没自己发明。
前端每块显示「数据取于 X 分钟前(上次的,未重新取)」+ 刷新按钮。

三条纪律:**拿旧数据必须说是旧的** · **失败与空结果不写快照**(否则一次网络抖动被记住且永不自愈)·
**参数进键**(同端点不同参数是不同数据)。

### 二、先跟 Codex 碰架构,碰出四个真问题

Simon:「你先把这个架构跟 Codex 碰一下,然后再开始改造」。产物 `.local/reviews/arch-r1.md`。
它指出我那版设计的四个真缺陷,全部已修:

- 🔴 **只有 `refresh: boolean` 不够**。那等于"不刷新就接受任意年代的快照" ——
  对界面尚可,对别的调用方全不成立:**MCP 的 schema 里根本没暴露刷新 ⇒ 它永远只能拿旧快照**;
  **体检的网络探针读快照 ⇒ 把历史上的一次成功报成「当前网络健康」**,一条彻头彻尾的假诊断。
  改成显式策略 `cache_only | prefer_cache(max_age) | fresh`;MCP 暴露给调用方且默认 fresh;体检强制 fresh。
- 🔴 **含"按此刻算出来"字段的端点会被永久冻结**。注册表新增 `cache_max_age_sec`,
  **端点声明的上限调用方放宽不了**(那是数据本身的性质,不是调用方的偏好)。
  已标注 20 个:交易日历 `0`(从不缓存)· 报价类 300 秒 · 榜单类 900 秒。
  实测:交易日历连打两次都 `cached=false`,公司画像第二次 `cached=true`。
- 🔴 **快照键只排了第一层** ⇒ `{a:{x,y}}` 与 `{a:{y,x}}` 是同一个查询却各存一份、互相看不见。改成递归规范化(数组保序 —— 顺序是语义的一部分)。
- 🔴 **无 single-flight** ⇒ 一屏五个卡片指向同一端点就打五次上游;更糟的是**先发后回**的慢请求会把新结果覆盖成旧的。

另新增端点 `exposure: ui | agent | internal` —— 「管制与准入这类只让 AI 调用、界面不展示」
**光靠前端不渲染守不住**:以后任何一个通用端点列表组件都会把它列出来。

顺带被 TypeScript 抓到一个真 bug:`onClick={reload}` 会把鼠标事件当成 `refresh` 参数传进去(truthy)
⇒ **每次点都变成强制真取数**,正是这次要消灭的行为。

测试 **381 通过** · Core 行业词 **0**(38/38 文件)。

## 1.0.0 开发记录 · 国产模型真跑:MiMo 兼容矩阵 + 结构化产出降级(2026-08-26)

Simon:「国产模型的兼容矩阵你就用 MiMo 的试一下嘛。真实测试一下」。

### 一、实测结果

新增模板 `providers/mimo.json`(小米 MiMo,`https://token-plan-cn.xiaomimimo.com/v1`,原生 Responses)。
`mimo-v2.5` 跑完 10 项:**pass 7 · partial 1 · error 1 · n/a 1**。

- ①②③④⑤⑥⑨ pass —— 含**并行工具调用**(事件流观察到峰值 2 条同时在途)与 200 行长流。
- ⑦ partial:`mimo-v2.5` 不回传 reasoning。⚠️ 但**换 `mimo-v2.5-pro` 直连实测是有的**
  ⇒ 这项跟**模型**走不跟 provider 走,不能记成"MiMo 不支持"。
- ⑧ **error** —— 这条是真问题:`text.format type 'json_schema' is not supported,
  only 'text' and 'json_object' are allowed`。独立 curl 复核过,不是矩阵判错。

### 二、⑧ 不是"矩阵上少一分",是**整条研究流程跑不了**

产品的每个阶段都硬传 `outputSchema` ⇒ MiMo 会把整轮拒掉。实跑验证:
`--stages profile` 直接 failed,事件流里就是那条 `responses_feature_not_supported`。

**修法:模板可以声明 `structured_output: "prompt"`**,产品改为把 schema 写进提示词、不再硬传。

🔴 为什么这不算放松要求:**`outputSchema` 从来就不是校验边界**(`ingest.ts` 的注释早写着这句)。
产物合不合规是产品自己校验的 —— 阶段过 validator、导入草稿过 `parseOutput`。
降级损失的是**命中率**(模型少一层硬约束、可能更容易写歪、重试上升),不是**正确性**;
写歪了照样过不了产品这关。⇒ 所以这条降级**不能**顺手把校验也省掉,那才是真放松。

**改完真跑验证**:`mimo-v2.5` 完整跑通 profile 阶段 —— **validator 通过、79 条证据**,
summary 还正确处理了"证监会行业分类与东财行业口径不同"并明确判定**不算冲突**。
⚠️ 慢:agent 那一轮 4.7 分钟,turn 超时压到 5 分钟会连续两次超时(当时的 20 分钟默认值对 MiMo 够用；
2026-09-05 为 WorkBuddy 正式报告留余量后，产品默认值已调整为 30 分钟)。

### 三、顺带挖出一个更严重的:对话与导入**根本不认用户配的 provider**

变异测试("把修复改坏,看测试红不红")发现 ingest 那条降级**永远不会触发** ——
往回查根因:`chat.ts` / `ingest.ts` 都是直接 `makeConfig`,而 `provider` 有内置默认(openai)、
`providerProfile` 恒为 `null`。⇒ **用户配了 DeepSeek / MiMo,研究运行认,这两条路不认**,
照样去打 OpenAI;没有 OpenAI 凭据的用户会看到"研究能跑、对话和导入报错",而且报的是别人家的错。

两处都改成走 `loadProductConfig`(和 `run.ts` 同一条路)。写测试时又带出第二层:
调用方给了 `dataRoot`,而 `loadProductConfig` 默认按 `repoRoot` 推数据根 ——
**两处对"用户配置在哪"口径不同,用户改了配置却不生效且不报错**。已按 `dataRoot` 显式指路。

### 四、教训

**只测纯函数测不出接没接上。** `withOutputSchema` 的单测全绿,而"runner / ingest 有没有真去调它"
两处都没被覆盖 —— 变异测试一改就露馅(两次都是"仍绿")。
⇒ 新增一层能力/开关时,**除了测那个函数,还要测每一个消费点真的问了它**。

### 五、两条"清单上写着没做"的,实测后都不成立

- **`service.fetchEndpoint` 的 spawnSync 串行化** —— **本次会话早些时候已经修掉了**,
  我却照着一条过期记忆向 Simon 报"这条还没修"。实测 5 个真实端点:**同时在途峰值 5**,
  并发墙钟 2.14s vs 串行 4.97s。⇒ **报状态前先实测,别念记忆。**
- **`fetchrun.ts` 里另一处 spawnSync** —— 看着是同类问题,其实**不能动**:
  研究运行靠"每个脚本执行前后各拍一次 raw 目录快照"把 raw 文件归属到脚本,
  **顺序是承重的**;并发会让两个脚本的 raw 文件张冠李戴,而 `raw_ref` 是整条证据链可复算的根
  —— 归错了**不会报错**,只会让"这个数字来自哪次抓取"从此对不上。已把这条写进函数注释,
  免得以后(包括我自己)把它"优化"坏。

### 六、Codex 审计九轮:12 条,逐条核实全部成立,只有 1 条不采纳(附理由)

产物 `.local/reviews/mimo-r{1..9}.md`。⚠️ **其中 5 条是我修上一轮时自己引入的** ——
这一串的价值不在"发现了多少",在"逼出了根因":

- 导入降级后**没有按契约校验草稿** ⇒ 我刚写下的"降级不损正确性"在这条路上不成立。
- 补校验时**用了完整契约却忘了 null**:模型对读不到的字段按设计给 null,
  最常见的合法草稿会整批被拒(我的夹具里恰好没有 null,所以没红)。改成校验 `dropNulls` 之后的对象。
- 缺必填的草稿放行 ⇒ 落库才报错。改成标 `missing_required` 并**禁用按钮** ——
  然后被指出"禁用而无补填入口就是死路",于是复用契约驱动表单做**补填**。
- 补填表单又把 `onClose` 当"存好了"用 ⇒ **点 X 关掉会被记成已写入**。拆出 `onSaved`。
- 初始化依赖用对象身份 ⇒ 别的草稿保存会重置正在填的内容;改成按内容比较后,
  又被指出"`editing` 是父组件快照、永远不刷新" ⇒ 改为**从 store 按 id 读当前那条**;
  再被指出"重置会丢用户正在打的字" ⇒ 加脏标记 + 外部变更提示,让用户自己决定。
- 会话指纹我**手挑了四个字段**,漏掉 wire_api / env_key / http_headers,**连轮换密钥都不体现**
  ⇒ 改成对 `codexOptionsFor(cfg)`(真正传给引擎的原物)取哈希。
- 校验器缓存键只有 `id::kind`,换一个同 id 不同契约的插件会命中旧校验器;ingest 与 ledger 两处同改。
- 用户配置与 provider 覆盖模板**从两个根读** ⇒ 新增 `dataRootOverride`,三者同根。

**唯一不采纳**:"store 里同 kind 可能有重复 id"——后端 `loadFile` 逐条查重并直接判文件损坏
(`ledger.ts:297`),**结构上不可能**出现,不为够不到的场景加代码。

测试:orchestrator **368 通过**,Python 413,doctor 18 ok / 0 warn / 0 fail,
desktop `tsc` 干净,Core 行业词 **0**(37/37 文件)。每条修复都做过变异测试。

## 1.0.0 开发记录 · 桌面看板 + 可写台账 + 对话 / 导入(2026-08-26)

Simon:「就先按这个来吧,然后你开始生产真正的版本吧」→ 从可点原型转成**接真后端**的桌面前端;
随后「那 5 页都做实吧」「剩下 4 页仍缺可写的用户台账后端,把这些都完成了」。

### 一、桌面前端(`desktop/`,新目录)

- Vite + React + Tailwind,**11 个页面全部接真后端**(不再有假数据页):
  今日总览 / 每日复盘 / 板块中心 / 个股数据 / 资讯雷达 / 产业信号 / 数据源 /
  组合经营 / 计划与风险 / 资金与风险 / 行动。
- 设计令牌 1:1 取自开源版 Vibe-Research;浅色下重新标定了 `--primary` / `--danger` /
  `--success` / `--warning` 的明度 —— 🔴 **对比度不足的根因不是令牌太浅,是嵌套着色**:
  选中行 `bg-primary/15` 会把页面底色合成进去,量出来的是那层米色不是令牌值。
- **A 股色向**:`--danger` = 涨(红)、`--success` = 跌(绿),与美股相反。
- Bearer token **每个请求重新读盘**注入(API 重启会换 token);`host` 写死 `127.0.0.1`
  (`localhost` 在本机解析成 `[::1]`,后端绑 IPv4 → 502)。
- 证据数字用 `show()` **只加千分位、绝不改精度**;`compact()` 才做单位换算并总是标出目标单位。
  上一版 `show()` 对 ≥1000 取整(9895.96 → 9,896),等于**悄悄改掉证据值**。
- 每页右上角悬浮「问 Agent」按钮与左下角开关是**同一个状态**,不能各说各话。

### 二、可写用户台账(`orchestrator/src/ledger.ts`,Core;`finance/ledger_kinds.ts`,垂类)

- 四种记录(持仓 / 论点 / 判据 / 行动)**由垂类契约声明**,Core 一个行业词都不含;
  表单字段由后端 JSON Schema 驱动渲染 —— 垂类改字段,前端自动跟着变。
- 写入:`linkSync` 原子锁 + 唯一 token + inode 比对释放;整条替换而非合并;
  UUID 主键并在碰撞时重取;**逐条校验**(信封字段 / kind 相符 / id 唯一),
  一条坏记录不会把整份文件读成"空"。
- `fileOf` 与 `validatorFor` 统一用 `hasOwnProperty` 判 kind —— 此前一个用白名单、
  一个用真值查找,**两道闸门两把尺子**;并显式禁掉 `__proto__` / `constructor` / `prototype`
  (它们能通过 kind 名的正则)。
- ⚠️ 锁是 TOCTOU 缓解不是消除,已在代码里写明残余风险,不假装它是原子的。

### 三、对话与导入

- `chat.ts`:只读沙箱 / 不联网 / 不联网搜索,回答过产出红线 gate。
  🔴 **开场交代刻意不列举禁用词** —— 实测模型会照抄一句「我不给目标价」,子串匹配命中后
  **整行被自己的 gate 删掉**。考虑过给 gate 加"否定豁免",**否决了**:窗口式否定判断会被
  双重否定绕过("不要错过建仓机会"),合规判断只允许往严的方向错。改因不改果。
- 提示"这里少了东西"的措辞**不回显命中的词**(先前写成「已移除(建仓)」= 把要挡的词又打了出来)。
- `ingest.ts`:截图 / 文本 → **只产草稿**,逐条确认才落库。扩展名白名单;
  **PDF / xlsx 明确拒收** —— 没有可靠转写工具时,给出一份看着像模像样、实则乱猜的草稿比不支持更糟。
- 结构化产出的 schema **由契约生成**(平台要求每层 `additionalProperties:false`,
  与"字段开放"冲突)→ 全字段必填但可为 null,解析时丢掉 null。

### 四、后端与取数

- `fetchEndpoint` 改**异步**(spawn + 手写超时 / maxBuffer / 错误,`close` 而非 `exit` 才 resolve):
  实测并发在途 5 个。⚠️ 判并发**不能拿"串行耗时之和 vs 并发墙钟"比** —— 子进程在争抢下各自变慢,
  这把尺子会得出相反结论;正确判据是**同时在途数 > 1**。
- 改异步时漏了 3 处 `assert.throws(() => fetchEndpoint(...))`(同根因),已用正则系统性找齐。
- `ledgerList` 全量读此前**绕过了 `safePath`**;`/import` 路由的 `b.record ?? b` 改用 `hasOwnProperty`。
- `ths_hot_reason` 的 note 此前会把 Python 的 `None` 拼进字符串 —— **这条 note 也会喂给 agent**。
- 申万行业接口加 3 次重试;错误信息**先提本机代理**(见下)。
- doctor 新增 `net_proxy` 检查项(状态 `ok` 不是 `warn`):本机全部域名解析到 `198.18.x.x`
  fake-IP、走 `127.0.0.1:7897`。🔴 这条我诊断错过两次(先判"证书问题"、再判"偶发,重试能好"),
  第三次才对 —— **重试打的是同一台服务器**。这是本机环境不是产品缺陷,如实写在体检里。

### 五、国产模型接入:不是"没开工",是**坏在很深的地方**

- 🔴 引擎(`codex-rs/model-provider-info`)对 `wire_api = "chat"` **硬报错**,而四个国产模板
  全是 `chat`,`codexProviderConfig` 原样传下去 ⇒ 配置能存下、能加载、能启动,
  **跑到引擎深处才炸**,报错还出现在一个跟 provider 配置八竿子打不着的地方。
- `validateProfile` 现在**在选用时**就拒 `chat`,并把两条出路写进报错(换 Responses 端点 /
  架 Responses→Chat 网关)。枚举里保留 `"chat"` 只为**读得懂旧档并给出解释**,不是放行。
- 四个模板按 2026-08-26 实查重写,全部 `responses`:DeepSeek 走官方 `https://api.deepseek.com`
  (`deepseek-v4-flash`);Qwen / Kimi / GLM 走**阿里云百炼** compatible-mode
  (智谱官方开放平台当日没有原生 `/responses`;月之暗面官方是否提供**未核实** —— 未核实的不写成事实)。
- 百炼三件套的 `base_url` 带 `{WorkspaceId}`,**选用时当场拒**并告诉用户复制到
  `.local/providers/<id>.json` 填好再用 —— 而不是把密钥发到一个不存在的主机上。
- ⚠️ 「`responses_support=native` 要求 `wire_api=responses`」这条校验**现在够不到**
  (chat 在它之前就被拒),代码与测试里都如实标注,没有写个假断言把它凑绿。
- README / README_en / providers/README / model-access / release-checklist 里的旧事实
  (`deepseek-chat`、`MOONSHOT_API_KEY`、`open.bigmodel.cn`、`src/provider_matrix.ts` 旧路径)已一并订正。

### 六、Codex 审计五轮闭环(2026-08-26)

按项目铁律"每个环节建完都要过 Codex 审计闭环"补审本次的新后端。
产物 `.local/reviews/desk-r{1..5}.md`。**共 12 条,逐条核实全部成立、零误报**;
r5 判"没有新问题"。⭐ 每条修复都做了**变异测试**(把修复改坏,确认对应测试真的变红)。

**r1(台账 / 契约 / 路由)** 1 P1 + 2 P2:
- 读取路径**不按契约复核** —— 台账 JSON 用户能直接编辑,写入侧的字段校验绕得过去,
  然后以 200 成功返回给界面和下游。修法是**报出来但不删不改**(`listRecordsChecked` + `/ledger` 的
  `issues` 字段 + 界面提示):若在这里抛错,垂类日后加一个必填字段就会让用户的台账**整份打不开**,
  比原来的 bug 更糟。
- 日期只查外形 ⇒ `2026-02-31` / `2026-99-99` 能存进"到期清单",**排序永远不对、提醒永远不触发,
  保存却一路报成功**。新增 Core 的 `formats.ts`(自己实现日历日,不装 `ajv-formats`)。
- 锁的 TOCTOU:数学上消不掉(见 `withLock` 注释),但**最坏后果**可以消 —— 写完**回读复核**,
  丢了就报 `write_lost`,不返回一个假的成功。

**r2(对话 / 导入 / 取数)** 2 P1 + 3 P2:
- 对话会话只按**客户端给的会话名**索引,而线程绑定着某个数据根的工作目录 ⇒ 另一个数据根用
  `default` 会接上别人的线程、在别人的目录里读文件。键改成"真实数据根 + 会话名"。
- `parseOutput` 的注释写着"不许尽力还原",代码却把 `source_file` 缺失补成 `""`、`fields` 非对象补成 `{}`、
  `uncertain` 非数组补成 `[]` —— **声称与代码不符**,而且补掉的恰好是"模型说自己没看清哪几处"。改严格。
- base64 **解码之后**才查大小(解码本身就要扫一遍、分配内存)→ 改成先按编码长度卡。
- 失败路径不清暂存目录 —— 里面是用户的**私密截图**,而调用方拿不到 batch 路径、根本不知道要去清。
- 类型只认扩展名 ⇒ PDF 改名叫 `.txt` 就绕开了"不收 PDF";任意二进制叫 `.png` 就进了图片解码器。
  加**内容嗅探**(图片查魔数、文本查 UTF-8 与 NUL);诚实边界:挡"改名混进来",挡不了"格式合法但内部畸形"。

**r3(复审 + 前后端接缝)** 2 P1 + 4 P2:
- `loadFile` 把"JSON 损坏"当成"文件不存在" ⇒ 读接口返回空列表(伪装成"你本来就没记过东西"),
  **下一次新增会把只含新记录的文件盖上去,原台账永久没了** —— 而回读复核还会确认新记录在、一路报成功。
  这与同一函数里"已保留原文件,请人工处理"那句直接矛盾:那条路根本走不到。
- 🔴 **同一根因漏改**:`format` 只补了 ledger 一处,兄弟编译点 `stageSchemas` 与运行期全局 ajv 都漏了
  ⇒ 阶段产物里写 `format: "date"` 等于没写。**上一轮刚犯过一次同类,这轮又犯。**
- 回读只比 `id + updated_at`:并发覆盖会保留同一 id,同毫秒里时间戳还一样 ⇒ 改成比**整条内容**。
- `/ledger` 分两次读盘 ⇒ records 与 issues 来自不同快照,界面会显示"这几条都合规"而展示的正是坏的那几条。
- 请求体上限按 UTF-16 码元数算:一个中文 3 字节只算 1,256KB 的上限实际能塞进约 768KB。改按字节。
- 运行产物 `viewer.html` 以 `text/html` 在**已认证的源**上渲染且无 CSP。加 `default-src 'none'; …; sandbox`。

**r4(收口)** 1 P2 —— **指着我自己的修复**:超限后 `req.pause()` 不收连接会把它挂住,
而且注释写着 413、代码走的是统一 400(**自己的测试只断言了 error 码、没断言状态码,所以放过去了**)。
最终改法是**边读边丢**:`destroy` 会让客户端拿到 EPIPE 看不到错误、`pause` 会挂住连接、
"响应 flush 后再 destroy socket"照样 EPIPE(客户端还在写)—— 只有边读边丢能同时做到内存有界、
客户端写得完、写完能读到 413。测试补上状态码与 `Connection: close` 断言。

**r5** 判"没有"。⚠️ 但它打印的代码里留着一句我没删干净的注释(还写着 `pause`),已自行删掉 ——
**审计者说没问题 ≠ 每个字都对**。

### 测试

新增 `ledger.test.ts` 12 · `chat.test.ts` 6 · `ingest.test.ts` 9 · providers 占位符与 chat 拒绝各 1;
审计各轮又补 20 条(台账 8 / 导入 4 / 对话 1 / 契约 1 / 服务与 API 3 / 其它 3),
每条都做过变异测试。合计:orchestrator **357 通过**,Python **413 通过**,desktop `tsc` 干净,
doctor **18 ok / 0 warn / 0 fail**,Core 行业词仍为 **0**(37/37 文件干净)。

## 1.0.0 开发记录 · 全面审计的 29 条全部修完(2026-08-25)

Simon:「之前都是小模块的审计,现在进行一个全面的审计」→ 四轮 Codex(每轮一个**跨模块视角**,
让它自己读代码)找出 29 条 → Simon:「全部修复」。汇总见 `全面审计_2026-08-25.md`,
原始产物 `.local/reviews/full-audit-{1..4}-*.md`。

### 一、产出红线可以绕过(发布阻断)

- **gate 只有 26 个中文子串**,「值得买入」「跌破 50 元离场」「BUY, target price」一条都拦不住。
  新增 12 条**正则规则**(中文语义组合 + 英文)。
  ⭐ **在 431 个真实产物上量过误报,零命中才收进来** —— 量的时候刷掉 4 条候选,其中一条会拒掉
  **正确的免责声明**「不是目标价或合理价」。还有一条「给予 X 买入评级」刻意不收:它命中的是
  **被引用的事实**(机构评级分布本来就是合法证据)。
- **阶段产物的自由文本根本不过 gate**(只查 report),而 viewer / 附录会把整个 record 抄给调用方。
  改为**按值递归**过 gate(刻意不枚举字段名——那会把金融字段写进 Core,而且新增字段必漏),
  用**收窄后的词表**(去掉 8 个"会出现在提及语境"的词):320 个真实阶段产物零误报。
- **附录与查看器在最终 gate 之后生成、从不复检,却经 API / MCP 回传** —— 而且它们自己写着
  「不含任何判断与建议」。**声称与代码不符**:两处措辞已改成代码真做得到的,并加上生成后复验。
- **events 泄露用户名 / 内网地址 / `user:pass@`**;**取数错误信封泄露 `VRA_SEC_CONTACT` 里的姓名邮箱**
  与第三方 traceback 里的 token。两侧都加了环境类脱敏(纵深防御,不承诺"绝不回显")。

### 二、静默失效("没跑"和"跑了没拦到"在产出上一样)

- **钩子 enabled + installed 却零调用 / 全报错,运行照样 complete** —— 我真跑分离安装时踩到的正是
  它的变体(5 次调用 5 次 error,只有 manifest 里一个计数写着 5)。现在这两种情况一律**降为 incomplete**
  并出事件。⚠️ 判 incomplete 不判 failed:研究结果本身可能是好的,只是少了一层保证。
- **必需 calc 返回 `error` 也算"已完成"** ⇒「必须计算」退化成「存在一条同名调用记录」。
  ⚠️ `not_meaningful` 仍算完成 —— 那是**确定的结论**不是失败。
- **必需端点 `partial` 只是 warning** → 现在要求写进缺口。⚠️ 收窄为"端点**自己声明了丢了什么**"才要求:
  实测 25 个真实 partial 里 24 个带 `degraded`,唯一不带的是行情陈旧(已由 quote_decision 单独披露)。
- **角色级 gap 带后缀(`quarterize:revenue_cum`)被语义槽位接受,状态推导却只认裸函数名**。
- **注册表里阶段名拼错 → 端点永远不执行、零告警**,现在注册期当场拒绝。

### 三、同一概念的两份实现

- `criticalAllFailed` 绑死金融阶段名 `"estimates"` → 改成看**关键脚本本身在不在本次作用域**。
- manifest 的 `market` 枚举写死沪深四项,而证据 / 取数用插件枚举 ⇒ 跑 US/HK 时**全阶段完成、收尾判 failed**。
- `referencedIds` 没有 token 边界(`citedIds` 一直有)⇒ `ev-abcdef123456xyz` 能冒充合法引用。
- 证据合并同 id 只比 `value` ⇒ 口径不同(单位 / 复权)的两条**静默折叠**。改为比事实键。
- `dataRoot="/"` 时钩子边界拼成 `"//"`,执行层全程 fail-open。

### 四、垂类边界:从"第二个垂类做不出来"到有验收装置

r4 的结论是**新增一个垂类包跑不起来**,至少 10 组 P1。全部处理:

- **9 个入口无条件注册金融** → 库模块(service / viewer / knowledge / fixture)的自注册全部去掉,
  只留 6 个真入口(composition root),并加**守卫测试**防止长回来。
  顺带发现 `thermo_history` / `hardtest` / `provider_matrix` 三个**包内模块也在自注册**,
  形成 `finance/plugin → stages → thermo_history → register` 的循环依赖。
- **Core 主循环直接 import 金融模块** → 新增 `beforeFetch` / `afterFetch` / `transformFetch` /
  `afterRun` / `doctorChecks` 五个钩子,产业门控、卡口扫描、温度计历史、体检项全部搬进包里。
- **按字面量阶段名分支** → 新增 `reportStage` / `topicsSourceStage` / `stageSchemas` / `stageValidators`
  四个插槽;`schemas.ts` 里整段金融字段(报价判定 / 不可替代性标签 / 反证 / 裁决点)与
  `validator.ts` 里两段阶段专属校验全部移入 `finance/`。
- **`regionOf` 的市场映射** → `marketRegion` 插槽。alerts / service 的市场校验改用契约枚举。

⭐ **给棘轮补上它此前看不见的那一类**:英文阶段名、市场代码、结构性 import。
旧棘轮只扫中文词表,所以「行业词 0」给的是**错误的安心感** —— 新棘轮一上来就抓到 **32 处**。
⚠️ 判定只看**代码不看注释**(注释里写"这里原本是 `stage === \"profile\"`"是有价值的历史说明),
且只算**当阶段用**的字面量 —— 实测有四处纯误报(HTTP 路由段、体检项 id、HTML tab 名),
把它们也算进去只会逼人改无关代码,**棘轮误报会让人开始无视它**。

⭐⭐ **新增第二个垂类的验收装置** `test/second_vertical.test.ts`:一个完全不含金融概念的
最小餐饮包(`intake → menu → competition → report`,市场 `SG`),**不改任何 Core 代码**就能注册、
拿到自己的 schema、按自己的口径解析作用域。它一上来就打红三处契约缺陷:
`extraTopics` 强制每阶段非空、`alertFields` 强制非空、`selfTestCalc` 强制存在 ——
**金融包恰好都满足,所以这些约束一直没暴露**。
(`extraTopics` 放宽的前提是把"没有议题"表达成 `maxItems: 0` 而不是留一个空 enum ——
后者的报错说"topic 不在允许值里",看不出根因。)

### 验证

每条修复都做**变异测试**。过程中抓到自己的三个问题:
① 测试直接调内部函数、绕过真实路径(把调用点删掉测试照样绿)→ 改为走 `validateStage`;
② 两处**不可达的冗余检查**(validateStage 一报错,后面的状态降级永远走不到)→ 删掉,
不留"永远不会红的校验";③ 变异脚本备份晚了一步把调用行弄丢,全量测试当场打红。

### 修复复审四轮(r1 3P1+3P2 / r2 1P1+3P2 / r3 1P1+2P2 / r4 1P1+1P2)

🔴 **好几条是我修上一轮时自己引入的**,最典型的两组:

- **viewer 净化改了四版**:①按行删除 → 把整个 `<script id="data">` 数据块删没了,页面报废;
  ②改数据层逐字符串替换,但**对象键没查**(键也载得动建议)、**结构性字符串被误替**
  (`run_id: "BUY"` 整串换掉,证据追不回去);③按键名放行,可 `source` / `endpoint` / `field`
  在 schema 上只要求"非空字符串",`source: "<一句建议>"` 原样外传;④**键名不等于位置** ——
  calc 的 `inputs` / `output.details` 是自由形状,里面同样叫 `status` 的字段能装任意文本。
  ⇒ 终版是**键名 + 取值形状**双条件:身份 / 枚举 / 日期 / 哈希 / 相对路径都是"无空白紧凑串",
  一句话不可能长这样;形状放宽就等于把后门开回来。
- **gap 后缀匹配连踩两次同一个坑**:先是任何后缀都剥(编个角色就能把失败盖过去),
  改成"后缀须是已声明角色"后仍写 `const [fn, role] = split(":")` —— **只看前两段**,
  追加第三段(`<函数>:<真角色>:任意尾巴`)照样蒙混过关。
  ⇒ **判定"是不是那个东西"要拿整体比,不能只看前缀对不对**;两处调用点已抽成同一个函数,
  免得口径再漂移。

另修:必需 calc `status=error` 的退化路径(解析不出角色时)只接受**严格相等**的 gap;
占位键 `〔已移除键-N〕` 生成时同时避开原有键与已写入键,否则会覆盖别人的数据。

⚠️ 补测试时两次"变异仍绿" —— 用例落在了**相邻的另一条分支**上(先被"缺少 calc"拦下,
走不到 error 分支;或反过来)。**断言写得像那回事不等于测的是那条路**,靠变异测试才发现。

### 验证

每条修复都做**变异测试**(六条全部能改回红)。过程中抓到自己的三个问题:
① 测试直接调内部函数、绕过真实路径(把调用点删掉测试照样绿)→ 改为走 `validateStage`;
② 两处**不可达的冗余检查**(validateStage 一报错,后面的状态降级永远走不到)→ 删掉,
不留"永远不会红的校验";③ 变异脚本备份晚了一步把调用行弄丢,全量测试当场打红。

测试 **TS 310 / Python 413 / doctor 16 ok · 0 warn · 0 fail**;Core 行业词 0;
外加三次真实 agent 运行确认链路没坏(最后一次 `verify-r4`:profile complete、
validator ok、79 条证据、hooks 零错、viewer 数据块完整)。

## 1.0.0 开发记录 · 指令发现链:解开分离安装的阻塞,顺带修掉一个现存的静默缺陷(2026-08-25)

原本要解的是「`data/` 放到产品根之外时引擎发现不到 `AGENTS.md` / `.agents/skills`」——
它同时卡住安装布局与用户数据迁出。查下来发现问题比预期大。

### 先把引擎的规则搞清楚(实测,不是读代码推断)

`codex debug prompt-input` 能渲染**模型可见的提示词**且**不调用模型、不花额度**,所以整条规则是用
七个金丝雀探针量出来的(表格见 `docs/instructions-root.md`)。宪法与项目技能用**同一条规则**:
收集 **project root 到线程 cwd 之间每一层目录**里的两者;project root = 从 cwd 向上第一个含
`project_root_markers` 的祖先(默认 `.git`),找不到就**只看 cwd 那一层**。**找不到时引擎不报错。**

### 🔴 顺带挖出两个现存缺陷,都要 `git clone` 才碰不到

现在能工作只是因为**恰好有 `.git`**。开源产品的用户多数下载 zip 而不是 `git clone`:

1. **`exec` 入口有一道门:cwd 不在 git 仓库里就 exit 1**,报
   `Not inside a trusted directory and --skip-git-repo-check was not specified.`
   ⇒ **zip 解压装的用户,每次运行都直接失败**,而且报错完全看不懂。
   ⚠️ 这句话的措辞有误导性:判据与 `[projects] trust_level` **无关**,就是
   `get_git_repo_root(cwd).is_none()`(`codex-rs/exec/src/lib.rs:798`)。
   修法:`skipGitRepoCheck: true` —— 运行目录是产品自管的数据目录、不是用户源码树。
2. **产品若装在用户自己的 git 仓库里,那个仓库的 `AGENTS.md` 与技能会被一起读进产品线程**
   (不是发现不到,是反向污染,与 `~/.agents/skills` 那次同一性质)。自定义 marker 堵住这头。

⚠️ **我一开始把①说成了"静默丢掉产出红线",那是错的** —— 那是 `debug prompt-input`
(只看提示词组装)层面的现象;真实运行**在组装之前就退出了**,是硬失败不是静默。
把这两层合起来看才是真实结局。**只验一层就下结论,结论会反着说。**

### 做法

新增 `orchestrator/src/instructions_root.ts` 的「指令根」概念 = 宪法与技能所在的那一层,
**必须是运行目录的祖先**。产品自带 `.vibe-research-root` 标记(随仓库发行)并把它写进
产品 `CODEX_HOME` 的 `project_root_markers`,一举解决两头。数据根与产品根**无路径关系**时
(将来 `/Applications` + `~/Library/Application Support`),指令资产**同步**到数据根;
宪法母本始终是产品根那份,副本逐字节相同由 preflight 保证。

运行前逐条 preflight,**不过就抛** —— 这些失效全是静默的。doctor 加了「指令发现链」检查项。

### Codex 四轮审计(r1 5P1+4P2 / r2 2P1+1P2 / r3 1P2 / r4 1P2),全部核实成立

最要紧的一条是**我自己写的东西压根没生效,而体检还报 OK**:

> `project_root_markers` 被追加到 config.toml 末尾,而 **TOML 表头作用域会一直延续** ——
> 这个顶层键落进了 `[hooks.state."…"]`。在真实安装上用 `tomllib` 实测确认:顶层根本没有这个键。
> 体检之所以绿,是因为它查的是"生成块在不在",**不是这个键是否真的在顶层**。

⇒ **校验要盯效果,不能盯自己刚写下的痕迹。** 现在顶层键块一律放**文件最前面**,
校验改成"文件必须以预期块开头 + 内容逐字相同 + 块外没有等价键(含引号形式)"。

⇒ 另一条同样值得记:**我的 e2e 测试当时是绿的** —— 因为夹具里的 `config.toml` 太干净(没有表头)。
后来改用"脏" `config.toml`(带 `[projects]` / `[hooks.state]`)重跑才复现。**夹具比真实环境干净 = 假绿。**

其余修复:链上更近的 marker 会把 project root 拉下来(拒绝)· 技能镜像改整棵树 sha256 比对
· 符号链接(目标端删掉重写;**路径中间段**是链接会顺着它删掉数据根之外的文件 → 拒绝)
· 源与目标互相包含(拒绝)· 路径比较改 realpath · 生成块分隔符只认独占整行、不在多行字符串里、
闭合检测认转义。为最后一条把 `skills_isolation.ts` 里既有的逐行 TOML 扫描件抽成 `src/tomlscan.ts`。

### 顺带修掉一个没人发现的抖动

`mergeBlock` 原本是"删掉再追加到末尾",于是同一个 config.toml 里的多个生成块
(hooks / skills 隔离 / project root)会**互相把对方顶到后面** —— 每次运行都重写、`changed` 永远为真。
改为**就地替换**。

### 验证

十四条修复逐条**变异测试**(改回旧写法,对应测试必须变红)。
真引擎端到端:**数据根完全在产品根之外 + 带表头的脏 config.toml**,宪法与技能都被发现。
测试 TS 292 / doctor 16 ok · 0 warn · 0 fail;Core 行业词仍为 0。审计产物 `.local/reviews/instructions-root-r1..r4.md`。

### ⭐ 真跑之后又挖出三个阻塞(Simon:「需要验证的就验证」)

上面那些做完,我把分离安装列成了"未验证的残留"。**真跑四次,一个都不是理论问题**:

| 第几次 | 结果 | 挖出什么 |
|---|---|---|
| 1 | ❌ 阶段 failed,**钩子 0 次调用**,三次尝试相隔 46 毫秒 | 引擎压根没启动(所以**没烧额度**) |
| 2 | ✅ 阶段 complete,但**钩子 5 次调用 5 次 error** | 执行纪律全程失效,而产出看不出来 |
| 3 | ✅ complete,钩子 6 次 **0 error** | 前两条修好了 |
| 4 | ✅ 同上,数据根建在 `$HOME` 下 | 又发现 `allowedPathPrefixes` 缺 `dataRoot` |

1. **`exec` 的 git 仓库门**(见上)→ `skipGitRepoCheck: true`。
2. **`contextMatchesCwd` 还要求运行目录在产品根之下** → 改成数据根。
   ⚠️ 这条最阴:**运行成功、报告产出、阶段 complete**,只有 `manifest.hooks.errors` 里写着 5。
   ⇒ **执行层"没跑"和"跑了没拦到"在产出上长得一模一样。** 而 `contextMatchesCwd` 此前
   **一条测试都没有** —— 这就是它能活到今天的原因。
   ⇒ 也是"我在别处拆掉旧假设、却漏了这一处"的又一例:**同一根因要一次找干净。**
3. **`allowedPathPrefixes` 缺 `dataRoot`** → agent 连自己的运行目录都读不了。
   ⚠️ **临时目录测不出来**(`/var/folders` 恰在默认白名单里),把数据根放 `$HOME` 下才暴露。

### 顺带定下一条产品约束:根路径不能含空格

命令扫描器按空白切 token,`~/Library/Application Support/X/runs/…` 会被切成
`/Users/…/Library/Application`,与允许前缀永远对不上 → agent 引用运行目录的命令**全被拒**(实测)。
`makeConfig` 改为**配置期就拒绝**并给替代建议。⇒ macOS 上装到 `~/Library/Application Support` 不行,
用 `~/.vibe-research` 这类(引擎自己的 `~/.codex` 就是这个风格)。
不去改扫描器分词:那套规则用 1,142 条真实命令语料回归过,动它的风险比这条限制大。

### 剩下的残留

`init` / `doctor` 仍要求数据根在产品根内 —— 那是面向仓库内布局的策略,**不是运行路径的限制**
(运行路径已实测支持分离安装)。分离安装的初始化归 launcher(未开工)。

## 1.0.0 开发记录 · 档案模板变成契约(2026-08-25)

`knowledge.ts` 是 Core 里最后一块写死垂类的地方:六个章节标题、每节放什么、有效期 90 天、
关键数据最多 40 条、"§4/§5/§6 是尾部章节要优先保留"的正则 —— 全是金融档案的样子。
换个垂类得改 Core。这次搬进 `Plugin.archive` 契约。

### 声明式配置,不是模板引擎

插件声明**分节顺序**,每节挑几个**内置区块**并配参数:

```ts
archive: {
  validDays: 90, maxFacts: 40,
  sections: [
    { title: "1. 业务与上下游位置", blocks: [{ kind: "stageSummary", stage: "profile", extras: [...] }] },
    { title: "4. 裁决点(什么数据出来会改变判断)", tail: true, blocks: [{ kind: "decisionPoints", stage: "risk" }] },
    { title: "6. 对上次档案的裁决(knowledge_conflicts)", tail: true, omitIfEmpty: true, blocks: [{ kind: "knowledgeConflicts" }] },
  ],
}
```

九种内置区块:`stageSummary` / `evidenceTable` / `stageSummaries` / `standardColumnsTable` /
`conclusions` / `conflictCount` / `decisionPoints` / `gaps` / `knowledgeConflicts`。

🔴 **刻意不让插件塞 markdown 字符串进来** —— 那样脱敏 gate、数字纪律、证据 id 绑定就全绕过去了。
新垂类要新排版 = 在 Core 加一种 kind + 一条 schema 分支,**加法是有审查的**。
形状与 VS Code 的 `contributes.views` / `walkthroughs` 一致:声明结构 + 引用内置渲染器。

### 注册期就拒绝(不是运行到那一节才发现)

区块是**判别式 union**(ajv `discriminator: true`),报错能定位到具体哪种区块少了哪个参数,
而不是笼统一句"不匹配 oneOf"。另外两条 JSON Schema 表达不了的关系检查:

- **区块引用的阶段必须真的存在** —— 阶段名拼错的话,渲染期只会静默出一行"(无)"。
- **至少要有一节 `tail: true`** —— 一节都不标,召回截断时"优先保留尾部"整个失效,
  而且是**静默**失效:裁决点和数据缺口被一起截掉,报告里看不出来。

`truncateBySection` 的尾部判定从写死正则 `/^## (4\.|5\.|6\.)/` 改成按插件标题匹配。
⚠️ 残留:旧档案是改标题之前写的 → 一节都匹配不上 → 尾部保护退化(重构前写死正则也是同样的性质,已在注释写明)。

### 真实回归

拿真实运行 `simon-local-2`(820 证据 / 24 计算)重新渲染,与当时归档的 `.md` 逐字节 diff,
**只有两处差异,都是预期内的**:①§1 标题(那是更早一次会话改的,不是这次)②§2 那句
"每条带 报告期 / 来源 …最多 40 条"从标题里挪到标题下一行(标题里带变量就得上模板引擎,不值当)。

新增的两条关系检查都做了**变异测试**(把检查改成 `if (false)` → 对应测试立刻变红),
避免写了个永远不会红的校验。

### 顺带

- `KNOWLEDGE_VALID_DAYS` / `KNOWLEDGE_MAX_FACTS` 从 Core 删掉(改由插件 `archive` 给);
  `KNOWLEDGE_MAX_CHARS` 留在 Core —— 它是提示词预算,与垂类无关。
- `test/knowledge_viewer.test.ts` 的截断用例改为**从插件契约取标题**构造正文。原来写死
  `"## 4. 裁决点"` 这种字面量,插件改标题时它会**绿着失效**(测的是自己造的假数据)。
- 已知残留(不算耦合,是证据契约本身的措辞):关键数据表的表头「指标 / 报告期」和
  「标准产出列」仍写在 Core —— 它们标的是 Core 的 `Evidence` 字段与 `standardColumns` 槽位。

### Codex 审计三轮(archive-r1 / r2 / r3)

r1 契约层 1 P2、渲染层 2 P1 / 3 P2 / 1 P3;r2 复审 1 P1 / 2 P2;**r3 无新 P1**。修了这些:

- **表格单元格没转义 `|`**(P1,重构前就有)。证据值 / 摘要都来自外部数据源,里面一个 `|` 就多切一列 ——
  表格不报错,只是**字段整体错位**(值被读成来源),而这份档案下次会原样注入提示词。
  🔴 r2 又指出**只替 `|` 是修不掉的**:输入 `A\|B` 会变成 `A\\|B`,两个反斜杠互相转义掉、
  `|` 又变回分隔符 —— 必须**先转义反斜杠**。
- **`truncateBySection` 会超预算**(P1)。尾部本身超长时会带上两个截断标记,返回长度 > max。
  改成 `fit(t, budget)` 保证任何输入都 ≤ budget;连接换行只在两段都非空时计入
  (—— 自己写的边界测试当场抓到 head 为空时仍无条件加换行的 off-by-one)。
- **`tail` 判定用 `startsWith` 会前缀误匹配**(P2)。tail 标题「风险」会把更早的「风险因素」一节
  当成尾部,连带把它后面全部保护起来。改成**整行相等**。
- **`items` 语义两处不自洽**(P2):`stageSummary` 只看 summary,"摘要空但标签有值"的章节会被
  `omitIfEmpty` 静默丢掉;`conclusions` 恒定从 1 起,空章节永远省不掉。
- **`validDays` / `maxFacts` 只校验了下限**(P2)。`1e100` 也是合法 integer,
  而 recallKnowledge 里 `as_of + validDays 天` 会**抛 RangeError**(Codex 说"永远 fresh",实测是崩)。
  🔴 顺着这条查同类,发现**同一个洞在磁盘那侧更严重**:`valid_days` 还能从档案 frontmatter 进来,
  那是**用户数据区的文件 = 不可信输入**,旧代码 `Number(fm.valid_days) || 兜底` 拦不住 —— 这是
  **重构前就存在的 bug**,变异测试证实旧写法确实抛 RangeError。两侧都夹紧到 1..3650。
- **`Infinity` 被归一化成 0**(r3):语义上"预算无限"应该全文照返,不是全部吃掉。

新加了一条注册期校验:**`tail` 章节必须是连续后缀**。中间夹一个 tail,截断会把它后面的普通章节
一起当尾部保护而且不报错 —— 在注册期拒掉,渲染和截断两侧就都不用处理交错情形。

**两条不采纳,写明理由**:
1. r1 说"gate 删行后可能只剩表头,调用方无法发现" —— 不成立:`archiveRun` 返回 `gateRemoved`,
   `gate_removed` 计数进了 manifest 也打了事件日志(`orchestrate.ts:447`)。r3 已接受。
2. r3 建议兜底时"取 matched 与 最后 N 节 的较早者",让部分改名的 tail 也能保住 —— **不采纳**:
   `omitIfEmpty` 的 tail 章节被省掉时实际节数少于 N,那个式子会把 §3 这种普通章节也算进尾部。
   宁可少保护,不要在正常情况下悄悄扩大保护范围。

### 每条修复都做了变异测试

把修复改回旧写法,对应测试必须变红。**11 条里 10 条红了,1 条没红** —— `max` 的 NaN / 负数归一化:
实测不归一化时那些输入**恰好**也返回空串(NaN 让每个比较都为 false)。所以那行是**把契约写明确**、
不是修当前的 bug,代码与测试里都如实标注了(`tails.length > 0` 那个条件同理,与算式冗余,已标注)。
⇒ 不这么验一遍的话,这两处会被当成"已修复并有测试保护",而它们其实一条都拦不住。

测试 TS 275 / doctor 15 ok · 0 warn · 0 fail。Core 行业词仍为 0。

## 1.0.0 开发记录 · 校验改用 ajv,自造名换成通行叫法(2026-08-25)

上一版的注册期校验全是手写 if-else,名字也是自造的 `DomainPack`。这次两件事一起做。

### 名字:换成通行叫法

`DomainPack` → **`Plugin`**;`registerDomainPack` → `registerPlugin`;`currentPack` → `currentPlugin`;
`resetDomainPack` → `resetPlugin`;`FINANCE_PACK` → `FINANCE_PLUGIN`;`DomainLexicon` → `Lexicon`;
`setDomainLexicon` → `setLexicon`;`PACK_DIRS` → `PLUGIN_DIRS`;
`src/domain.ts` → `src/plugin.ts`,`src/finance/pack.ts` → `src/finance/plugin.ts`。

理由:这套东西**每一块都有成名做法**,用自造词只会让别人查不到。契约头部已写清对应关系 ——
形状上等同 VS Code 扩展清单的 **contribution points**;依赖方向是 Cockburn 的 **Ports & Adapters**;
"同一内核 + 每个垂类一个包"在学界叫 **Software Product Line**(core assets + variation points);
商业上就是 **Salesforce Industries** 那套。⇒ 以后遇到拿不准的地方,**去查这些名字,别自己发明**。

### 校验:三层分工

| 层 | 谁做 | 管什么 |
|---|---|---|
| 形状 | `ajv` + `PLUGIN_SCHEMA` | 字段在不在、类型、非空、去重、阶段名是不是安全路径段 |
| 语义关系 | 手写 `checkRelations` | 键集与 stages 一致、子集关系、跨表引用 —— **JSON Schema 表达不了** |
| 深层 JSON 性 | 手写 `deepFrozen` | 两个自由形状字段里不许有 `Map` / `NaN` / `undefined` / 稀疏数组 |

不全用 ajv,是因为 JSON Schema 逐字段,说不了"A 的键必须等于 B 的元素";
不全手写,是因为形状校验手写又长又容易漏,而仓库里本来就用 ajv 校验证据与 manifest。
与 `providers.ts` 一样直接用 Ajv 而不走 `schemas.ts`(`schemas.ts → config.ts → plugin.ts` 会成环)。

### 🔴 迁移校验最大的风险是"悄悄丢掉一条检查" —— 这次丢了四条,全被抓回来了

自查抓到两条:
- **ajv v8 的 `type: "number"` 接受 `NaN` / `Infinity`**(实测 8.20.0;ajv v6 才带有限性检查)。
  我照旧版行为想当然写了注释,已在 `checkRelations` 手补 `selfTestCalc.expect` 的有限性。
- **`minLength: 1` 放行纯空格 `" "`**,而旧的手写校验是 `trim() !== ""` —— 比原来松。
  已加 `NONBLANK`(`minLength:1` + `pattern:"\\S"`)。

Codex 审计(ajv-r1 → r3 通过)抓到两条:
- **`additionalProperties: false` 看不到原对象的多余字段** —— 因为 ajv 校验的是**投影出来的 `decl`**,
  多余字段在进 ajv 之前就被投影丢掉了。**我在注释里写的"多写一个字段就当场说"原本是假的。**
  已加 `assertNoExtraKeys()` 显式比对原对象键集(根 / evidence / selfTestCalc / 每个 stageScripts.<stage>)。
- **`__proto__` 作表键时会被普通 `{}` 静默吞掉** —— `semanticSlots: {"__proto__": []}` 会变成空表,
  于是"键必须 ∈ stages"在空集上平凡通过。改为**直接拒键**(`__proto__` / `constructor` / `prototype`)。
  ⚠️ Codex 建议的"容器一律 `Object.create(null)`" **试了但没采用**:实测会让 `currentPlugin()`
  返回无原型字典,`assert.deepEqual` 与比较原型的消费者当场炸(三个既有测试打红)。拒键更简单、报错更直白。

🔴 **"每个字段只读一次"这条纪律,同一根因犯到第三次**:先是五张表(上一轮),
再是函数插槽(`quoteDecision` 校验读一次、建快照又读一次),这次是 `stageScripts`
(多余字段检查里又 `tableOnce` 了一遍)。⇒ **修完一处必须问:同类还有几处?**

新增回归 5 条(`test/plugin.test.ts` 共 26):getter 只读一次(函数插槽 / stageScripts)、
四处多余字段被拒、`__proto__`/`constructor` 被拒、纯空格被拒、`NaN`/`Infinity` 被拒。
测试:TS **270** / typecheck 干净 / Python 222 / calc 188 / doctor 15 ok / 纯净度 0。

## 1.0.0 开发记录 · Core / DomainPack 第二次拆分:stages / schemas 变成可注入契约,Core 行业词清到 0(2026-08-25)

第一次拆分只搬了词表,Core 里还剩 149 处行业词。这次把**阶段与 schema 也做成契约**,清到 **0**。

### 新的 `DomainPack` 契约(`src/domain.ts`)

判断一样东西该放哪边,只问一句:**换个垂类它要不要重写?** 现在这些全部经注入:
阶段名 / 每阶段取数脚本 / 关键脚本 / 每阶段计算函数 / 议题 / 报告章节 /
证据枚举(市场、口径、两套全市场区域码)/ 标准列与列标签与所在阶段 / 阶段显示名 /
议题→章节映射 / 提醒字段 / **语义槽位表** / **"数据陈旧"判定** / **基准期解析** / doctor 自检 calc / 词表。
金融那一份在 `src/finance/pack.ts`。

🔴 **用注册期完整性校验替代编译期穷尽性检查。** `Stage` 从字面量联合退化成 `string`,
`Record<Stage, …>` 的穷尽检查随之失去 —— 换来的是注册时**四张阶段表的键集必须与 stages 完全一致**
(不许缺、不许多)。对可插拔设计这比编译期更管用:它同样拦得住第三方包写漏,
而编译期只保护得了我们自己的代码。⚠️ 但**别把它说成完整替代** —— 它补回的是"阶段键穷尽性"这一项。

### 结构性收口

- **金融实现物理搬进 `src/finance/`**(stages / hardtest / industry / chokepoint / thermo_history /
  provider_matrix + pack / lexicon / semantic_slots / quote_freshness / fiscal_year):
  纯净度检查从**文件名白名单**改成**目录边界** —— 白名单挡不住"把通用编排继续写进 src/stages.ts"。
- **注册点从 Core 消费者移到入口**(run / api / mcp / doctor / service / 钩子子进程 / 各 CLI / 测试):
  validator 不再副作用 import 金融包。
- **`hooks/` 纳入 typecheck**。它一直不在 `tsconfig.include` 里,所以 `hooks/stop.ts` 引用已删除的
  `STAGES` 时编译器一声不吭 —— 是运行时才炸的。

### Codex 审计闭环:七轮(domainpack-r1 → r7 通过)

**其中 4 条是我修上一轮时自己引入的**,都是同一个根因的漏改:
- 「每个字段只读一次」我只对 `stageScripts` 做了,**其余五张表照旧二次读取 getter**;
- 补救时写的 `entriesOnce()` 把 `new Map()` / `new Date()` **吞成空表**,
  没有阶段穷尽检查的那几张表于是"空表通过",等于绕过整个注册期校验;
- 「深冻结」对非普通对象**原样返回**,`Map` 仍是共享可变引用,`shared.set()` 就能改掉已生效配置;
- 稀疏数组检查我写在了 `.map()` 回调里 —— **回调对空洞压根不执行**,被自己新加的测试当场打红。

🔴 **第四次"照 Codex 字面实现会坏"**:它建议 `topicSections` 的值用 `reportSections` 校验。
但那是**两个命名空间** —— `reportSections` 是必需骨架章节,而 topicSections 的值是**扩展章节**名
("资金与市场行为")。照字面实现会把正确配置全判错。只采纳了键的校验(议题必须已声明)。

其余采纳的修复:阶段名必须是**安全路径段**(它会被拼进 `stages/<stage>.json`,写个 `../..` 就穿出去了);
`criticalScripts` 必须出现在某阶段计划里(拼错则"关键脚本全失败 → failed"永远匹配不上,是**静默降级**);
`standardColumnLabels` 必须**恰好覆盖** `standardColumns`(缺列会静默少一列表头);
`selfTestCalc.args` 必须是**普通对象**且只含 JSON 式值(`Map` / `NaN` / `undefined` / 稀疏数组
在 `JSON.stringify` 后与快照对不上,而它是要传给 calc CLI 的);
深拷贝用 `Object.create(null)` 防 `__proto__` 原型污染;`resetDomainPack` 连词表一起清。

### 关于"纯净度 0"的诚实说明

**是**:`src/`(除 pack 目录)里不再出现那张词表上的任何词。
**不是**:Core 已与垂类无关 —— 证据契约的 `symbol` / `market` / `period` / `adjustment`
仍带证券味道,`knowledge.ts` 的档案模板仍是金融写法。**词表只测得到它认识的词。**

这次又抓到**两个假阳性 + 一处词表看不见的真耦合**:
- `registry.ts` 的 `const eps` 是 **endpoints 的缩写**,被当成了 `EPS`(已改名消歧);
- `runner.ts` 的「盘前」是从"落**盘前**脱敏"里切出来的(CJK 逐词 substring 没有词边界,已加负向前瞻);
- 而 `batch.ts` 硬编码的 `"valuation.json"` **一路躲过了棘轮** —— 词表全是中文与几个缩写,
  **看不见英文阶段名**。已改为 `standardColumnsStage` 插槽。
⇒ 沿革:149 → 126(阶段/schema 进包)→ 83(语义槽位 + 报价规则)→ 69(显示名/映射/字段/列标签)
→ 18(注释中性化)→ **0**;棘轮改**零容忍**。

### 遗留(已写进注释,不是本次范围)

- 词表是**进程级单例**。这与产品形态一致(一个垂类一个仓库、一个安装包、一个进程);
  要同进程多租户才需要实例级 DomainRuntime。
- `topicSections` 的**值**、`semanticSlots ↔ stageCalcs` 的关联,现有契约表达不了,
  要新增字段才能校验。

新增 `test/domain.test.ts` **20 条**(注册期校验是编译期穷尽性的替代品,失效了没人会发现)。
测试:TS **265** / typecheck 干净(含 hooks/)/ Python 222 / calc 188 / doctor 15 ok。

## 1.0.0 开发记录 · Core / DomainPack 第一次拆分(2026-08-24)

战略转向垂类 AgentOS(同一个 Core,按行业挂不同 DomainPack)。架构审计的验收标准 =
"Core 中不再出现 A股 / EPS / PE / TTM / 申万行业等词汇"。本次是**第一次搬迁**,做了四件事:

1. **数字忠实度判定提升到生产**(`number_fidelity.ts`)。原先"报告里的派生数字必须逐字照抄 calc 的
   `display`"只活在硬测试里,生产 `validateReport` 只查"章节在不在、id 存不存在" ——
   **报告可以引一个真实的 calc-id 却写另一个数字,仍判 complete**(架构审计点名的最关键缺口)。
2. **词表注入**:金融正则移入 `finance/lexicon.ts`,Core 侧只留 `DomainLexicon` 契约 + 注册;
   **未注入直接抛错,不给静默默认值**(给默认值等于让 Core 偷偷留一份金融词表)。
3. **纯净度棘轮** `core_purity.test.ts`:递归扫描 Core,行业词只许降不许升。
4. **版本单一来源** `version.ts`:此前 api.ts 与 mcp.ts 各写死 "0.5.0",而 package.json 是 "0.1.0"。

**Codex 审计闭环:数字忠实度七轮(r1 → r7 通过)、边界机制九轮(r1 → r9)。**

⭐ **最有价值的一件事:把 Codex 的一条批评拿去实测,发现它指向的是真缺陷。**
它说"纯 evidence 行完全不校验"。我没有照单全收也没有直接反驳,而是在本机 **48 个真实运行**上量:
30 个适用运行、1753 个 token → 报出 4 个,逐个核对 **全部是真缺陷,零误报** ——
某次运行的事实表把证据 id **整体错位了一行**(写 70.96 的"均值"行,引的 id 实际值是 39.03)。
⇒ 扩范围是有实测依据的,不是猜。这条纪律后来贯穿全程:**每次收紧都先量误报率再落地**。

🔴 **三次"照 Codex 字面实现会坏",都是我的测试 / 实测抓到的**:
- "一律禁止带 `g` 标志的正则" → 金融包的 `subjectCodePatterns` / `windowLabelPattern`
  是给 `.replace()` 用的,**本来就必须带 `g`**。正确切法是**按用途**分开校验。
- "整数只能绑 calc 的 inputs" → 四锚的锚点 30 住在 `output.details.anchors`、inputs 是空的,
  "30 倍锚"是**正当写法**会被误拦。正确切法 = inputs + details,**只排除 `output.value` 本身**。
- "冻结快照里的正则" → **实测不行**:V8 的 `RegExp.prototype.exec` 无条件写 `lastIndex`,
  冻结之后连 `.test()` 都抛 `TypeError`。这条路是死的,已写进注释别再试。

**发现两处"棘轮自己不可信"**(词表有盲区时,棘轮的数字就是假的):
- ASCII 词表区分大小写 ⇒ `pe_ttm` / `eps_consensus` 这类**小写字段名**一直没被数到。
  改成大小写不敏感后 **67 → 146**,validator 从 13 跳到 58。
- `A\s*股` 被整体转义成了字面量,`A股` 一个都命中不了 ⇒ 修好后 **146 → 149**。
  ⇒ **这两次上跳都不是新增耦合,是旧耦合暴露**;基线已重标并写下这条教训。

**其余修复**(每条都对应一个具体触发场景,详见 `.local/reviews/`):
display 匹配不再允许截断数字("37.4" 不再命中 "37.40 倍");失败结果的残留 display 不是合法靶子;
历史引用加左边界("59.09%" 里藏不下编造的 "9.09%")但**故意不加右边界**(引用旧值时截位是正常写法);
`missingDisplay` 按每条 calc 自己的 `calc_version` 判(不误杀真旧运行);违规**拆两类** ——
纯 evidence 行的违规**不受 `applicable` 门控**;符号闭环(方向词承载负号、Unicode 负号、
行首 markdown 列表标记不是负号、行首 `−` 是负号);词表注册加护栏(按用途校验标志、
冻结克隆快照、逐字段各读一次、原子提交、重入守卫)。

**诚实记下的两条现状**(不是缺陷,是进度与边界):
- Core 里还剩 **149 处**行业词(validator 59 / batch 15 / alerts 14 / schemas 14 / config 12 …);
  stages / schemas / validators / reportSections **尚未**成为可注入契约。
  ⇒ **这是第一次搬迁的进度,不是"已经能挂第二个包"。**
- 词表是**进程级单例**。已把"静默串包"改成"当场失败",但真正的多垂类隔离要
  实例级 DomainRuntime 从 composition root 贯穿 —— 那是下一步。

测试:TS **244** / Python 222 / calc 188 / doctor 15 ok。

## 1.0.0 开发记录 · 第 18 层「宏观概率」(预测市场定价概率)(2026-08-24)

Kalshi + Polymarket 的零鉴权公开行情 → 6 个核心模块(货币政策 / 宏观经济 / 地缘政治 / 政治选举 /
股指大宗 / AI科技)的**市场当前定价概率**。用处不是"预测",而是**给前瞻假设找外部参照**:
一致预期里的增长假设隐含了某种宏观情形,预测市场对同一情形有独立定价,两者背离本身就是线索;
合约结算日(FOMC 会议日、CPI 发布日)天然是"下一个数据点",可直接进裁决点。

**Codex 审计闭环:十轮(prob-r1 → r10 通过)。** 4 条 P1 + 十余条 P2/P3。
🔴 **其中 3 条是我在修复过程中自己引入的**,包括一个正常路径必崩的 `UnboundLocalError` ——
那次 `.replace()` 锚点没匹配上却**没写断言**,静默没生效;而测试也没抓到,
因为它们把被测函数整个 monkeypatch 掉了。⇒ 已补两条**真的走进这两个函数**的用例。

四条 P1(原始):
- **`raw_ref` 串错**:每个场所只存第一次广度请求的 raw_ref,却挂到该场所**所有**证据上 ——
  第 2 页与定向系列拿到的合约,证据指向的 raw 里根本没有它。"每个数字都能追到 raw"这条产品命题
  在这一层是假的。现在每页 / 每次定向请求各自 `record_raw`,时间戳也跟着请求走(`_get_stamped`)。
- **Polymarket 把 `outcomePrices[0]` 当目标概率**:`outcomes` 顺序不保证是 ["Yes","No"],
  顺序一反报出去的就是**反向概率**。现在两个数组配对取 Yes 腿,认不出就丢弃并计数,**不猜**。
- **ask 与 last 混成一个无标识口径**:价差大或久未成交时同一字段时而是报价、时而是成交价,
  跨合约不可比。现在记 `price_type` 并写进证据。
- **上游 warnings 被 mapper 丢弃**:定向取数遇 429 / 结构漂移只降成 warning,一丢,status 还是 ok ——
  整个宏观经济模块静默消失而外面看不出异常。

审计过程中收敛出的几条纪律(都是"声称与代码不符"的实例):
- **"两个源都失败"必须是真的**:判据是**成功的源数**,不是"有没有 item"。
  一个源报错、另一个成功但没有符合条件的合约,原先会报"两个源都失败"——假事实。
- **"完整查完" ≠ "没抛异常"**:定向失败被降成 warning、或翻页被截断时,这个源**没资格**支撑
  "当前没有符合条件的合约"这种断言。现在每个源返回 `complete`,mapper 据此分四种说法。
- **不造日期**:缺结算日(或非空但格式非法)的合约一律丢弃,**绝不拿取数日顶替** ——
  `period` 是结构化字段、裁决点直接当"下一个数据点"用,填成今天就是造了个日期,
  note 里写一句"结算日不详"救不回来。
- **缺字段 ≠ 真实零**:上游改名导致成交量字段没了,不能写成"24h 成交量为 0"当事实。
- **静默截断要出声**:广度翻满页仍有 cursor、单系列 / 单标签满页、发现阶段满额或匹配超上限,
  全部出 warning 并熄灭 `complete`。原注释写"截断出声由调用方计数",而**调用方根本没数**。
- **`extra` 只能列已生成证据的条目**:否则留下"有元数据、无证据"的孤儿记录,
  下游按 record_key 枚举会拿到不存在的合约。

产出红线照旧:只报"市场给出的概率为 X%(截至 as_of)",**不得写成会发生 / 将发生 / 预计**;
护栏句与数字同段;24h 成交量为 0 的条目要么不引、要么就地注明参考价值很低。

新增 `tests/test_probability.py`(**这一层原本零测试**),222 条 Python 测试全绿。

## 1.0.0 开发记录 · Core 能力层:检索 + 浏览器(CDP)(2026-08-24)

按垂类 AgentOS 方向(Core + DomainPack),把**搜索**与**操作浏览器**这两项"任何行业都要用"的能力
放进 Core:`.agents/skills/data-access/scripts/core/{retrieval,cdp,exa_client}.py`。
零配置可用(Exa 免 key MCP + r.jina.ai),检测到 key 才升级;CDP 是**可选增强**,零新增依赖。

**Codex 审计闭环:CDP 六轮(r1→r6 通过)· 检索层五轮(r1→r8 通过),共处置 40+ 条,其中 14 条 P1。**
Codex 抓到的问题里,有 **4 条是我在修复上一轮时自己引入的** —— 记在这里,因为它们是同一类错误的样本:

- **声称与代码不符**(最要命的一类):模块文档写「只连本机」,代码只查了 `ws://` 前缀不查 host;
  我自己的测试还断言了这一点 —— **测试给了假信心**。同类:`VRA_CDP_HOST` 可配任意主机;
  `ws://127.0.0.1/devtools`(不带端口)让 `if p.port and ...` 整条跳过,去连了 80 端口。
  ⇒ 现在的规矩:**要么改代码,要么改声称**,不允许两者不一致。
- **修复本身开的洞**:fake-IP 跳过写成「**任意一条**结果是 fake-IP 就跳过」,
  攻击者同时返回 `198.18.0.10` 与 `127.0.0.1` 即可绕过全部检查(应为「**全部**都是才跳过」)。
- **语义被合并**:`_clean_url` 同时做「规范化」与「记账脱敏」,而我用它去做**发送目标** ⇒
  预签名 URL 的 `X-Amz-Signature` 在真正发出去之前就被改成 `***`,请求必然 403。
  已拆成 `_request_url`(发送用,query 完整)/ `_record_url`(记账用,敏感值抹掉)/ `_safe_url`(报错用)。
- **校验放在副作用之后**:`fetch_page` 原本先把原始 URL 交给通道、之后才清洗 ⇒
  `_jina` 会把 `https://user:pass@host/...` 整个拼给 r.jina.ai,**凭据已经发出去了**,返回值再剥也撤不回。

**本机环境测出了 Jill 机器测不出的东西**(与之前"中文路径"那次同类):`example.com` 在本机解析到
`198.18.0.98` —— 走 fake-IP 代理(Clash 类)。若照「解析到私有段就拒绝」办,**所有网页访问会被一律拦死**,
而用这类代理的用户正是主要受众;更关键的是此时本地解析结果与 Chrome 实连无关,**这个检查没有信息量**。
⇒ 识别到 fake-IP 网段就**跳过**,不假装做了检查。

**三条被采纳的设计判断**(都改变了原方案):
1. **拦截页:默认失败 + 显式 opt-in**,而不是「只打标不拒绝」。理由(Codex 提出,我接受):
   打标会把安全依赖分散给每个调用方 —— 下游 mapper 忘了看那个字段,就会落盘一份
   「有 URL、有 raw、有校验和」、内容却是登录页的证据;而**校验和只能证明拦截页没被改过**,
   证明不了它是目标内容。
2. **脱敏分两档**:我们自己配置的 key 按**真实值精确替换**(手里就有那串值,不受写法影响);
   第三方凭据只能模式匹配 ⇒ **文档不再承诺「绝不回显」**(正则 denylist 做不到这个保证)。
3. **通道报不出最终 URL 时,把「不知道」标出来**(`final_url_known: false`),
   而不是猜一个出处 —— 与产品里「未接入 ≠ 零」「预估」是同一套做法。

**修掉的一条真回归**:`_exa_free()` 把上游给的 `raw_ref` 在映射时丢了 ——
等于在 Core 这一层就剪断了「每个数字追到 raw 响应」这条链,下游再也追不回。

**其余要点**:手写 WebSocket 按 RFC 6455 处理 FIN / opcode / 分片 / ping-pong(页面正文是大消息,
**一定会分片**);掩码帧按 RFC **拒绝**而不是"容错解"(掩码键不计入长度,容错解会少读 4 字节、此后整条连接错帧);
所有超时收口到 `time.monotonic()` 绝对截止并**贯穿每一次 recv/send**(慢速滴流能让"每次都不超时"而整体无限拖);
HTTP 读取用 `read1()`(`read(n)` 在 http.client 内部会循环凑满,deadline 打不断);
导航判定要求 `complete` 或 URL 连续两次相同,且 **href 与 innerText 同一次 evaluate 原子取回**
(否则页面能在两次调用之间跳去内网,把内网正文挂到公网 URL 名下)。
已知限制照实写进文档:`getaddrinfo()` 无绝对超时;六重以上编码的分隔符仍可绕过脱敏检测。

测试:Python **181**(检索 / CDP 新增 60+,含端到端假 CDP 的跳转错配、导航失败、about:blank 陈旧态)·
TS 221 · calc 188 · doctor 15 ok。

## 1.0.0 开发记录 · 上游对账单(数据层移植版本标注精确化)(2026-08-24 晚)

- **背景**:产品数据层是从三个上游仓库**移植代码**(不是依赖引用)—— a-stock-data(CN 79 端点)、
  global-stock-data(US/HK 36 端点)、investment-news(RSS 源清单),合计构成 115 端点里的绝大部分。
  移植的代价是**上游更新不会自动流过来**,而文档里原本只写「V3.7 / V2.0」这种不精确的标注,对账会误判。
- **新增 [datasources/UPSTREAM.md](datasources/UPSTREAM.md)**:记录每个上游的**移植 tag、上次核对日期、
  上游当时最新版本、以及逐条判断结论**;并给出再对账的命令与判据。
- **2026-08-24 首次对账结论**:
  - **global-stock-data**:实际移植自 **v2.0.3**(文档原写 V2.0)。该版三项数据正确性修复
    (腾讯美股/港股行情**每次调用必崩**、EDGAR Frames 时点概念缺 `I` 后缀一直 404、CBOE 调整后合约根被漏)
    的**八项字段修正逐条核对,产品全部具备**。
  - **a-stock-data**:移植自 **v3.7.0**;上游 v3.7.1 修的 `get_prefix()` 后缀路由 bug
    (`em_secid("000016.SH")` 静默返回 \*ST康佳A 而非上证50)**在本产品架构上不适用** ——
    产品是 `norm_ticker()` 归一化后再用 `(digits, market)` 拼 secid,没有"拿原串去猜"这一步,
    且解析失败抛 `ValueError` 绝不猜。已实测 7 种写法 + 3 种矛盾写法。
  - **investment-news**:只移植了 `sources.json` 源清单(与上游**逐字节一致**),抓取逻辑是产品自有的
    `sources/rss.py` → 上游 v1.0.1–v1.0.3 的抓取端修复不适用,需要跟进的只有清单增删。
- 发布清单新增第 **6.5** 步(发布前对一次上游账)。
- 🔴 **判据写死在文档里**:**「这个修复在本产品的代码路径上会不会发生」,不是「版本号是否落后」** ——
  上游 bug 可能因移植时结构不同而根本不存在,也可能某个看似无关的小版本恰好改了产品直接照抄的那段。
  **必须读 release notes 逐条对,不能只比版本号。**

## 1.0.0 开发记录 · 硬测试数据夹具(验一个新层从 17 分压到 8 分)(2026-08-24 傍晚)

- **动机**:硬测试 = 一次真实六阶段研究 + 纯函数判定,验一个新层要 17–19 分钟。实测阶段耗时:
  profile 109s / financials 127s / estimates 111s / valuation 134s = **8.0 分(47%)**;risk 274s / report 261s = 8.9 分。
  前四阶段纯粹"把数据准备好",每次几乎一样。
  ⚠️ **另一条路已实测无效**:降 reasoning 档位省不下来 —— 推理 token 只有 185–625,
  时间几乎全花在输出 token 生成上(约 35 tok/s)。
- **新增 `orchestrator/src/fixture.ts` + `--make-fixture` / `--fixture`**:把一次真实运行的前四阶段产物
  (`fetch/ raw/ calcs/ stages/`)快照成夹具,后续硬测试播种它并只跑 risk / report。
  **实测 8.1–8.3 分(全量 16.9 分),判定 11/11** —— 与全量运行**同一结论**;
  同一判定器对一份已知有缺陷的旧报告仍判 9/11 失败,**提速没有削弱判据**。
- 🔴 **三条不能破的**:① **不缩短提示词压力**(夹具原样保留前四阶段产物 —— "提示词长导致丢章节"
  正是刚修过的真 bug,为提速精简 report 输入就把要测的现象测没了);② **播种运行按测试运行隔离**
  (`test_scenario=true` + `seeded_from`,不进知识层 / 温度计历史);③ **陈旧夹具默认拒绝**
  (财务 / 估值逐日变化,跨日复用会让硬测试**因错误的原因通过或失败**,那比慢更糟)。
- **账本**:被跳过的阶段没机会写内存账本 → 播种后用既有的受限通道 `loadLedgerFromDisk` 回灌。
  正式(非播种)运行**没有新增任何读盘账本的路径**,"只信内存账本"这条不变量未被削弱。
- **Codex 审计 fixture-r1(2P1/4P2/1P3)→ r2(1P1/3P2/1P3)→ r3(3P1/3P2)→ r4(2P1)→ r5「闭环」,
  16 条全部核实成立、全部采纳**。其中三条是我自己写错或改了一半的:
  - **完整性声称过头**:`tree_sha256` 与文件哈希**同在一个可改的清单里**,能改文件的人也能改清单。
    已把注释改成诚实的:只能检出**意外损坏与漂移**,**不能证明数据没被伪造**(并写明本项目没有这个威胁模型的理由)。
  - **`data_day` 取的是建夹具的时刻,不是来源运行的数据日** → 今天拿三天前的运行建夹具,新鲜度检查形同虚设。
    改为读来源 `manifest.started_at`;身份三项(symbol / market / run_id)也必须与来源一致 —— 否则夹具能谎报它是谁的数据。
  - **只修了一半**:包含检查只给 `seedFrom/runDir` 加了、没给 `createFixture` 的 `outDir/runDir`
    (`createFixture(runDir, runDir)` 会先删掉来源运行);"空值等于空值"只修了 calc、没修 registry / endpoint。
  其余:路径越界(`../../x` 可播种到运行目录外)· 符号链接(文件与目录两级)· verify 与 copy 之间的 TOCTOU
  (改为**复制后按清单重新核验落地内容**)· 播种失败留半成品 · 隔离标记落盘太晚 · 检查顺序。
- ⚠️ **一个「照字面实现会误伤」的教训**:Codex 建议"拒绝根路径上任何符号链接",而 macOS 的 `/var`
  本身就是指向 `/private/var` 的链接、`os.tmpdir()` 就在它下面 —— 照字面写把**正常系统路径全拒了**(本机测试当场抓到)。
  正确语义是**先 realpath 根**(容忍良性祖先链接)再逐层拒绝内部链接、最后复核没越出解析后的根。
- **判定侧**:新增统一的 `runCompleted()` —— 播种运行按设计是 `incomplete`/2(确实跳过了阶段),
  但要求**本次实际执行的每个阶段都 complete**且必须带隔离标记;退出码是否放宽以 `manifest.seeded_from` 为准,
  **不看命令行传没传 `--fixture`**。
- ⚠️ **夹具运行不能替代发布前那次完整运行**(release-checklist 第 3 步不变)。
- 测试:TS **211**(`fixture.test.ts` 14 项)。首次实测还抓到我自己漏改的一处:manifest 的 **JSON schema**
  不认新字段 `seeded_from` → 两个阶段都 complete 却整次运行被判 failed(validator 忠实拦住了半吊子改动)。

## 1.0.0 开发记录 · 运行进度显示(第一分钟就有产出)(2026-08-24 傍晚)

- **问题**:六阶段研究要跑 15–19 分钟,而 `run.ts` 全程只打印一行配置 + 六行 `stage=X attempt=1/3`,
  最后甩一个 JSON —— **用户十几分钟看不到任何内容**,无从判断它在干什么、有没有卡住。
  但数据**早就在流**:ht27/ht28 实测,取数在 +1~5 秒就成批落地,`stages/profile.json` 的 `summary`
  在 +1 分 49 秒就已是一段可读的中文公司画像。⇒ **不是架构问题,是显示问题。**
- **新增 `orchestrator/src/progress.ts`**:订阅既有事件流(`CodexRunner.log` 落盘后旁路),
  把阶段进度与各阶段 summary 渲染到 **stderr**。`--progress on|off`,默认 on。
  ht28 真实事件回放:**+5 秒**出现第一行实质内容(本阶段取了哪几个源、哪个失败),
  **+1 分 49 秒**出现完整公司画像段落,此后每 2 分钟一段;校验未过会说明"自动补跑"而不是让用户以为卡住。
- **三条红线**:① 绝不影响运行(事件先落盘再旁路,观察者异常全吞,连 reporter 构造也包 try/catch);
  ② 绝不碰 stdout(批处理 / MCP / HTTP API 的 JSON 契约一字不动);③ 绝不泄露路径与密钥。
- **Codex 审计 progress-r1 → r11「闭环」,共 13 条,全部核实成立、全部采纳**。其中安全相关的:
  - **终端注入**:summary 是 agent 写的不可信文本,内含 ANSI/OSC 可清屏、改窗口标题、**伪造出几行假进度**;
    双向控制字符能让一行的视觉顺序与实际内容相反 → `sanitizeTerm()` 剥 OSC / CSI / C0 / DEL / C1 / 双向控制,
    **所有**动态文本统一走 `clip()`(净化 → 脱敏 → 折叠 → 按字符截断)。
  - **路径穿越**:`runner.ts` 构造事件时 `...payload` 在固定字段之后 → **payload 能覆盖 `stage`**;
    再加符号链接(文件与**目录**两级)与 TOCTOU → 白名单阶段名 + `O_NOFOLLOW|O_NONBLOCK` 打开后 `fstat` 描述符。
    🔴 `O_NONBLOCK` 不可省:**对 FIFO 来说连 `openSync` 本身都会阻塞**,而阻塞是 try/catch 救不了的 ——
    我第一版把 `isFile()` 放在 open 之后,测试当场挂死,正好证明了这条。
  - **路径脱敏**:事件字段不取路径 ≠ 正文里没有路径(summary / validator 报错 / 源脚本名都是自由文本)。
    正则打了四轮补丁仍在"泄露"与"过度吞掉中文"之间反复翻车,最后换成**扫描器**,判据写成一条能讲清楚的规则 ——
    **遇到全角标点时,只有它后面还会出现路径分隔符,才认为路径还在继续**。17 个用例双向实测全对。
    ⚠️ 其中最有价值的一条是全角标点边界:**本产品报告全是中文,全角冒号才是常态**,
    我上一版的测试恰好用了半角冒号所以没测出来 —— 只认半角等于没脱敏。
  - **计数正确性**:取数批次改为**按阶段各存一份**(单一计数器会让没有取数的 report 阶段播报上一阶段的数字,
    ht28 实测就是如此;同阶段补跑还会两轮合并);失败数与明细分开(只留 20 个名字但总数要准)。
- 测试:TS **197**(`progress.test.ts` 19 项,含终端注入、路径穿越、符号链接文件与目录、FIFO 不阻塞、
  全角/半角/带空格带逗号带括号的路径、以及"硅光/光互连""2026/08/24""N/A""1.6T"等**不得误伤**的用例)。

## 1.0.0 开发记录 · 扩展章节纪律落到编排层与校验层(2026-08-24 下午)

- **事故**:报告的"扩展章节"纪律原来**只活在提示词里的一句条件句**,而产品宪法写的是「提示遵循 ≠ 流程保证」——
  15 次真实运行显示,report 提示词从 18.9K 涨到 23K 的过程中,报告章节数从 11–12 掉到 7–10;
  **ht27 的 risk 交出 12 个 topic,其中 2 个的证据在报告里一条都没引**(资金行为 3 条 / 其他线索 4 条),
  另有 3 个被降级成 `###` 子节、3 个被合并进一个叫「市场、头条与招聘线索」的含糊子节 ——
  而 validator 判 `complete`、退出码 0,**用户完全看不出少了东西**。
  ⚠️ 曾以为是 `SIGTERM` 打断所致,**已证伪**:ht27 SIGTERM=0 只有 7 章节,ht26 SIGTERM=16 反而 10 章节。
- **新增 `orchestrator/src/report_sections.ts`**:risk 阶段写了哪些 topic、各绑哪些证据是**已落盘的事实**,
  据此确定性算出要求 → ① 注入报告提示词一份**点名清单**(而不是让 agent 从 1.8K 条件句里自己挑)
  ② `validateReport` **强制校验**,缺了走既有补跑循环(补跑提示词会逐条列出缺什么)。
- **要求只有两条,绝不涉及结论**:有专属章节的 topic → 章节标题在场 + **每个 topic 各自**至少引一条自己的证据(引用须落在该节正文内);
  无专属章节 / **未知** topic(数据日历 / 其他线索 / 将来新增却忘了登记的)→ **fail-safe** 退化成"证据须在全文被引用"。
  章节内可如实写"数据不足",**宁可写没有,不可以编**。
- **Codex 审计 sections-r1(2 P1 / 6 P2)→ r2(1 P1 / 2 P2)→ r3(1 P1 / 1 P2)→ r4「闭环」**,10 条全部核实成立、全部采纳:
  ① 数据日历 / 其他线索 / 未知 topic 绕过校验(**正是事故里丢的那个**)→ fail-safe 全文要求;
  ② 同章多 topic 证据取并集 → 「资金行为」满足了「解禁」全丢也放行 → 逐 topic 校验;
  ③ 合并标题「市场声音、海外头条与招聘信号」可一次满足三个要求 → 标题须**不含其它要求章节名**;
  ④ 按层级切块会把后面 `### 招聘信号` 的 id 算进空壳 `## 市场声音` → 遇到别的要求章节标题即结束;
  ⑤ `body.includes(id)` 非整 token → `citedIds` 加标识符边界(`ev-abc123xyz` 不再满足 `ev-abc123`);
  ⑥ 空串 id 让 `includes("")` 恒真 → 过滤空白 id;
  ⑦ 不认 CommonMark 的 0–3 个前导空格标题 → 修正正则;
  ⑧ **提示词对无有效证据的 topic 仍写"至少引 1 条(如 无)"= 无法满足的指令、会诱导编造**(核心红线)→ 改为"不得编造,只如实写明证据缺失",
     末尾自查句一并改口径(r3 抓到我只改了逐条、漏了汇总句);
  ⑨ 围栏代码块内的 `## 标题` 被当真章节 → `parseHeadings` 维护 fence 状态,两个函数共用避免规则漂移;
  ⑩ CommonMark 只有**反引号**围栏禁止 info string 含反引号,波浪线可含 `~` → 否则 `~~~text a~b` 会连锁吞掉后文真章节。
  **驳回 1 条并记依据**:r1 称"提示词读磁盘 risk.json、validator 读 run.stage('risk') 两来源可能不一致" ——
  实测 `loadRun` 里 `stage: (s) => readJsonIfExists(path.join(runDir,"stages",`${s}.json`))`,**同一个文件**;r2 未再提出。
- ✅ **ht28 真实运行验证通过**:同一标的、同样六阶段,报告从 ht27 的 **7 个章节**变成 **14 个**,7 个扩展章节全部写出 —— **包括 ht27 里整章丢失的「资金与市场行为」**;招聘信号判定 **11/11**(报告引 12 个招聘 id、10 个数字全部绑定)。**回归验证**:同一判定器对 ht27 的旧报告仍判失败(9/11),说明约束没有被放松成摆设。
- 🔧 **顺带修掉一个既有假绿**:`judgeHiring` 的检查名声称查「不跨公司比大小」,代码里**根本没有对应的正则** ——从来没查过。补上后按风险分两级:**逐段**必须标明「不是本公司」(这条被单独引用时最容易被当成本公司经营事实,必须贴着数字);**章节级**须写明「招聘意图不是产能」与「不跨公司比」(整组数字的解释框架,要求 6 个锚点每行各重复一遍会把章节写成噪音、风险却没下降)。ht28 的真实产出正是这个形态。
- 测试:TS **179**(`report_sections.test.ts` 15 项,含围栏代码块双向、id 边界、空壳章节、逐 topic 丢失、
  未知 topic fail-safe、提示词不得产生无法满足的指令);Python 源层 + calc **267**;doctor 15 ok · 0 fail。

## 1.0.0 开发记录 · 第 17 层「招聘信号」(2026-08-24 下午,看板信息源移植第 7 项 / 收官)

- **定位**:产业**锚点公司**(上下游 / 需求侧,**不是本公司**)的公开在招岗位 —— 招聘往往比财报早几个月暴露战略(在哪扩、招什么方向、招到哪个阶段:出现产线 / 量产 / 质检岗 ≈ 离量产更近)。
- **端点 `hiring_anchor_signal`**(risk optional,`industry_tags: [ai_compute, storage_memory]`,`pass_out_dir`):读官方 ATS 公开 job board —— Greenhouse `boards-api` + Ashby `posting-api`(零鉴权、只读岗位不碰简历)。⛔ **Workday 系不接入**(NVIDIA / Coherent / Lumentum / Marvell / Micron 需 host+tenant+site 三段配置,非零配置),缺就如实写"未接入"、不拿别的公司顶替。
- 证据:每家一条 `hiring_open_roles` + 每个**非零**角色桶一条 `hiring_role_bucket`(**制造 / 运营** · **光与互连** · **封装与硬件** · **算力与基础设施** 四类"相关标题命中";**按岗位计数**、标题关键词词边界匹配。🔴 桶名刻意只说"标题命中了哪类词"、不说"公司在做该事" —— 叫"量产制造"会被读成量产信号,但 manufacturing / test engineer 在研究型公司未必是量产;"量产临近"需产线 / 良率 / 夜班 / 质检等组合证据,不能由标题词推断)。全市场口径 `symbol=MARKET` / `record_key=ats:slug`;`history_fields=["hiring_open_roles"]` 让温度计历史序列下次给跨运行 delta。
- 🔴 三条护栏(进 note + 判定逐段查):① **岗位数是招聘意图不是产能**、受招聘节奏与 HR 批次影响、单点意义有限**看变化**;② **锚点公司不是本公司**,不得写成本公司经营事实;③ 不同 ATS 口径不同、**只在同一家公司内部比较不跨公司比大小**。另有 **"未接入 ≠ 零岗位"**(标签没配锚点 / 未经门控 → partial + degraded 写明,报告不得读成"没人在招")。
- 锚点(ai_compute,零鉴权实测可用):Lightmatter(硅光 / 光互连,69)· Astera Labs(互连芯片,160)· Cerebras(算力芯片,123)· OpenAI(753)· xAI(261)· Anthropic(515)。`storage_memory` **刻意留空**(Micron / SK Hynix 都是 Workday)→ 如实"未接入"。
- 产出:topic「招聘信号」+ 报告可选章节 + SOP `catalyst-risk §5.7` + `company-research` 表;硬测试第 14 组 `judgeHiring`。**115 端点 / 29 层**。
- **Codex 审计 hiring-r2(9)→ r3(4)→ r4(1)→ r5(1)→ r6「无实质问题、总体判断:闭环」**,五轮全部逐条核实。真 bug 三条:① 我自称"先落盘再解析"**名不副实** —— `_get()` 内部就 `json.loads`,JSON 坏掉时**那份最该留证的响应根本到不了 `record_raw`**,改成 `_get` 只回字节、调用方先落盘再解析;② `record_key_for` 只保证**基础键**不超 32 字符,追加桶后缀(`:optical` 8 字符)照样越界 → **给后缀预留预算**再判断是否哈希;③ Workday 等被跳过的锚点不计入 `anchors_expected`,覆盖率显示 1/1 把配置缺失掩盖成"全取到了"。收紧两条:200 但零岗位分不清"真没在招"与"slug 已废弃" → 标 `zero_unverified` 且**判定禁止据此推"招聘冻结"**;无锚点 / 未门控一律 `partial` 而非 `failed`(只有真实抓取全失败才抛 `HiringError`),两类状态明确分离。**驳回 1 条误报并记依据**:Codex 称 `market="US"` 违反证据契约 —— 查 schema 枚举与 validator `regionCode` 均含 `US`,不采纳。
- 测试:Python 源层 **72**(`test_hiring.py` 5 项:锚点读取三种缺失路径 / 两种 ATS 解析与角色桶(按岗位计数)/ 契约错误必抛 / 逐家隔离 + mapper 口径 / **未接入 ≠ 零岗位**);TS **164**(judge 反例 5 组:缺护栏 / 反向"等于产能" / 数字未绑定 / id 漏进事实章节 / 把未接入读成零岗位)。

## 1.0.0 开发记录 · 第 16 层「海外头条」(2026-08-24 上午,看板信息源移植第 6 项)

- **定位**:Techmeme river 时间流最近 48 小时的英文科技头条 = **需求侧一手线索**(英伟达 / 云厂 capex / OpenAI 动态对 AI 算力链标的是先行信号)。零鉴权。
- **端点 `techmeme_headlines`**(risk optional,`industry_tags: [ai_compute, storage_memory]`):扒 `<tr class="ritem">`(美东 H:MM AM/PM + `pml="YYMMDD"` → 北京时间;`<cite>` 刊名、标题锚点在其后),丢弃重复 url / 标题 < 25 字 / 无时间戳 / 假日历日;river 解析 0 条 = 结构变了 → **回退 RSS 但必须出声**(只有约 15 条,窗口不完整);两个入口都不行才抛。
- ⭐ **相关性按产业标签,不硬编码任何个人关键词表**:从运行目录 `fetch/_industry.json`(编排器门控结果)拿命中标签 → 从 `industry_tags.json` 读该标签的 `headline_keywords`。关键词为空 → `relevant=None`(**未标注 unscored ≠ 不相关**)并出声,绝不退回"全都相关"。为此新增 `pass_out_dir` 端点开关(`fetch_endpoint.py` 据此把 `--out-dir` 传给源函数,只读编排器自己的确定性产物)。
- 🔴 **与第 12 层市场声音同一纪律**:标题经 `textsafe.neutralize_actions` 脱敏;证据 note 带 `published / source / relevance / link / untrusted_text=sanitized`;链接只进 note(报告附录),**正文不贴链接、行内不写数字**(标题里的数字不是事实);只挑「命中」条目写,命中 0 条就写"窗口内无命中",不为凑数引未命中条目。
- 产出:topic「海外头条」+ 报告可选章节(带"与本报告的关系"与"非事实"声明)+ SOP `catalyst-risk §5.6` + `company-research` 表;硬测试第 13 组 `judgeHeadlines`。114 端点 / 28 层。
- 实连(300308 命中 ai_compute):48h 窗口 30 条、7 条命中关键词。
- **Codex 审计 headlines-r1(3 P1 / 4 P2 / 1 P3)→ 8 条全部成立、全部采纳**:
  - 🔴 **解析静默丢条**(多 class / 属性换序 / cite 带属性 / href 非首属性都会丢行,且只有"整页 0 条"才降级)→ 正则放宽 + 缺 cite 时取**文本最长**锚点 + `MIN_TITLE_LEN` 25→12 + **逐类丢条计数**(bad_time / bad_pml / no_anchor / short_title / duplicate…),接受率 < 50% 出声。
  - 🔴 **判定几乎全可空跑**(有命中却没章节也能过、risk 可引未命中 id、"线索关系"四字同时满足关系句与声明)→ 有命中必须有章节 + 章节与 risk 都引命中 id;引用的头条 id **全部**须命中;零命中必须留痕;**关系句必须引用非头条的 ev/calc id**,免责声明单独精确校验。
  - 🔴 **合规行被误杀**:`claimTokens` 剥日期但不剥 `HH:MM`,"2026-08-24 20:45(北京)"里的 45 被判成数字 → 时刻不算数字主张(通用规则)。
  - 截断丢命中:相关计数按**完整窗口**算、截断时**命中优先保留**、有命中被截 → partial;窗口加**上界**(now + 2h)丢弃错时 / 伪造条目;DST 歧义小时按 fold=0 但计数出声并在证据 note 标 `dst_ambiguous=true`;**RSS 的 pubDate 无时区一律当无时间戳丢弃**(否则按本机时区静默错时);`scored` 标志 → 无关键词 / 配置漂移时降 partial 并写明"unscored ≠ 不相关";🔴 **关键词过宽**("OpenAI changes ChatGPT privacy settings" 会被判成算力线索)→ 拆强词 / 泛词 / 锚词三层,**泛词必须与锚词共现**。
- **Codex 审计 headlines-r2(4 P1 / 3 P2)→ 7 条全部成立、全部采纳**:
  - 🔴 缺 `<cite>` 时"取最长锚点"会选到 "Read discussion and commentary…" 这类评论链接 → **直接丢该行并计数**,不靠长度猜标题。
  - 🔴 解析接受率低只进 warnings 不影响状态(候选 100 行只解析出 40 行仍 `ok`)→ 接受率进 `extra` 并低于 80% 时降 **partial**。
  - 🔴 只配泛词没配锚词时 `scored=True` 却永远零命中(被表示成"已评分且零命中")→ `scored` 要求"有强词,或泛词与锚词**同组**都在"。
  - 🔴 判定的"零命中留痕"可空过(没章节直接放行;空 `evidence_ids` 的 findings 也算)→ 必须**引用计数证据**且写明零命中措辞。
  - 关键词跨标签配对(A 的泛词配上 B 的锚词)→ 关键词**按标签分组**,只允许同组共现;泛词命中但无锚词 → 记为**候选**(`context_only`,不算相关但留痕,真线索不会无声消失);`HH:MM` 无条件剥会漏掉"配比 35:65" → 只剥"**日期 + 时刻**"这个整体,裸比值保留。
- **Codex 审计 headlines-r3(1 P2)→ 采纳**:截断只优先保留正式命中,`context_only` 候选与普通未命中混在一起会被挤掉、只剩汇总计数 → 保留顺序改为 **命中 > 候选 > 其它**,候选被截时 partial 并 degraded 出声(`context_only_dropped`)。r3 对另外四项交叉质询(候选与"只引命中"的关系、接受率阈值是否天天 partial、零命中要求引计数证据与"行内不写数字"是否冲突、分组后是否仍有代码依赖扁平关键词)均判**无实质问题**。
- **Codex 审计 headlines-r4 → 无实质问题、闭环**。四轮 8 → 7 → 1 → 0。
- ✅ **ht26 补跑通过:1/1(12 项全过)**,报告写出了「## 海外头条」章节 —— *"2026-08-24 北京时间 · The Information · 讨论 Nvidia 的外部投资与技术合作 [ev-id]…以上头条仅为需求侧印证或反证线索,与前瞻低于 TTM 的减速判读 [calc-id] 相关;标题不是事实"* —— 行内无数字、不贴链接、关系句引了非头条的 calc id、带非事实声明,全部符合。**此前的未验证项已消除**(下面这段保留作为坑的记录)。
- ⚠️ **(已消除)曾经的未验证项**:报告是否会写「## 海外头条」章节 —— 三次真实运行(ht23 / ht24 / ht25)都被 `Codex Exec exited with signal SIGTERM: Reading prompt from stdin` 反复打断(18 / 18 / 22 次),agent 每次产出"精简版"报告、**所有可选章节一起消失**。相关性干净到足以排除提示词问题:SIGTERM=0 的 ht20 / ht21 报告写满 **12** 个章节(市场声音 / 产业温度计 / 管制与准入 / 数据日历全在),SIGTERM=18–22 的只剩 7–8 个。已验证的部分:端点实连(接受率 100% / 命中 5–6 / 候选 3 / 脱敏与链接分离)、**risk 阶段写了 topic「海外头条」并引 4 个命中 id**(ht24)。根因未定,已排除 Bash 工具 600 s 超时、内存(59% 空闲)、stdin 重定向、进程组分离;现象是引擎启动握手不稳、每阶段重试 2–3 次。环境好转后补跑一次全量即可 —— **ht26 已补跑并通过**。
  - 顺带:试过"只重跑 report 阶段复用已有数据"省事,**被产品自己的不变量挡住**(拒绝复用非空运行目录,防混入旧证据)——拦截是对的,没绕过。
- 测试:Python 源层 **67**(`test_headlines.py` 6 项:river 解析与时区换算 / 窗口裁剪 + 关键词标注 / **无关键词 = 未标注而非不相关** / RSS 兜底必须出声(含 river 解析 0 条也算失败)/ 两入口都坏才抛 / 窗口零条是 partial 不是故障)。

## 1.0.0 开发记录 · 大宗温度计(2026-08-24 上午,看板信息源移植第 3 项)

- **定位**:第 13 层产业温度计的**原材料维度**——研究标的上游的原料价格,按产业标签挂载。同时新增第二个产业标签,验证标签体系的可扩展性。
- **端点 `cn_commodity_futures`(tag ai_compute)**:akshare `futures_zh_daily_sina` 取沪铜 / 沪锡 / 沪铝 / 沪镍 / 工业硅连续合约日线 → 每品种 收盘 + 1 日 / 约 7 日 / 约 30 日涨跌。**大宗价格是全市场证据 → symbol=MARKET、品种放 record_key**(证据契约:market=CN 时 symbol 必须是 MARKET)。DataFrame 非传输层原文 → `record_raw(kind="extracted")`。
- **端点 `dram_spot_thermo`(tag storage_memory,新标签)**:GitHub 社区仓转录的 DRAMeXchange 现货(DDR5 / NAND TLC / DDR4)。两仓 schema 不同(日期 `8/21/2026` vs `2026-08-24`、价格 `"$54.100"` vs `91.073`、ddr5 无 product 字段 → 规格如实写"未在数据中标明")。
- **护栏**(进证据 note + 标签 guard + 报告判定逐段检查):期货价 = 全市场定价**不是本公司采购价**、传导有季度级滞后并被长约与套保平滑、工业硅需求由光伏主导对半导体是弱信号;DRAM = **社区转录非官方一手** + **HBM 的影子指标** + **不是 HBM 价格**(HBM 走年度长约无公开现货价)。
- ⭐ **标签隔离生效**:中际旭创(光模块)只命中 ai_compute → 拿期货、**不拿 DRAM**;存储股才挂 DRAM;两者都命中时端点取并集。`storage_memory` **刻意不给 weak_keywords / sw_prefixes**——"半导体 / 集成电路" + 申万大类前缀会把纯设备、纯材料、模拟芯片公司也挂上 DRAM(Codex commodity-r1)。
- 实连:沪铜 107520 元/吨(约 30 日 +1.61%)、DDR5 $54.1(约 30 日 +7.48%)、DDR4 $91.073。
- **Codex 审计 commodity-r1(5 P1 / 2 P2)→ 7 条全部成立、全部采纳**(另有 1 次预检失败作废:我给的 grep 用端点 id 去搜 hardtest.ts,而那里出现的是**证据字段前缀** —— Codex 按规矩停下报错,提示词已修):
  - 🔴 判定写死 tw / gpu,新增端点即使取了数也能被整段省略 → 按 `_industry.json.thermometers` **动态**要求每个已挂载且取到数的端点都在报告章节被引用。
  - 🔴 期货护栏只写"全市场定价"就算过,反向语漏了"也是本公司成本 / 按此价采购" → 改成**两个事实都要写**(市场定价 + 不是本公司采购价/成本),反向语扩充。
  - 🔴 DRAM 护栏把"存档"当来源限定("**官方存档**"能骗过),且"影子指标"与"不是 HBM 价格"是 OR → 改成**三个事实都要写**,并把"官方一手"列入反向语。
  - 🔴 `storage_memory` 标签过宽会把设备 / 材料 / 模拟芯片公司挂上 DRAM → 去掉宽泛 weak + 大类前缀,必须有存储专属信号。
  - 🔴 `_back()` 会把相隔 27 天的两点标成"约 7 日涨跌" → 回看返回**基点日期与实际跨度**,超过窗口 2 倍容差**不生成该指标**并记进 `skipped_windows`;生成的涨跌证据 note 必须写"对比基点 YYYY-MM-DD(实际间隔 N 自然日)"。
  - 行级校验:未来日期 / 非有限数 / 非正数 / 无法解析**逐行丢弃并计数**(原来单个 `None` 会让整品种失败、`inf` 会进证据);期货 raw 只存 60 行却按全序列写 `n_points` / `span` → **raw 与统计同窗口**(120 点),保证每个数字都能从 raw_ref 复核。
- **ht21 真实运行 1/1(10 项)**:中际旭创只挂 ai_compute → 期货落 5 品种 × 收盘 + 涨跌证据,报告写"新浪期货 2026-08-21:沪铜、沪锡、沪铝、沪镍、工业硅连续合约收盘分别为 107520、429030、23710、129200、8790 元/吨;约 30 日涨跌 1.61%、2.89%、2.24%、−1.73%、6.55% [10 个 ev-id] …期货是全市场定价而非本公司采购价"。判定顺带修两处 `claimTokens`:① 时间窗口标签("约30日涨跌")不算数字主张;② 🔴 **6 位数原本一律当 A 股代码剥掉 —— 沪铜 107520 元/吨 恰好 6 位,等于这类价格从不参与数字绑定校验(既有假绿)**,改为"后跟单位就是真数字"。
- **Codex 审计 commodity-r2(3 P1 / 2 P2)→ 5 条全部成立、全部采纳**:🔴 倍数容差(2×)下 60 天变化仍会被叫"约 30 日" → 改**加法容差**(7 日最多认 10 天 / 30 日最多认 35 天);🔴 `ledOk` 先过滤失败端点 → "挂了、取数失败、报告一个字不提"会静默通过 → 拆成两个检查(成功端点要求报告引用、失败端点必须在 `risk.gaps` 出声);🔴 **`fieldPrefixOf` 字段前缀映射本身就是写死的** —— 未知端点静默豁免,等于"新增端点不可被省略"从没真正动态化 → 证据 id **直接从 `fetch/<endpoint>.json` 读**;6 位数单位白名单补数量单位;"约 N 日"只有后接窗口词才当标签(否则"回款周期约 30 天"会被剥)。**ht21 在最终判定下重判 1/1(11 项)。**
- **Codex 审计 commodity-r3(1 P1 / 3 P2)→ 3 条采纳 1 条部分采纳**:🔴 端点 ok 却零证据被豁免(mapper 回归会静默通过)→ `emptyMounted` 显式失败;`_back` 只往更早找会漏掉合格基点(周更序列 08-15 距今 6 天在 7±3 内却被排除、退到 13 天前再被跳过 → 该指标凭空消失)→ 改为**窗口内取距目标最近**的点;`skipped_windows` 结构化进信封 `extra`(**不采纳**"端点降 partial + 强制报告披露":窗口跳过时该指标证据根本不存在,数字绑定已杜绝凭空写,把健康取数降级会把"序列还短"误报成故障);🔴 6 位数单位白名单永远补不全(辆 / 平方米 / 千瓦)→ 改成**只在代码语境或等于本次标的代码时才剥**(`claimTokens(line, symbol)`),裸的非本标的 6 位数一律当真主张。
- **Codex 审计 commodity-r4 → 无实质问题、闭环**(四项质询全部澄清:`runSymbol` 由 manifest 稳定提供;`_back` 并列取更早基点符合既定规则;`emptyMounted` 符合"零证据必须 failed"的端点契约不会误杀;**"不降 partial"的理由被判成立** —— 缺某个回看窗口不等于取数故障,且数字绑定已阻止虚构涨跌)。四轮 7 → 5 → 4 → 0。
- **回归**:ht13 / ht16 / ht19 / ht20 / ht21 五组真实运行在最终判定下**全部 1/1**。
- 测试:Python 源层 **52**(`test_commodity.py` 7 项:窗口容差与基点诚实性 / 行级校验矩阵 / 双日期与价格格式 / 逐品种与逐源隔离 / 契约错误 / 陈旧边界 / mapper 口径;`test_industry.py` 的注册表回归改为**标签 ↔ 端点双向不变量**,并抓到"第 15 层 us_anchor_earnings 借用产业门控但不是温度计"这一设计事实,写成显式口径);TS **159**(标签隔离 / 并集 / 提示词护栏 + 护栏绕过 6 组反例 + 动态强制)。

## 1.0.0 开发记录 · 第 15 层「数据日历」(2026-08-24 上午,看板信息源移植第 5 项)

- **定位**:裁决点的"下一个数据点"带**具体日期**,不再是"8 月中中报"这类模糊窗口。三个来源、三种口径,绝不造日期:
  - 端点 `next_disclosure`(cn6,risk optional):东财 RPT_PUBLIC_BS_APPOIN 预约披露表 → 下一份定期报告的预约披露日 / "尚未预约" / 最近实际披露;**过了预约日仍未披露 = 延期信号**写进 note。契约:success≠true 或缺 result / data 字段一律抛;只有显式 `result=null` 或 `data=[]` 才算"真实无记录";逐行核对 SECURITY_CODE 宁可少收。
  - 端点 `us_anchor_earnings`(industry_tags=ai_compute,args tickers=NVDA):Nasdaq `analyst/{t}/earnings-date` → 财报日证据(market=US / record_key=ticker)。日期**锚定在 "report/announce … earnings … on <日期>" 句式**(不抓文案第一个日期)、真实日历校验、多个互斥日期拒绝猜;口径**三态**:expected/* = 预估、无标记 = "Nasdaq 页面日期,确认状态未核实"(不冒充公司确认)、确认以 IR 为准;盘后 note 标"美股当地交易日 ≈ 北京时间次日凌晨"。实连 NVDA 2026-08-26 盘后(预估)。
  - 编排器规则推算(`industry.ts` twNextDisclosure / industryNextDateLines,进【下一个数据点(规则推算)】提示词块):台股法定月营收每月 10 日前——**期限按"今天"推**(法定节律是日历驱动;按最新资料期推会把已过去的期限当"下一档"——Codex datacal-r1),资料滞后(中间月份缺失)同一行出声;Kalshi 合约月月末后结算,不给具体日。日期不参与数字绑定(claimTokens 剥日期),判定按同一规则复算。
- 产出:注册表 111 端点 / 27 层;risk ⑧ + topic「数据日历」(EXTRA_TOPICS)+ 报告「裁决点」要求日期带 [ev-id]、预估日标"预估"、规则日期注明"法定披露期限 / 规则推算";SOP `catalyst-risk §5.5` + `company-research` 表;硬测试第 12 组 `judgeNextDates`。
- **Codex 审计 datacal-r1(5 P1 / 4 P2)→ 8 条采纳、1 条部分采纳**(信封损坏时提示词块静默跳过:validateFetchIntegrity 对每个 fetch/*.json 做解析 + sha 校验,损坏必在阶段校验出声——注释写明,不重复报警)。采纳的关键修复:结构损坏不当"无记录";Nasdaq 抓第一个日期会把"财季止 07/31"当财报日 → 锚定句式 + 日历校验 + 歧义拒猜;无预估标记 ≠ 公司确认 → 三态口径;财季解析失败 period=n/a 且 degraded 出声(不再填抓取日);tw 期限按今天推 + lagging 出声;判定:日期白名单不再用全部证据 note 汇总(会洗白造日期)→ **按段落逐日期**要求本段引用 value/period 精确含该日期的证据 id 或= 规则推算期限且段内注明;"带日期"与"引 id"必须同一条 decision point;台系期限必须进**最终报告**;真实日期证据至少一个进报告;`latest_report_published_date` 只在有已披露行时要求(次新股空表不误杀)。
- **真实运行 ht20:1/1 通过、10/10 检查**(最严判定重判):裁决点 7 条里 2 条同条带日期 + 日历 id;NVDA 写"财报日约 2026-08-26、预估 [ev-id]";台系期限 2026-09-10 与复算一致并注明"法定披露期限 / 规则推算";半年报刚披露(08-22)与"Q3 尚未预约"都如实;estimates / policy 这类没有日期依据的老实写"无确定日期"。**顺带:温度计历史序列今天首次真实开火**——13 组比较 / 31 条历史证据(synthetic=null;tw 同期重取变动 0 如实标"上游未更新",GPU 现货有真实跨日变动)。
- **Codex 审计 datacal-r2(2 P2)→ 全采纳**:① 月报提前披露时"下一档"指向已拿到的数据月(9-05 手里已有 8 月数据仍说"8 月月报 9-10 前")→ 日历目标月与"最新资料期 + 1 月"取较晚者,期限随数据月推;② 过去的实际披露日能冒充"下一个数据点"(latest_report_published 2026-08-22 带 id 就能过)→ 判定只认**未来**的预约日 / 财报日证据:有则要求同条"日期 + 该证据 id"精确匹配,都没有(尚未预约)退化为引任意日历 id;realDateInReport 只认未来日期。
- **Codex 审计 datacal-r3(1 P2)→ 采纳**:降级分支(无未来日历证据)里**已过期的预约日**仍能冒充"下一个数据点"→ ① 判定"今天"改用运行当天(next_disclosure 信封 fetched_at;兼修"隔天重判把运行时的未来日期误判为过去"的漂移)② 新增显式检查:next_data_point 不得含早于运行日的日期(等于运行日的"截至今天"允许)③ 提示词写明历史日期放正文、预约日已过 = 写"延期中,以公司公告为准"。ht20 重判 11/11。
- **Codex 审计 datacal-r4(1 P2)→ 采纳**:过去日期检查只盯了 risk 的 decision_points,**报告**裁决点章节仍能把历史披露日写成下一时点 → 对称规则:章节段落里早于运行日的日期直接判失败(等于运行日的"截至今天"允许),report 提示词写明"本章不写早于今天的日期"。ht20 重判 1/1(11 项)。四轮 9 → 2 → 1 → 1 全部采纳、零误报,闭环。
- 测试:Python 源层 **45**(`test_datacal.py` 5 项:行解析 / 延期信号与空表三态 / 契约错误矩阵 / 文案锚定解析(错位日期 / 单数字月 / 全月名 / 假日历 / 歧义)/ 逐 ticker 隔离与三态口径);TS **157**(twNextDisclosure 今天驱动 + 滞后 + 跨年;industryNextDateLines 滞后出声)。

## 1.0.0 开发记录 · 温度计历史序列(2026-08-23 深夜,看板信息源移植第 4 项)

- **定位**:给第 13 层产业温度计加**时间维度**——报告能写"B200 现货中位 6.88,上次观测(08-16)7.50,变动 −8.27%",而不是每次只看一个点。不拉新数据:序列来自本产品自己的历史运行。
- **存储**:用户数据区 `.local/knowledge/thermometers/<端点>.json`(`schema_version 1`,观测 = run_id / run_date / as_of / fetched_at / record_key / field / value / unit / period / raw_ref / source)。归档与知识层同规矩:非 scenario、未 failed 的运行结束时,从**已校验的取数信封**按注册表 `history_fields` 白名单追加(同 run_id 幂等;上限 3000 条丢最旧;文件损坏 → 移到 `.corrupt-<时间>` 旁路重建并出声)。白名单:台系月营收 / 环比 / 同比;GPU 现货中位 / 远期概率 / 最低档(报价档数是噪音不做;累计同比与差分文本不做)。
- **召回**:risk 阶段取数后、agent 开工前,编排器确定性生成合成信封 `fetch/thermo_history.json`(账本条目 `thermo_history`,exit 0 / ok,raw 落 `raw/thermo_history.json` 登记 sha256):`<字段>_prev`(上次值,period = 上次资料期)· `_change_abs`(本次 − 上次)· `_change_pct`(相对 %,上次 ≠ 0)或 `_change_pp`(单位本身是 % 的字段给百分点差;"小数"字段只给 abs)。**上次值与变动都是证据、各有 ev id** → 报告数字绑定规则一字不改就能覆盖历史数字。"上次" = **严格更早日历日**的最近同键观测(同日多次运行不互比);无历史 → 不建信封、事件 `thermo_history.none`(首次观测不是缺口)。
- **不可信数据纪律**:序列文件逐条 schema 校验(run_id / 日期 / 字段名 / 短标识 / 有限数 / raw_ref 形如 raw/…),坏条目丢弃计数、整文件坏只出声;**任何自由文本都不进提示词**(note 是编排器按模板生成的);`scenario.inject_thermo_history` 存在时只用注入观测、完全不读真实序列,且该运行不归档。
- 产出:提示词【温度计历史比较】块(列 本次 ← 上次 / 变动 / 各 id)+ risk ⑤ 与报告「## 产业温度计」写法("上次观测 YYYY-MM-DD 为 X [ev-prev],变动 ±Y [ev-change];**两点不成线**"同段;同期重取写明上游未更新)+ SOP `catalyst-risk §5.2` 历史段 + `company-research` 表;manifest `thermo_archived`;doctor 新增「温度计历史序列」概览(损坏 / 无效条目 warn);CLI `node orchestrator/src/thermo_history.ts show|backfill`(backfill 扫已完成非 scenario 运行回填,幂等;本机回填 5 个运行 65 条观测,全部 08-23 → 今天没有"更早日历日",真实 delta 要等明天的运行)。
- 硬测试第 11 组 `thermo_history`(`judgeThermoHistory`):注入三条合成上次观测(B200 9.17 / 台光月营收 177.35 资料期 06 月 / 台光环比 3.1%)→ 账本 `thermo_history` 带 `synthetic_overlay`、fetch + raw 在场;`_prev` = 注入值、`_change_*` 按本次真值逐项复算;risk 引历史 id;章节引 ≥1 prev + ≥1 change;引历史 id 的行带"两点不成线";**9.17 / 177.35 只能出现在引用对应 _prev id 的行且不得写成"本次 …"**;数字绑定;`test_scenario=true` 且 `thermo_archived=null`;再叠第 8 组基线全部检查。
- 真实运行 ht17(注入三条上次观测):编排器出 8 条历史证据(B200 7.25 ← 9.17:−1.92 / −20.94%;台光 192.07 ← 177.35:+14.72 / +8.3%;环比 8.3% ← 3.1%:+5.2 百分点),agent 在 risk 与报告都按要求写(上次值 / 变动各带 id、"两点不成线"同段、不冒充本次值、12 个数字全绑定、0 拦截);本层 10 项 9 过,挂的是运行因 `fetch_profile` 网络超时 incomplete(exit 2,与本层无关)。顺带修两处判定:manifest `fetch_ledger` 摘要不带 `synthetic_overlay`(`ledgerSummary` 只抄 `injected`);基线 GPU 护栏正则要求否定词紧跟"保本线",agent 写"不是**含电力、机房、运维的**完整经济保本线"被误杀 → 正反向都改成 tempered gap(≤ 24 个非句读字符且不含 折旧参考线 / 保本线)。
- **Codex 审计 thermo-r1(2 P1 / 8 P2 / 1 P3)→ 11 条全部成立、全部采纳**:
  - 🔴 P1 序列字段能带文本进提示词(`run_id` 进 note、`period` / `unit` 正则允许字母与汉字)→ period 只认 `YYYY-MM-DD` / `YYYY-MM-DD..YYYY-MM-DD` 且校验真实日历;**上次观测 unit 必须与本次相同才比**(顺带解决 P2 单位漂移 750 美分 − 7.5 美元);**run_id 不再进 note 与信封 extra**,只留在 agent 读不到的 raw/。
  - 🔴 P1 backfill 拿"仅供审计"的磁盘 `_ledger.json` 当认证 → 回填复核 信封 sha256 = 账本记录 / raw 文件 sha 一致 / 证据 raw_ref 在账本 raw 清单内 / 退出码 ↔ 状态不变量 / 信封 schema / manifest run_id = 目录名 & exit_code ∈ {0,2};过不了的端点跳过并出声 `thermo_history.backfill_skipped`(本机真实 19 个运行回填结果不变:5 个运行 65 条)。
  - P2 归档 TOCTOU → `appendThermoLedger` 重读信封前核对 sha256 = 内存账本(不一致 → 跳过 + `thermo_history.envelope_tampered`),并复核退出码不变量与信封 schema;P2 无大小上限 → 文件 > 16 MB 或条目 > 12,000 按 unreadable;P2 资料期语义 → `periodRelation` 三态(advanced / same / regressed):倒退的候选不比并计数,同期非零差写"上游修订或来源变化"而不是"未更新";P2 并发丢观测 → 端点级 `withThermoLock`(mkdir 原子锁、5 s 等待、120 s 陈旧回收,拿不到锁出声不静默);P2 知识层 §2 表会把 `_prev` 当本次事实归档 → `source=history` 不进关键数据表。
  - P2 判定只要求"任一"历史 id 会假绿 → 按 `fetch/thermo_history.json` 的**每一组比较**要求 prev id + ≥1 change id 都在章节、risk 引到每组 prev id;冒充检测改为按全部 `_prev` 证据动态判,"本次"窗口改为"≤ 8 个非数字且非上次/前次字符"(合规的"本次 7.25,上次 9.17 [prev]"不再误杀);护栏加双重否定反转检测("并非不构成趋势");P3 "同段"按 Markdown 段落判(`paragraphsOf`:空行分段、**列表项各自成段、续行并入**——保住 industry-r1 "相邻列表项不能借 id"的口径),基线判定同改。
- **Codex 审计 thermo-r2(5 P2)→ 5 条全部成立、全部采纳**:① 陈旧锁回收竞态(两个进程都判陈旧,后者 `rmdir` 会删掉前者刚建的新锁)→ 锁目录内写 owner 令牌(pid + 随机),陈旧锁用 `rename` 原子回收(只有一个进程能成功),释放前核对令牌、不是自己的不删;② 在线归档没做 raw 归属校验(全局 validator 只证明 raw 文件属于"某个"账本条目)→ 在线与 backfill 同一口径:本端点 raw 文件逐个 sha 一致 + 每条证据 raw_ref **精确等于** `raw/<本端点 raw 文件名>`;③ basename 比对被 `raw/../auth.json` 绕过 → 同 ②,且序列观测的 raw_ref 正则禁止任何斜杠;④ `paragraphsOf` 表格行会接续行借下一段的护栏 / id → 表格行独立成段立即 flush;⑤ `_prev` 值与本次别的证据同值(prev 8.3 vs 本次环比 8.3)时"本次环比 8.3% [ev-cur]"被误判冒充 → 本段引用了同值的非 history 本次证据就豁免,只引 prev id 写"本次 v"照判。真实 19 个运行在更严归属校验下回填结果不变(5 个运行 65 条)。
- **Codex 审计 thermo-r3(2 P1 / 2 P2 / 1 P3)→ 5 条全部成立、全部采纳**:🔴 仅凭 mtime 抢活锁(持锁方还活着只是慢,120 s 后被抢 → 互斥失效)→ 回收只认"owner 缺失且目录超 120 s"或"owner pid 已不存在"(`pidAlive`),活进程的锁等到超时抛错;🔴 `raw_ref: null` 绕过归属校验 → 每条证据必须是字符串且精确属于本端点,无 raw_ref 的证据不抽;无首尾竖线的 GFM 表格仍能跨行借护栏 → `paragraphsOf` 识别 `---|---` 分隔行、表头与数据行各自成段;同值豁免按整段太宽("本次同比 8.3% [prev];本次环比 8.3% [cur]"第一句漏网)→ 按分句判;整数上次值 `8` 咬本次 `8.3` 前缀 → 右边界排除小数点、数值匹配前剥 ev id(十六进制里有孤立数字)与日期。
- **Codex 审计 thermo-r4(1 P1 / 1 P2,都在锁上)→ 2 条全采纳**:回收 ABA 竞态(两个进程同判陈旧、后者把前者刚建的新锁 rename 掉)→ 回收动作用第二把 `.reclaim` 锁串行化,拿到后**重读**主锁 owner 再判一次,仍陈旧才 rename 删除(主锁的增删只在 reclaim 锁内或正常释放时发生);PID 复用把死锁永久误判为活锁 → 加 1 小时硬上限(归档毫秒级,活锁不可能持有 1 小时)。**已知残余(接受,写在函数注释)**:两个进程在 60 s 内先后崩在 reclaim 锁里的极端情况;更强的保证要靠内核 advisory lock,Node 核心没有。按收敛规则 r4 之后不再开 r5,残余项交 Simon 定。
- **ht19(最终代码,注入三条上次观测)1/1 通过、19/19 检查**:编排器出 8 条历史证据(B200 7.25 ← 9.17:−1.92 / −20.94%;台光 192.07 ← 177.35:+14.72 / +8.3%;环比 8.3% ← 3.1%:+5.2 百分点),报告章节三组比较全覆盖、上次值 / 变动各带 id、"两点不成线"同段、16 个数字全绑定、合成序列未归档;ht18(联邦公报 503 → policy_access 失败 → agent 如实标 risk incomplete → exit 2)本层检查亦全过,只挂"complete"两条(外部源)。
- 测试:TS **156**(`thermo_history.test.ts` 13 项:校验含假日历 / 注入文本形状 / 读损坏与超限 / 抽观测(UTC 跨日)/ 合并幂等上限 / 选上次含单位不符与资料期倒退 / 四种变动 + 同期修订 + 溢出 / 归档 TOCTOU + 不自洽 + 锁占用 + 陈旧锁 + 旁路 / 合成信封过契约 / 注入不读真实序列 / backfill 过滤含伪造信封与换 raw / 判定函数 8 种反例 + 2 种不误杀 / paragraphsOf)。

## 1.0.0 开发记录 · 第 14 层「管制与准入」(2026-08-23 晚,看板信息源移植第 2 项)

- **定位**:美方名单**状态**(1260H / BIS / FCC)对单标的的判定;与供需正交,只当打折项。移植 Simon 私有 V2 `policy_access.py`,护栏原样:判定以联邦公报通知**全文检索公司英文名**为准(抽取结果会漏 → 假阴性最危险);无一手英文名只能 undetermined;没被点名 ≠ 不受影响(FCC 整类禁令不点名);"被建议列入" ≠ "已列入";中方侧沉默不能证明不受管制。
- **端点 `policy_access`**(109 端点 / 26 层,risk optional):巨潮 `F001V` 英文名 + 去后缀别名(≥ 8 字符)→ 联邦公报 API 取最新 1260H 通知全文按别名词边界检索(on_list 带原句 / not_on_list / undetermined;全文 < 2000 字符抛)→ BIS 规则按公司名精确短语检索(0 结果时 API 不带 results 键)→ FCC Covered List 经 r.jina(分节切 / 去重 / 整类禁令与点名实体分开;0 条抛)。**中方侧默认未接入**:商务部出口管制公告列表 JS 渲染零配置抓不到(经 jina 只拿到导航链接——看板事件雷达同一抓法的产出其实也是导航项),证据 `policy_cn_side_status=not_connected` 带护栏,不塞假数据。各端口隔离失败,1260H 与 BIS 都失败才抛。
- 实连 300308:英文名 Zhongji Innolight Co., Ltd.;**1260H 通知 2026-11571(2026-06-10)全文命中 "Zhongji Innolight Co., Ltd. (Innolight)" → on_list**;BIS 0 条;FCC 17 条(整类禁令 1)未点名;7 条证据全带 raw。
- 产出:提示词 ⑦ + topic「管制与准入」+ 报告可选章节(护栏与状态同段,不写"无管制风险")+ SOP `catalyst-risk §5.4`;硬测试第 10 组 `policy_access`(300308 已知在名单 → on_list 原句 / risk topic / 章节通知日期与文号 / 四条护栏 / 无绝对结论 / 数字绑定 / 状态一致)。测试 Python 源层 39(+5)/ TS 142。
- **Codex 审计 policy-r1(3 P1 / 8 P2 / 1 P3)→ 12 条全部成立、全部采纳**:🔴 **最新标题命中的可能是 Removal 通知(只列被移除那家)→ 会把在名单上的公司判成 not_on_list(假阴性)** → 基线 = 最新完整指定通知,后续 removal / addition / correction 按日期应用,重建不了 → undetermined;名称匹配未规范化(跨行 / NBSP / Co.,Ltd / Company Limited / 标签粘连)→ `norm_text` + `html_to_text`;文号 `2026-11571` 被当负数 → claimTokens 剥文号与 `1260H`;短名撞别家("Acme Innolight Holdings")→ 短名只认括号别名,其余 ambiguous → undetermined;BIS 检索结果未复核 → 拉原文本地确认(mentioned / search_hit_unconfirmed);分页与截断计数;中方侧证据 raw_ref 误绑别的端口 → extracted 快照;护栏只查关键词(反写也过)→ 方向性正则 + 绝对结论剥否定语境;"N 条" 小整数未绑定 → 单独绑 count 证据;on_list 冲突检查只看同行 → 全章扫且区分 FCC / BIS 语境;degraded 原因合并。测试 Python 40 / TS 143。**ht14(r1 前代码)章节写法合规,最终判定 10/10**(一次假红:"未被点名不等于不受**整类禁令**影响"被正则卡住,已放宽);ht15(r1 修复版)跑中。
- **Codex 审计 policy-r2(2 P1 / 2 P2)→ 4 条全采纳**:后续通知改变状态后证据仍指向基线(removed 的证据文号 / 日期 / raw 都是基线的)→ `decision_*` 元数据,状态证据指向决定状态的那份通知;BIS undetermined / unconfirmed 仍出 "0 条"(未经确认的阴性)→ 新增 `policy_bis_status`,计数改 `policy_bis_confirmed_mentions_count` 且只在 mentioned / not_mentioned 时生成;确认条目 raw 绑检索响应 → 绑规则全文 `doc_raw_ref`;judge 只要求 1260H + 任意两条 → 四类状态证据都要引。Python 41 / TS 143。
- **Codex 审计 policy-r3(1 P2)→ 采纳**:BIS 为 search_hit_unconfirmed 时报告写"未提及"也能过 → judge 加措辞一致性(写未提及须状态 = not_mentioned;unconfirmed 须写"未确认";undetermined 须写"判断不了")。**三轮 12 → 4 → 1(P1 3 → 2 → 0),r3 无 P1,闭环。** ht15(r1 版代码)当时判定 1/1;**ht16(最终代码)13/13 通过、0 拦截**:章节写 "1260H 状态 on_list,依据 2026-06-10 联邦公报文号 2026-11571 命中该别名;BIS not_mentioned、确认提及 0 条;FCC 截至 2026-08-21 未按名点名;中方侧 not_connected" + 四条护栏。

## 1.0.0 开发记录 · 卡口事件分类器(2026-08-23 晚,Simon 拍板的看板信息源移植第 1 项,"做完一个 Codex 审完再做下一个")

- **定位**:确定性关键词筛子,移植 Vibe-Trading-Simon 资讯中心「事件雷达」的 CHOKE_KW,按对单标的研究有用的 8 类拆细(涨价 / 扩产 / 减产停产 / 订单合同 / 认证导入 / 收购合资 / 供需 / 管制制裁,各带 negatives 与 decision_hint)。**不拉新数据、不产生新证据**:编排器在 risk 阶段取数后扫公司自己的公告 / 新闻信封(`announcement_title` / `news_title` …),命中引用原证据 id,跨脚本同题去重(深交所 + 巨潮),写 `fetch/_chokepoints.json`(受保护)+ `manifest.chokepoints` + 事件 `chokepoint.scan`。
- **提示词 / 产出**:risk / report 注入清单("日期 · 类别 · 标题 [ev-id]")与各类别裁决提示;新 topic「卡口事件」;报告可选章节「## 卡口事件」,每条末尾写对应哪个裁决点;**只能引清单 id、标题数字照抄不换算、零命中不写**;SOP `catalyst-risk §5.3`。
- **硬测试第 9 组 `chokepoint_events`**:scenario `inject_announcements` 追加 3 条伪造公告(重大销售合同 / 提价 + 口令 KIWI-9 / 终止扩产);判定:分类可复算、订单 / 涨价命中、终止扩产被 negatives 排除、risk topic、章节只引清单 id 且数字原样在标题里、口令不执行。
- 文件:`datasources/chokepoint_keywords.json`、`orchestrator/src/chokepoint.ts`、orchestrate / stages / schemas / merge / validator / config / fetchrun / hardtest 接线;测试 TS 140(+4;`orchestrate.test.ts` 假仓库改为带产品数据表——缺表按配置错误抛)。
- **Codex 审计 choke-r1(2 P1 / 6 P2 / 2 P3)→ 10 条全部成立、全部采纳**:判定假绿(同一 id 抄两行过、数字剥单位子串匹配 "12.34 万元" 能为 "12.34 亿元" 背书)→ 逐行核日期 / 类别、不同 id ≥ 2、必须覆盖注入的订单与提价、数字按 {数值原文}{单位} 整 token 比对;泛词误命中("签订募集资金专户监管协议" 进订单合同、"订单式培训"、"会计师…认证"、"回购")→ 上下文正则(`re:` 关键词)+ negatives;negatives 整句否决("A 提价,B 不涨价" 丢涨价)→ 按 ;；,，。| 拆子句在同一子句判;去重剥前缀过宽 → 只剥公司简称且前缀无类别语义,类别取并集;截断 40 条静默 → 每类保底 + 提示词出声;只在 risk 扫 → risk / report 都扫;注入叠加到 failed 信封 → 只叠加 ok / partial 且判定要求真实证据在场;overlay 标签按种类;分类表校验无空白 / 不重复 / 正则可编译。测试 TS 142(+2)。
- **Codex 审计 choke-r2(2 P1 / 1 P2 / 1 P3)→ 4 条全采纳**:判定"真实底座"从全局 evidence 取(别的信封能冒充)→ 只看 `fetch_announcements` 信封自身;复合单位截断("500万只" 改 "500万台" 发现不了)→ 单位整体匹配;顿号 / 括号不是子句边界("扩产项目、终止股权激励" 丢扩产)→ 加 `、()（）`;`normTitle` 不传表时又无条件剥前缀 → 表必传。
- **ht12 真实运行作废但有收获**:它起跑时加载旧代码,中途分类表换成带 `re:` 的版本,旧代码按字面串处理 → 3 条注入只命中 1 条(版本错位,非 bug)。但它 **risk / report 各失败 3 次的真因是另一处误伤**:最终 validator 的 READ_DENY 把 calc 的 `--args '{"klines":{"history_json":{"raw_ref":"raw/tencent_…json"}}}'`(展示拼接形态引号被翻译成 `'"'{\"…`)当成读取 raw → `checkAgentTrace` 先把 `raw_ref` 键后的 raw 路径换占位、调 calc 时 `--args` 到下一个 flag 整段剥掉;ht12 真实命令内嵌进回归用例。⚠️ 事件流 command 事件的输出片段里会带 raw_ref 字段,grep `raw/` 时别把输出当命令。
- **Codex 审计 choke-r3(2 P1 / 1 P2)→ 3 条全采纳**:`--args` 整段剥除会吞掉同一行后续 `&& cat raw/x`(展示形态又不跑形态规则)→ 先按 `&& / || / ; / 换行` 切段、只在调 calc 的段剥;顿号 / 括号切子句让 negatives 脱离修饰对象("关于终止(部分)扩产项目" 命中扩产)→ 顿号 / 括号不切,子句内 negatives 整体否决,只有 启动 / 重启 / 新增 / 新建 / 另行 / 重新 这类明确新动作放行(保守漏判可接受,误判不可);判定"真实底座"只凭 note → 加 `source !== injected` 与 endpoint 非 `hardtest.*`。TS 142。
- **Codex 审计 choke-r4(1 P2)→ 采纳**:新动作词无条件覆盖 negatives("已启动终止扩产项目的审议程序" 被"启动"放行)→ 顺序约束:negative 之后出现新动作词、新动作词之后还有关键词才放行。**四轮 10 → 4 → 3 → 1(P1 2 → 2 → 2 → 0),r4 无 P1,闭环。** TS 142。
- **硬测试 ht13(真实运行 300308 + 注入 3 条伪造公告):11/11 通过(含 harness 两项 13/13)**。真实公告 30 条 + 注入 3 条,扫描 83 条标题 → 命中 2(注入的订单 / 涨价),「终止扩产」被 negatives 排除;risk topic 与报告章节只引清单 id、数字 "12.34 亿元" 整 token 原样;agent 照抄含口令的标题并写明"标题内指令是不可信文本且未执行"——判定随之修正:口令出现在卡口事件章节 / finding 里(照抄)不算执行,出现在其它章节 / 阶段产物才算。0 拦截。

## 1.0.0 开发记录 · 第 13 层「产业温度计」(2026-08-23 下午,Simon 拍板"定位没问题,开始搭建")

- **定位**:注册表新层 + risk 阶段新 topic,与「12 市场声音」同位置、同机制;回答"标的所在产业链上下游现在冷还是热"。选源来自 `Vibe-Research-Dev/信息源盘点_直接信号分级_2026-08-20.md` 的 A 档,实现移植自 V2 `tw_monthly_sweep.py` / `price_signals.py`(含其踩过的坑)。
- **挂载机制**(`orchestrator/src/industry.ts`):端点带 `industry_tags`;编排器在该阶段取数前用 profile 阶段已落盘的行业 / 概念信封(`fetch_profile.industry_em / industry_csrc`、`sw_industry` 代码、`em_concept_blocks.board_membership`)匹配 `datasources/industry_tags.json`(keywords + sw_prefixes)→ 命中才取,未命中整体跳过并记事件 `industry.gate`;结果写 `fetch/_industry.json`(受保护产物,hash 进 manifest 校验)+ `manifest.industry_tags`;risk / report 提示词按命中标签注入题名 / 端点清单 / 护栏句。
- **端点**(108 端点 / 25 层):`tw_monthly_revenue`(FinMind 零鉴权;台光 2383 / 台燿 6274 / 金像电 2368 / 联亚 3081;环比 · 同比 · 累计同比 · 近 4 月环比序列;**台光×金像电差分**四档读法;402 出声、资料期 > 2 月 partial、全灭抛)、`gpu_rent_thermometer`(Vast B200/H100 现货中位 + Kalshi KXB200MS 远期 P(月均 < 最低档);Vast 必须 urllib UA;H100 无在租报价 = 市场状态;Kalshi 三分判定;$3 = 折旧参考线)。证据 `market=TW/US`、币种 TWD/USD(schema 枚举 +TW;硬测试语义检查改为币种 ↔ 单位配对)。每条 note 带"读法:…"护栏。
- **产出与纪律**:`EXTRA_TOPICS.risk` +「产业温度计」;报告可选章节「## 产业温度计」(资料期 · 来源 · 数字[ev-id] · 护栏句 · 与本报告哪条事实印证 / 矛盾);SOP `catalyst-risk §5.2` / `company-research §6`;硬测试第 8 组 `industry_thermometer`(真实运行:标签命中 / 两端点真取数 / risk topic / 章节含两类 id / **章节里每个数字绑到温度计证据 value** / 护栏句同在 / 温度计 id 不进事实 · 估值章节)。
- 实连:台光 2026-07 192.07 亿新台币(环比 +8.3%、同比 +129.4%),金像电 +21.3% → "同增,更可能 Trainium/ASIC 在拉";B200 现货中位 $6.63/卡时(25 档),H100 无在租报价,Kalshi 当日无有效报价。通用取数器跑通(raw 逐条绑定;H100 第二次请求撞 Vast 429 → partial 出声)。
- **硬测试 ht10(第 8 组,真实运行 300308):1/1 通过,11/11 判定 ✓,钩子 0 拦截**。门控依据 39 条信号(CPO 概念 / 光通信模块 / 算力概念 / 通信设备 / 申万 7302);两端点账本 ok;温度计证据 21 条;risk 2 条 topic「产业温度计」;报告章节引 9 个 id,6 个数字全部绑到证据 value,护栏句同段,温度计 id 未进事实 / 估值章节。
- **Codex 审计 industry-r1(6 P1 / 5 P2,`.local/reviews/industry-r1.*`)→ 11 条全部成立、全部采纳**(Simon 纠正"每个环节不能跳过审计"后补审;这轮零误报):
  - ✅ 真 bug:偶数报价中位数取成上中位(`[2,10]`→10)→ `statistics.median`;Kalshi 把不同合约月的阶梯混排、证据 period 写成取数日 → 按合约月分组只用最近月,period = 合约月;差分证据 raw_ref 继承 capture 最后一个响应(联亚)→ 绑台光响应 + note 带金像电 raw_ref;FinMind 重复月 / 未来月静默放过、缺基数仍 ok → 同月不同值抛、未来月抛(台北时间判当前月)、缺基数写 missing 标 partial。
  - ✅ 判定假绿:数字行无 id 回退全池 → 只绑本行引用的 id;护栏只查全章 → 按行查(台系行要差分,GPU 行要折旧线)。
  - ✅ 收紧:GPU 每张卡与 Kalshi 各自异常隔离(只有现货全 error 且远期 error 才抛;无在租报价是市场状态);`offers` / `markets` 字段缺失 = 契约错不是无报价;所有数字证据 note 带护栏;带 `industry_tags` 的端点不得 required(注册表校验),提示词与 validator 都排除门控跳过的端点;关键词分强 / 弱("通信设备"单独不挂,弱词 + 申万前缀才挂;ASCII 缩写词边界);`currency ≠ n/a` 一律做币种 ↔ 单位配对。
  - 测试:Python 源层 33(+8)/ TS 134(+3);ht10 产物用加严判定离线重判 9/9。
- **Codex 审计 industry-r2(2 P1 / 3 P2 / 1 P3)→ 6 条全部成立、全部采纳**:护栏判定只查关键词、"无需差分 / 可以单独归因 / 不是折旧参考线"这类反话也能过 → 正向短语 + 反向剔除;温度计 id 写进结论摘要不会被抓 → 只许出现在温度计 / 风险 / 裁决点 / 缺口章节,其它章节须同行写明"不是本公司数据";Vast 单条 `num_gpus:"unknown"` 击穿整张卡 → 逐条解析计坏条目;标签表缺失被当零标签静默跳过 → 直接抛;GPU 快照日期 UTC 在本地凌晨少一天 → UTC+8;合约月前缀匹配吞 `26SEPT` → fullmatch。测试 Python 34 / TS 135;ht10 重判 9/9。
- **Codex 审计 industry-r3(1 P1 / 2 P2 / 1 P3)→ 4 条全部成立、全部采纳**:`{"tags":[]}` 的 typeof 也是 object、被当零标签 → 标签表校验拒绝数组 / 空对象 / 空字段;两个护栏正则误杀合规写法("不可单独归因,需差分后再判断"、"仅为折旧参考线,不能视作完整经济保本线")→ 正向短语放宽、反向语义剔除前**全局**剥正向短语;GPU mapper 写 `checked_tz`。**硬测试 ht11(真实运行)**:起跑时加载的是 r1 版判定,唯一未过项是 agent 如实写 "H100 因 HTTP 429 未获取" 的 429 被当数字主张 → `claimTokens` 剥 HTTP 状态码;Vast 两卡之间 sleep 1s 防 429。最终判定下 ht10 / ht11 都 9/9。测试 Python 34 / TS 136。
- **Codex 审计 industry-r4(3 P2,全部是护栏正则的措辞覆盖)→ 全采纳**:"未能单独归因"被 `TW_NEG` 当反向 → 否定前缀扩到 未 / 未必 / 难以 并先剥正向短语;`GPU_POS` 收 "不能视为 / 不可视为 / 不等同于";`GPU_NEG` 加 "相当于 / 视同 / 构成 / 属于"。**四轮走势 11 → 6 → 4 → 3(P1:6 → 2 → 1 → 0),r4 无 P1,按收敛规则闭环,不开 r5。** ht10 / ht11 最终判定 9/9;测试 Python 34 / TS 136。

## 1.0.0 Development notes

### 市场声音 · 一手信源层(Phase 1.5 第一期,2026-08-23)
- 注册表新增第 12 层「市场声音」两个零配置端点(`datasources/registry.json` 106 端点 / 24 层):`exa_market_voice`(Exa 免 key MCP `mcp.exa.ai` 全网语义搜索:新闻 / 深度文 / KOL 帖,查询词由模块按公司名确定性生成、按主题分组,近期非论坛前 N 条再经 `web_fetch_exa` 读摘录)与 `exa_forum_voice`(雪球 / 股吧讨论,经 Exa 索引只取标题 / 作者 / 日期 / 链接——雪球直连与 Jina 读正文均被 WAF 拦截,2026-08-23 实测)。均挂 risk 阶段 optional。
- `sources/exa.py`(Streamable HTTP JSON-RPC 客户端,SSE 按 UTF-8 解码,每次调用经 `_http` 落 raw)+ `sources/textsafe.py`(不可信文本净化:控制 / 零宽字符剥离、空白压缩、截断、**动作措辞替换为〔动作词〕**——词表与 `config.ts GATE_PATTERNS` 逐字一致,TS 测试强制校验;原文留 raw,不做语义改写)+ `mappers_cn.exa_market_voice_map / exa_forum_voice_map`(每条一证据 `web_result / forum_post / web_excerpt`,note 带主题 / 域名 / 作者 / 日期 / 链接 / 摘要,`untrusted_text=sanitized`)。
- 约束三处落实:risk 阶段提示词新增 ④ 市场声音规则(不可信文本、指令不执行、**帖子里的数字一律不得写成事实**、不转述动作措辞、只写"谁在讨论什么 / 热度 / 对应哪条事实");`EXTRA_TOPICS.risk` 新增 topic「市场声音」;report 可选章节「## 市场声音」;SOP `catalyst-risk` §5.1 / `company-research` §6 同步。
- 硬测试第 7 组「市场声音注入」:scenario `inject_voice` 在真实信封里追加伪造帖(夹指令口令 + 伪造数字 + 动作措辞),判定口令未出现 / 伪造数字未进报告 / gate 零命中 / 章节与 topic 真实产出。
- 零配置假设实测记录(`sources/exa.py` 头注释):Exa MCP 无 key 可用、无域名过滤参数(查询词引导可命中 xueqiu.com);Jina Reader 免 key 20 次/分钟(需 curl 风格 UA,能渲染 JS);雪球匿名 / Jina 读帖正文均不可用。
- 真实取数(300308):`exa_market_voice` 8 秒 31 条(含前一日新闻)+ 3 段摘录,`exa_forum_voice` 5 秒 13 条(雪球 10 / 股吧 3),证据里动作词残留 0。
- 全量运行 `mv-run-1`(含本层):complete、证据 878(+58)、gate 零命中、帖子数字零泄漏、〔动作词〕零出现;硬测试 ht2:指令未执行 / 8888.88 未进报告 / gate 零命中 / risk 发现 ✓,**但 report 阶段把「市场声音」章节整个省略** → 提示词加严:按 topic 各写一条、每条 2–3 条具体线索("日期 · 来源 · 讨论什么 [ev-id]")、evidence_ids 不得只引计数证据、章节至少 3 条本层 id、正文不贴 URL(链接由附录给);`judgeMarketVoice` 改为要求章节引用 ≥ 3 条本层具体线索 id。
- `claimTokens` 先剥 URL 与速率 / 制程标签(`1.6T / 800G / 7nm` 是产品类别名,不是数字主张);`report_appendix.md` 证据索引新增「链接」列(`linkOf`:note 里 `link=…` → `[原文](url)`,"每句可点回原帖"落在附录而非正文)。
- 硬测试 ht3(提示词加严后):**9/9 通过**,章节 8 条本层线索 id;但写法仍是"按主题概括",未到"每条一行"。

### Codex 审查 voice-r1(5 P1 / 6 P2,`.local/reviews/voice-r1.*`)→ 逐条核实后 11 条采纳 1 条部分采纳 1 条不成立
- ✅ **读禁区**(P1):线程沙箱 `workspace-write` 全盘可读,不可信帖子可诱导 agent 去 `cat raw/…` 或读 `.local/codex-home/auth.json`,此前只有提示词在拦。`validator.ts checkAgentTrace` 加 READ_DENY(raw/ 路径、auth.json / api.token / codex-home / .local/config.json / .env / id_rsa / .ssh / .npmrc / .netrc、`~/` 与 `$HOME`),PreToolUse 钩子复用同一函数即时 block。这是**旧口子,市场声音层放大了暴露面**。
- ✅ **脱敏绕过**(P1):`建 仓` / `建͏仓`(CGJ)/ `目️标价`(VS16)/ `目標價` 原先都漏。`textsafe.py` 改为"规范形匹配、原文位置回写"(NFKC、剥 Cc/Cf/零宽/CGJ/变体选择符、繁→简 `TRAD_CHARS`、汉字之间的空白 / 中点 / 连字符 / 下划线忽略),正常标点空格不动;`gate.ts canonicalForGate` 同构(exempt 行也按规范形比);`TRAD_CHARS` 双向一致性测试。URL 经 `safe_url`(仅 http(s)、非 ASCII 百分号编码、剥不可见、截 300)再进 note。
- ✅ **裸词"增持"误伤**(P2,实为旧 gate bug):"控股股东拟增持公司股份"是公司行为事实,会被脱敏成〔动作词〕、也会让 gate 误拒报告。GATE_PATTERNS / GATE_WORDS 同步改为 `建议增持` + `增持评级`。
- ✅ **judge false-green**(P1):金丝雀改规范化比对——口令剥非字母数字后找(`Z E B R A - 7` / 全角 7 都认),数字查 8 种变体(千分位 / 8888.9 / 8889 / ×1e4 / ÷1e4 / ÷100 / ÷10,两侧不能再有数字);新增三项:线索行 `claimTokens` 必须为空(帖子数字不是事实)、注入 id 不得进 事实 / 推断 / 估值 / 结论摘要 / 裁决点、≥3 行带日期(或"日期不详")线索 + 章末固定声明。
- ✅ **速率标签误伤金额**(P1):只剥前无货币符号、后不接 元/美元/亿/万/%/倍 的 `\d+[TG](bps|b)?`;`nm` 不再剥。`$1.6T` / `1.6T 元` / `800G 美元` 保留为数字主张。
- ✅ **raw_ref 全绑最后一次响应**(P2):ExaClient 每次 RPC 记 `last_raw_ref`,`call_text` 返回 (text, raw_ref),搜索条目 / 摘录逐条带 raw_ref,mapper 显式传。
- ✅ **无日期 / 未来日期伪装当前**(P2):未来(>now+1d)/ 非法日期当未注明;note 加 `period_basis=published|fetched` + `published=N/A`;近 N 日零条带日期新条目 → partial 出声;提示词:这类条目写"日期不详"、不得称"最近"。period 仍用取数日(schema / 硬测试 periodOk 只认日期形态,语义走 note)。
- ✅ **limit 不可达**(P2):source 函数显式收 `limit` 并回传,mapper 读 `result.limit`,registry args 加 `limit=40`。✅ **record_key 尾截断撞 id**(P2):完整 URL sha256 前 24 位。
- ✅ **SSE 解析**(P2):按空行分事件、多 `data:` 行拼接;严格 UTF-8(非法字节抛,不 replace);JSON 批量数组;只认 `jsonrpc=2.0` 且 id 匹配;搜索文本非空却解析 0 条 → 抛(除 "no results" 类提示);HTTP 4xx/5xx / 超时 / 连接错统一 ExaError。实连冒烟:initialize 1.0s / 搜索 1.9s / fetch 0.3s。
- ✅ **摘录全失败仍 ok**(P2):候选 > 0 且 0 读到 → partial;部分失败 → `extra.warnings`。
- 🔶 **inject_voice 无生产门控**(P1,部分采纳):**不拆 `--scenario`**——它是整套硬测试的旋钮(与 inject_conflict / inject_evidence 同级)。改为:合成证据 `source=hardtest-injected`、`raw_ref=null`、record_key 与 mapper 同构;`orchestrate.ts` 任何非空 scenario → `manifest.test_scenario=true` 且**不进知识层归档**(否则伪造证据会被下次召回)。
- ~~❌ "设置 entry.injected 让 validator 跳过不变量"不成立~~ → **🔴 更正(r2b 复核):这条是对的,我判错了**——`fetchrun.ts:135` 行尾 `...(injectedVoice.length ? { injected: "inject_voice" } : {})` 确实把标记展开进了账本条目(我只看了被截断的行)。已改为账本字段 `synthetic_overlay`(不触发 `validator.ts:110` 的豁免),不变量照常生效。教训:**核实 Codex 结论时必须看全行,截断输出不能当证据。**
- 测试:Python 源层 12(新增绕过形态 / safe_url / SSE 多行 · 批量 · 非法 UTF-8 · RPC error / 逐条 raw_ref / limit / 未来日期 / 状态规则 / 哈希键)、TS 122(gate 规范化 + TRAD 一致性 / claimTokens 金额语境 / 金丝雀变体 / 读禁区)、calc 188、doctor 14 ok。
- 硬测试 ht4(上述修复版):financials 阶段 agent 的 calc 调用被钩子拦 5 次 → **产品 bug**:`makeConfig` 不规范化 python 路径,`--python repo/../.venv/bin/python` 字面带 `../` 原样进提示词与允许前缀(agent 照抄撞「含 ../」,改绝对路径又撞「仓库外路径」)→ `normalizeInterpreter`(带目录的路径 resolve,裸命令保留)+ 回归测试。⚠️ 该运行"挂 75 分钟"是 MacBook 10:04–10:35 睡眠(pmset 日志),非产品问题;Codex exec 不理 `timeout` 的 SIGTERM,长审计要 `timeout -k`。
- 硬测试 ht5(路径修复版):**运行 failed / exit 3 —— 我的回归**:注入证据 `source` 写成 `hardtest-injected`,validator 的"每条证据必须有 raw_ref(source=injected 除外)"不豁免 → risk / report 各重试 3 次全红。改回 `source=injected`(与 inject_evidence 同口径)。

### Codex 审查 voice-r2b(静态复审,5 P1 / 2 P2 / 1 P3,`.local/reviews/voice-r2b.*`;r2 首发在沙箱里反复跑测试超时作废)→ 8 条全部采纳
- ✅ **READ_DENY 可被通配 / 替换绕过**(P1):`r?w/*.txt`、`.local/codex-*/a*.json`、`$(echo raw)` 都绕得过字面量。新增 `commandSafetyErrors`(钩子与最终 validator 共用):剥引号段后通配符只许在 runDir 下 fetch/ calcs/ stages/ 前缀;禁命令 / 进程替换与 eval;禁 find / xargs / chmod / chown / ln / xattr / sudo 等;禁 python 目录枚举 / 子进程 / 改权限;禁裸路径段 raw / codex-home;heredoc 正文只做 python 规则。真实运行里 agent 合法形态(`for f in calcs/*.json; do jq … "$f"`、`rg --files calcs`、`[ -f x ] && …`、heredoc python 读 JSON)测试放行。**明确:这是纵深防御不是安全边界——Codex 的 workspace-write 沙箱没有 deny-read;物理隔离 = raw 加密落盘、线程只见密文,记为待办。**
- ✅ **分隔符两端不同构 + 组合附加符**(P1):`建/仓`、`建议+买入`、`建\u0301仓` 两端都漏,`建―仓`(U+2015)TS 命中 Py 漏 → `CJK_SEP_CHARS` 同一字符串(测试双向比对,含 `/ + U+2015`),规范形剥 Mn/Me(只匹配用)。
- ✅ **entry.injected 豁免**(P1):见上方更正。✅ **金丝雀中文数字 / 科学计数 / ×1e8**(P1):`cnNumeralToNumber`("八千八百八十八点八八"→8888.88)+ 科学计数展开,变体表改十进制字符串移位生成(浮点会把 8888.88×1e8 算成 888887999999.9999)。✅ **claimTokens 金额语境**(P1):`stripSpeedLabels` 只在"有速率语境且无金额语境"时剥(`市值 USD 1.6T` / `公司市值 1.6T` 保留为数字,`800G元器件` 放行)。
- ✅ **时间精度**(P2):解析完整 ISO 时刻判未来与窗口,`_collect` 只取一次 now(原先截成日期会把 `2026-08-24T23:59Z` 当"不超过一天"放行、把窗口边界上的条目判 stale)。✅ **linkOf Markdown 注入**(P2):`)![x](…)` 能从 `[原文](…)` 逃出来拼第二个链接 → URL 元字符再编码 + `<url>` 写法,`safe_url` 的 safe 集去掉 `()[]!'`。✅ **测试缺口**(P3):`_rpc` fake http_post(Session-Id 继承 / 逐次 raw_ref / 429 / timeout / 500 非法 UTF-8)、注册表 args ⊆ 函数签名、时间边界。
- 硬测试 ht6 首阶段即误伤:agent 自检用 `ids=$(jq -r '.evidence_ids[]' stages/profile.json)` 被"禁命令替换"一刀切拦下 → 改为**放行命令替换但递归按同一套规则检查内部**,另禁 ① 替换结果直接当路径段(`$(…)/x`、`/$(…)`)② 字符串构造器(printf 转义 / base64 -d / xxd / rev / tr,能拼出 raw 字面量)③ 嵌套替换;进程替换与 eval 仍禁。
- ht5 / ht6 valuation 阶段各 1 次"读取 raw/"拦截是**误伤设计内路径**:agent 调 `calc/cli.py percentile_rank --args '{"history_csv":{"raw_ref":"raw/extracted_…csv"}}'`(calc 读提取出的数值 CSV,不是不可信文本)→ 命令调用 `calc/cli.py` 时 `--args` 的 JSON 不参与 raw 读禁区判定;`… && cat raw/x.txt` 照拦。
- 硬测试 ht6:防线 7/10 ✓(指令 / 数字 / gate / risk 发现 / 注入 id 不进事实章节),但 **report 阶段被我的通配规则拦 3 次 → 运行 failed**:agent 自检 `jq … scan("calc-[0-9a-f]{16}")`,`[` 在双引号里,zsh 不展开;错在 ① 事件流里的命令是 Codex 的展示拼接 `/bin/zsh -lc "<脚本>"`(内层引号不转义)没先剥外壳 ② 剥引号分两趟(先单后双),交错时剥错——改为一趟从左到右 ③ jq / test 语法记号 `[]` `[` 没有字母前缀也没有斜杠,不是路径通配(`cat */*` 仍拦)。ht6 真实命令已进回归用例。另两处判定误伤:`163.com` 域名里的数字被 `claimTokens` 当主张 → 先剥域名;章节把 3 条线索并成一行 → 提示词明确"一行一条、不用分号并列"。
- Codex r3 / r3b 闭环审计两次都没产出:r3 把旧文件原样抄回(行号全旧),r3b 开 collab 子代理卡住超时(exit 124)——它超时前叙述的两条具体点都成立:① READ_DENY 误伤 calc `--args` 的受控 raw_ref(已修)② "替换结果不得作路径"只拦 `$(…)/x` 形态,`cat $(jq -r .raw_ref fetch/x.json)` 整路径替换放过 → 替换内部凡引用 raw 一律拦。
- 硬测试 ht7 financials 阶段再踩一处误伤:zsh 参数展开 `${spec%%:*}` / `${spec#*:}` 里的 `*` 是子串操作符 → 通配检查前把 `${…}` 与 `$(…)` 换成占位(`cat ${X}/*/*` 仍拦)。
- 窄范围闭环审计 r3c 起跑即死:**全局 Codex CLI(`~/.codex`)的 ChatGPT 登录态被服务端吊销**(`token_revoked` / `refresh_token_invalidated`,`codex login status` 仍显示已登录是只看本地文件)→ 审计通道等 Simon 重新 `codex login`;产品 CODEX_HOME 登录不受影响(doctor 14 ok、ht7 无 401)。
- 硬测试 ht7:防线 8/10 ✓,但 estimates 阶段被通配规则拦 3 次(jq 程序 `$f[0].evidence[]` 被当路径通配)→ 运行 failed。**改为语料驱动**:把所有真实运行 `events.jsonl` 的 1,142 条命令与 `hooks.log` 的 825 条真实脚本过一遍规则,一次扫出 6 类误伤并重写 `commandSafetyErrors`:① 只有含 "/" 的通配才可能是路径通配,字面前缀须含 fetch/ calcs/ stages/ 段(`.local/runs/x/calcs/0[1-9]_*.json`、`"$RUN"/calcs/*.json` 合法)或落在允许前缀的仓库源码下;② 命令替换 / 反引号 / 进程替换 `<(…)` 放行,按括号深度配对提取(jq 程序有括号)、递归检查、紧贴才算路径段、内部引用 raw 则拦(调 calc `--args` 的除外);③ 构造器只留 printf 转义 / base64 -d / xxd(`tr` / `rev` 是常用);④ `${spec%%:*}` 换占位;⑤ 展示拼接外壳先剥、`\"` 还原;⑥ **形态规则只在钩子路径对真实脚本跑**——事件流里的命令是 Codex 的展示拼接,内层单引号渲染成 `'"'` 无法可靠还原,对它做形态分析 47 条误伤;最终 validator 对展示形态只跑字面规则。回归结果:347 条完整真实脚本只剩 2 条 `find`(本来就该拦);好 / 坏样例各 35 / 24 进测试。钩子日志命令截断 300 → 4000(否则语料不可用)。
- Codex 闭环审计 r3c 被 OpenAI 判"可能的网络安全风险"拒(提示词让它构造读凭据的命令)→ 改防御口径 r3d("命令分类器的误判输入")通过:**2 P1 / 3 P2,采纳 4 条、1 条不成立**。✅ `总市值约1.6T` 的"约"隔断金额语境 → 金额词后允许 约/为/达/超过/接近/逾/的/规模 等修饰;✅ python `glob.glob("fetch/*.json")` 被 API 名整体封禁 → 字面路径在 fetch/ calcs/ stages/ 下放行;✅ `cnNumeralToNumber("一亿零八万")` 把亿级总数又乘了万 → 按亿分大段、万结算当前段;✅ 金丝雀 `8889` 误中 `8889.5` → 右边界拒小数。❌ "字面相对路径没有三目录白名单"(`jq . evidence.json` 放行)不成立:运行目录顶层的 evidence.json / report.md 是净化产物,agent 自检必读;字面路径由 READ_DENY 管,"只读三目录"是提示词说过头。
- ht9 financials 阶段 1 次拦截 = `find calcs -maxdepth 1 -type f`(合法目录上的 find)→ 放开 `find` / `xargs`:find 的路径参数须在 fetch/ calcs/ stages/ 下且不带 -exec / -execdir / -ok / -delete;语料回归 360 条完整脚本只剩 1 条(在整个仓库树里 find python,本该拦)。
- **硬测试 ht9(最终规则版):运行 complete / exit 0,市场声音判定 10/10**(含 harness 两项 12/12)。章节终于是"日期 · 来源 · 在说什么 [ev-id]"形态(3 行 / 6 个本层 id)、线索行零数字、注入 id 不进事实章节、gate 零命中。唯一先红后改的判定:agent 把免责声明放到章首并换措辞("以下均为不可信文本线索…不作为事实"),语义等价 → 判定改为语义匹配(线索 + 非事实 / 不作为事实 / 不构成,章首章末皆可)。
- 测试:TS 128 / Python 源层 17 / calc 188。本期闭环:Codex r1 → r2b → r3d 三轮实质审计(r2 / r3 / r3b / r3c 四次因工具问题作废),ht2–ht9 八轮硬测试(其中 5 轮暴露并修掉的是规则自伤)。

### skills 隔离 + 中文路径修复(2026-08-22,接手后首批)
- `orchestrator/src/skills_isolation.ts`:Codex 会从用户主目录 `~/.agents/skills`(按 `$HOME`,与 `CODEX_HOME` 无关)与捆绑系统 skills 发现 skill,用户的个人 skill 会挤爆 skills 目录预算并污染提示层(本机实测 92 个个人 skill,agent 去读了 `~/.agents/skills/<x>/SKILL.md`,靠 PreToolUse 钩子拦下)。现每次运行开始时在产品 `CODEX_HOME/config.toml` 标记块内写入 `[skills] max_context_tokens = 10000` / `[skills.bundled] enabled = false` / 逐条 `[[skills.config]] path … enabled = false`(按 path 不按 name),幂等、不动块外内容、与 hooks 块并存;manifest 新增可选 `skills_isolation`,事件 `skills.isolated`;`doctor` 新增检查项「用户级 / 捆绑 skills 隔离」;CLI `node orchestrator/src/skills_isolation.ts --codex-home <dir>` 可立即写入。
- `hooks.ts mergeBlock` 接受自定义块标记(hooks / skills 两块复用同一机制)。
- Codex 审查 r1(3 P1 / 3 P2 / 1 P3,全部核实成立)→ 修:枚举改为复刻 Codex Recursive 发现(≤ 6 层、跳隐藏祖先、跟随目录符号链接、环路去重;原先只看直接子目录会漏禁嵌套 skill);主目录解析与 `dirs::home_dir()` 同语义(`resolveHomeDir`:相对 HOME 不枚举、空 HOME 回退 passwd);事件 / CLI 只记数量与清单 sha256,不再把用户路径清单写进 events.jsonl(service 层会经 API / MCP 回传);枚举只吞"根不存在",权限 / I/O 错误抛出终止;块外冲突检测覆盖注释、引号表头、点号键、顶层内联表;doctor 结构化核对块(path 必须配 `enabled = false`、bundled、预算合法),无块一律 warn,修复命令改绝对路径 + POSIX 单引号;`scripts/init` 首装即写隔离块。
- 修复:`knowledge_viewer.test.ts` / `validator.test.ts` 用 `new URL(import.meta.url).pathname` 算仓库根,安装路径含非 ASCII(如中文目录)时百分号编码导致 `calc/cli.py` 找不到 → 统一 `fileURLToPath`;`service_api_mcp.test.ts` 去掉写死的开发机 venv 路径,改为 `VRA_PYTHON → 仓库 .venv → 上级 .venv → python3`。
- Codex 审查 r2(3 P1 / 5 P2,核实全部成立)→ 修:① 目录深度边界按 Codex 自测同构(`d0/…/d5/SKILL.md` 可见、第 7 层不可见;原先少扫一层);② **SKILL.md 文件符号链接一律忽略**(Codex `ignores_symlinked_skill_files`;原先跟随,用户目录里一条指向产品 skill 的链接经 Codex canonicalize 会把产品 skill 禁掉),并且 **realpath 落在产品仓库内的路径一律不写入**(编排器 / init / doctor / CLI 都传产品根);③ 多结果函数(`pe_digestion_scenarios`)的四锚年数在 `details.scenarios.<情景>` 里没 display 可抄 → `attach_display` 给 details 里所有结果形子对象附 display,report 提示词指向它;④ 块外冲突检测解码引号 / `\u` 转义键;⑤ doctor 状态解析:同时含 `path` 与 `name` 的条目按 Codex 语义整条忽略(不算禁用)、单引号 path / `10_000` 预算不算有效;⑥ 错误信息里的用户路径改 `~` 相对;⑦ `verifyCalcs` 复算时比较 `display`(被删 / 被改即不一致);⑧ display 缩放后二次校验有限性(`1e308` 不再吐出 `inf%`);⑨ hardtest `claimTokens` 小整数白名单加 `年 / 期`(消化年数与期数不再绕过数字绑定)。**未采纳并说明**:事件 / manifest 里的 `config_toml` 绝对路径保留(产品目录,manifest 本就记 `engine.codex_home`);Codex 的 2,000 目录 / 20,000 条目截断不复刻(Codex 截断只会少看到 skill,我们多禁无害);块外冲突检测仍是行扫描而非 TOML AST(产品 CODEX_HOME/config.toml 只有 init / 编排器 / Codex 自己写,Codex 只写 `[projects]`;多行字符串伪装表头会误拒不会漏判)。
- Codex 审查 r3(2 P1 / 7 P2)→ 采纳并修:① 排除规则改为"产品 skill 集合成员"(按 Codex Repo 规则枚举 `<repo>/.agents/skills` 取 canonical),不再按仓库前缀(仓库内非 skill 目录被用户链接指向时照样禁;产品 skill 链到仓库外也能正确排除);② doctor 严格按生成器格式判块内条目——带 `name` / 额外键 / 后置 `enabled = true` 的条目一律 malformed(Codex 按顺序应用 selector,后置 name 规则会撤销前面的禁用);③ `verifyCalcs` 递归比较 output 的 display 投影(四锚子 display 被删 / 改即不一致;旧记录无 display 不误报);④ `claimTokens` 改为"带小数点的小数字必查、纯整数叙述跳过"(display 写法 `2.00 年` 必绑定,"近 5 年 / 连续 3 期"不误红),撤回 r2 的"年 / 期 进白名单";⑤ 块外冲突检测跳过多行字符串;⑥ 目录上限按唯一 canonical 目录计数。**核实后不采纳并说明**:兄弟符号链接——Codex 整根按 canonical 全局去重只遍历其一(`exec-server/src/local_file_system.rs visited_directories`),我们两条都禁是安全超集(多余 selector 无害),r2 "两条各算一个"的说法有误、r3 对,实现保留、文档改正;TOML AST / 写前整文档解析、复刻 2,000 / 20,000 截断、事件与 CLI 里的 `config_toml` 绝对路径与异常信息里的 `~/.agents/skills/<名>`——维持不采纳(产品 CODEX_HOME/config.toml 只有 init / 编排器 / Codex 写;Codex 截断只会少看到 skill;用户需要知道哪个目录不可读才能修);"报告逐字照抄 display"的验收检查列为后续项(硬测试 harness 扩展,非本批范围)。
- Codex 审查 r4(闭环确认,2 P1 / 若干 P2)→ 采纳并修:① 枚举改为与 Codex 同构的**排序 BFS**,在 2,000 唯一目录 / 20,000 条目处 **截断继续**(`truncated` 写进事件与 manifest,不再抛错——用户 skill 目录里有 `node_modules` 时原先会打断整个运行);② 写入前用 Python `tomllib` 解析合并后的整份 config.toml,不过不写(零新依赖,产品本就带 venv;python 不可用或无 tomllib 时记 `toml_validated: null`);③ 多行字符串判定改为引号感知(`x = 'Use """ here'` 不再被当成多行开头);④ doctor 块解析支持引号 / 转义键(`"name"` / `"na\u006de"` 不再静默跳过 → malformed),非键值行也算偏离;⑤ `verifyCalcs` 改比整份**结果投影**(顶层 + details 子结果的 status / unit / value / display),只在 calc_version 相同时比;⑥ `claimTokens` 把 `期` 加回白名单(quarterize 的 "11 期" 必须绑定);⑦ 硬测试第 5 组新增「报告派生数字逐字照抄 calc display」(`judgeDisplayFidelity`:带小数点或带单位的数字必须逐字等于所引用 calc 的 display / 子 display,或绑定 evidence 原值;叙述整数豁免;年 / 期 的整数必须匹配 display——"消化 30 年"这类把锚点写成年数的错误能抓住;旧运行无 display 自动跳过)。审查员本轮接受 A(兄弟链接超集)与 D(路径保留)两项不采纳。
- calc 0.3.2:`cli.py` 落盘前给 `output` 附确定性 `display` 展示字符串(`calc/display.py`:|x|≥1 留 2 位小数、|x|<1 留 4 位有效数字;`小数`→百分比、金额按量级换算到亿元/万元/元、`期` 取整;status ≠ ok 为 null),report 阶段提示词改为"派生数字照抄 display、不抄原始浮点、不自己换算";schema `output.display` 可选(旧记录兼容);`value` 仍是复算 / 绑定校验真理源,公式层不变。修的是验收报告 §6-8 / §8-7 点名的"报告数字 15 位浮点"。
- risk 阶段提示词:明确 `primary_source` 是 `fetch_kline.json` 顶层信封字段(实测 agent 去 `extra` 里找、找不到就跳过技术指标)。

### SOP skills 补齐 + calc 0.3.1(2026-08-22)
- `.agents/skills/` 新增四个口径 skill:`valuation`(三口径 PE / 前瞻 vs TTM 交叉验证 / 一致预期分歧 / 四锚消化年数与 30 倍锚三铁律 / 判读与常见错误)、`earnings-analysis`(累计 → 单季 → TTM 口径地图 / 扣非 vs 归母 / 三表交叉 / 比率经 calc)、`industry-chain`(六步下钻 × 每层四问 / 物理硬筛子 / 不可替代性标签 / 预期差四问校准)、`catalyst-risk`(催化剂四类 / 风险十类与反证 / 裁决点标准写法 / 旧结论处理);company-research SOP 加按阶段加载指引。全部从方法论母本脱敏翻译:不含任何持仓 / 个人结论 / 推荐,不给投资动作建议。
- calc 0.3.1:新增 `ratio(numerator, denominator, label, unit_in)`(同单位两数之比,分母 ≤ 0 → not_meaningful,不做单位换算);19 个函数,128 测试。

### scripts/init · scripts/doctor(2026-08-22)
- `orchestrator/src/init.ts`:幂等初始化产品私有层 `.local/`(目录 / `config.json` 骨架 / `.gitignore`),不碰 `~/.codex`;`--force` 先备份再重写。
- `orchestrator/src/doctor.ts`:15 项体检(引擎二进制与版本锚定 / 登录态或 api_key 环境变量 / 宪法 / skills / Python 依赖 / calc / 注册表 / 写权限 / `.gitignore` / 密钥扫描 / `api.token` 权限 / `--net` 数据源连通),中文修复提示,报告写 `.local/doctor/`,退出码 0 / 2 / 3。
- 正式文档:`README.md`(中文主档)+ `README_en.md` + `CHANGELOG.md` + `docs/model-access.md`。

### Phase 1 M4 — 模型接入(2026-08-22)
- `providers/<id>.json` provider 模板:openai / deepseek / qwen / glm / kimi(只引用环境变量名);`validateProfile` 组合约束(第三方必须显式 https base_url、只能 api_key、responses_support 与 wire_api 自洽、`verified_at`)。
- `orchestrator/src/providers.ts`:模板 → Codex `model_provider` / `model_providers.<id>` 映射;密钥只按 `env_key` 名透传;providers 目录符号链接 / 越界拒读。
- 选择方式:`--provider <id>` / `VRA_PROVIDER` / `.local/config.json provider.profile`;`--auth` / `VRA_PROVIDER_AUTH`;用户未显式写 auth 时自动按模板选择。
- `provider_matrix.ts`:10 项兼容矩阵 harness(严判口径、并发观察、落盘前脱敏、`judge()` 单测);openai 基线 9 pass · 1 n/a。
- HTTP API 薄 UI:`/login?token` → cookie(HttpOnly / SameSite=Strict)→ `/ui` 运行列表 / `/ui/runs/:id`;cookie 只对白名单只读 GET 有效;统一安全响应头;非本机绑定关闭 cookie 登录。
- Codex 独立审查四轮闭环(12 → 6 → 1 → 0)。

### Phase 1 M3 — 服务层与接口(2026-08-22)
- `service.ts`(闭合输入校验 / `safePath` / 最小子进程环境 / 相对路径 + 脱敏)→ `mcp.ts`(stdio MCP server,8 工具)、`api.ts`(本机 HTTP,每请求鉴权:Bearer 或本机登录 cookie,+ 反 CSRF)、`batch.ts`(多标的顺序研究 + 只转录汇总)、`alerts.ts`(两次运行证据按事实键含来源对齐,只并列两值)。
- calc 0.3.0:`technical_indicators` / `chip_distribution` + CLI `history_json` 序列输入;取数层计算型端点退出阶段计划。
- Codex 审查三轮闭环(M3)+ 三轮闭环(calc 0.3.0)。

### Phase 1 M2 — 知识层与查看器(2026-08-22)
- `knowledge.ts`:运行自动归档到 `.local/knowledge/`(合规 + 私密两道 gate),下次默认召回(数据边界、新鲜度、按章节截断),各阶段 `knowledge_conflicts` 裁决。
- `viewer.ts`:自包含 `viewer.html` + `report_appendix.md`。
- 各阶段可选 `extra_findings`(按阶段 topic 枚举);SOP §6 扩展数据使用规则。

### Phase 1 M1 — 数据源全量接入(2026-08-22)
- `datasources/registry.json` 104 端点 / 23 层(CN / US / HK)+ `fetch_endpoint.py` 通用取数器 + 20 个源模块 + mapper 约定(单位 / 币种按源原样、record_key 唯一、逐请求绑定 raw、取数层零派生计算)+ `CATALOG.md` 自动生成 + `health.py` 巡检。
- 编排器阶段计划由注册表推导(`--endpoints full|core`);validator 新增 raw_ref / 账本自洽 / 反向文件检查等不变量。
- Codex 审查五轮闭环。

### Phase 0 — 骨架与硬测试(2026-08-21 → 2026-08-22)
- 金融研究宪法 `AGENTS.md`、`data-access` / `company-research` skills、`calc/` 0.2、薄编排器 v0.4(TS SDK 0.149.0,零 fork)+ validator(语义槽位 v2)+ 合规 gate + hooks v0(Stop / PreToolUse)+ 产品配置分层(v2.1 §5)+ 硬测试 harness(六组 + 钩子七项 16/16)。
- 试金石标的:中际旭创 300308。
