# Vibe-Research v1.0.3 个股研究升级事故 · 诊断记录

## 2026-09-05 傍晚 · 真根因锁定 + 修复（推翻"非确定性/模型弱"的早期结论）

### 最终结论（重要更正）

**早期（凌晨~下午）我判断剩余失败是"模型非确定性/需要更强模型"——这是错的，是用外部归因搪塞。**
傍晚扒 163509 run 的 hooks.log + validator 事件时间线后，真根因是**确定性的 harness bug**，已修复（PR #46）：

1. **Stop 钩子收工预算误杀**：`MAX_STOP_BLOCKS=2`。模型按 SOP 攒 3-5 轮 calc（quarterize→latest_quarter→ttm_sum→ttm_yoy→qoq，每轮数据全对）才在最后一步写 stage 文件；每轮"想收工"触发 Stop 记一次 block，第 3 轮还在做 calc 就被强杀 → stage 文件没写完本轮就终止。
2. **编排器从不回头复核**：被杀轮次的 stage 文件在**后续阶段的轮次**里被模型补写落盘（163509 里 financials.json 16:45 落盘 status=complete、estimates.json 16:46 落盘 status=complete——磁盘证据在），但主循环只校验"当前阶段的当前轮"，账本与磁盘脱节 → 阶段永久 failed。

"看起来像非确定性"的原因：补写发生在哪个后续阶段、补写后还有没有轮次，随每次 run 的节奏漂移——但**机制本身 100% 确定**。

### 修复（PR #46，分支 fix/stage-recovery-and-stop-budget）

- `orchestrate.ts`：主循环后加**阶段终复核（恢复路径）**——对非 complete 阶段按原顺序重跑 validateStage+受保护产物+calc DAG，过则认领升级。安全边界：只认领"失败原因纯属收工时机"的阶段；行为违规/turn 失败/契约违约不恢复；不重跑跨阶段 checkAgentTrace；report 不碰。
- `hooks.ts`：`MAX_STOP_BLOCKS` 2→6（"连续空转"语义）。
- `stop.ts`：**推进感知**——两次拦截之间 calcs/ 有新落盘 ⇒ 在干活，不占空转额度；真空转 6 次才终止。

测试：新增恢复路径+安全边界双场景，orchestrate 23/23、hooks 5/5；全量 572 测试 6 fail 为存量（本地模板+端点白名单，基线同样）。

### 历史修复清单（此前 4 个确定性 bug，全部完成）

| 修复 | 文件 | 状态 |
|------|------|------|
| validator TypeError 判死 run | merge.ts + validator.ts | PR #45 |
| 数字忠实度误杀可溯源引用 | number_fidelity.ts | PR #42（review 收口：小数 1e-6）|
| em_stock_news 缺 raw_ref + 东财反爬 | mappers.py + eastmoney.py | PR #43（review 收口：JSONP 主/akshare 兜底）|
| _atomic_write 缺目录 | server.py | PR #8（已合并）|
| **收工预算误杀 + 账本不复核** | **stop.ts + hooks.ts + orchestrate.ts** | **PR #46（本文）** |

### 验证结果（两次真实茅台 run，非单测）

| run | 代码 | profile | financials | estimates | valuation | risk | report | 终态 |
|-----|------|---------|-----------|-----------|-----------|------|--------|------|
| 163509 | 旧（仅 #42/#43/#45） | incomplete | **failed** | **failed** | **failed** | **failed** | failed·缺产物 | **failed exit=3** |
| 030922 | 带 PR #46 全部修复 | complete | complete | complete | complete | complete(3) | complete(2) | **complete exit=0 gate ok** |

030922：807 条 evidence、20 个 calculation、report.md 165 行（每个数字挂真实 ev-/calc- 引用）。
report 数字忠实度收敛 att=1 有 2 违规 → att=2 归 0（"提示该引谁"修复生效）。
**结论：#46 的三个修复（终复核恢复路径 + Stop 推进感知/预算 6 + 忠实度违规提示）把六阶段 run 从"5 阶段锁死 failed"推到"全 complete"，闭环达成。**

## —— 以下为过程记录（早期结论"非确定性/模型弱"已被上方推翻）——

## 2026-09-05 下午 · 最终判定（run 20260905-163509-600519，带全部确定性修复 version a007a9b）

### 结论速览

- **确定性 bug 已全部修完并验证**（4 个，见下方"修复清单"）：validator TypeError、数字忠实度误杀、
  em_stock_news 缺 raw_ref、东财反爬取数。
- **最终 run（163509）仍 failed，且这次连 profile 都 incomplete** —— 比前几次更糟。
  同一套代码、同一只茅台，004305 那次前 4 阶段全 complete 并写出了 report.md，163509 这次 profile 就没过。
  **这是决定性证据：剩余失败是非确定性的**，不是某个能确定性修掉的代码 bug。
- 根因收敛：Qwen3.8-27B 在 9 万+ token 的多轮 agent 循环里，"每个阶段写 stage 文件"这个动作
  时成时不成（有时 3 次 attempt 内写完、有时写不完就被 Stop 钩子判死）。单发能力 24/24 实验全过，
  问题在长循环的稳定性，不在单条推理。

### 10 次 run 全记录（茅台为主）

| # | 时间 | 代码 | 死点 | 性质 |
|---|------|------|------|------|
| 1-4 | 8/29-8/30 | 升级前 | report 成稿 | 数字校验误杀（已修 #42）|
| 5 | 9/2 | v1.0.3 | report 成稿 | 同上（前 5 阶段全过）|
| 6-8 | 9/3-9/4 | +我的PR | financials 缺产物 | ①validator崩溃(已修#45)②非确定性 |
| 9 | 9/5 00:09 | validator已修 | financials 崩溃 | **validator崩溃实锤**(已修) |
| 10a | 9/5 00:43 | validator已修 | risk(raw_ref) | **前4阶段首次全过、report.md写出**（raw_ref已修）|
| 10b | 9/5 11:12 | 全部已修 | 卡回 financials | 非确定性 |
| 10c | 9/5 16:35 | 全部已修(a007a9b) | profile 都没过 | 非确定性 |

### 修复清单（全部完成 + 本地验证 + 提PR/等review）

| 修复 | 文件 | 验证 | 状态 |
|------|------|------|------|
| validator TypeError 判死 run | merge.ts(loadCalcs按calculation_id过滤) + validator.ts(output防御) | 崩溃run重放 ok=true、18/18测试 | PR #45 |
| 数字忠实度误杀可溯源引用 | number_fidelity.ts | 9/105→1/105、29/29测试 | PR #42（review收口:小数1e-6）|
| em_stock_news 缺 raw_ref | mappers.py(SDK源extracted落盘) | 10条证据raw_ref全补齐 | 随PR #43 |
| 东财反爬取数恒空 | eastmoney.py(JSONP优先/akshare兜底) | 境内被拦→自动回落10条 | PR #43（review收口:主备反转）|

本地 main 已全部合入（commit a007a9b + 0518293）。

### 待老板决策（非确定性失败，超出确定性修复范围）

1. **强模型**：这套 Codex harness 六阶段流程是给 GPT-5 级模型设计的。Qwen3.8-27B 在
   9万+ token 多轮循环里写 stage 文件时成时不成。换上更强/更稳的模型（MiMo/GLM/DeepSeek 任一
   key）大概率稳定走通——这是治本方向。
2. **报上游 issue**：给 simonlin1212 附这 10 次 run 数据 + "Qwen 27B 六阶段 agent 循环非确定性失败"
   的结论，这是他产品架构层面的事（默认模型选型/上下文管理）。
3. **接受现状**：确定性 bug 已清零，茅台这种干净标的偶尔能走完（10a 那次证明了），
   复杂标的/多轮循环不稳。重试几次有概率过。

## —— 以下为凌晨 cron 写的初版诊断（问题A/B已修，保留作过程记录）——

## 2026-09-05 凌晨 · 带修复重跑（run 20260905-004305-600519）仍 failed —— 两个新问题，均非原崩溃模式

### 结论速览

- **原事故两个 bug 已确认消失**：本次 run 无 validateStage TypeError（calcs/ 参数文件问题未再触发），数字忠实度校验从"大面积误杀"降到 1/89。
- **不是"零命令 turn"模型退化**：14 个 turn 全部有真实命令（1–11 条），模型在主动干活（risk 阶段甚至自己 grep 定位缺 raw_ref 的证据）。
- **本次 failed 由两个新问题独立导致**（见下 A、B），风险面比原事故小：数据/计算全部正常，824 条 evidence、20 个 calculation，gate ok，取数 44 项中 41 ok。
- 未强行修复，留给老板决策。

### 阶段状态（manifest）

| 阶段 | 状态 | attempts | 说明 |
|------|------|----------|------|
| profile | complete | 1 | |
| financials | complete | 2 | 第 1 次被 stop 钩子拦（缺产物），重试过 |
| estimates | complete | 2 | 同上 |
| valuation | complete | 3 | 同上 |
| risk | **failed** | 3 | 10 条 em_stock_news evidence 缺 raw_ref，stages/risk.json 始终没写出 |
| report | **failed** | 3 | 同 raw_ref 问题 + 数字忠实度 1/89 误杀（"-83.83"） |

### 问题 A（主因）：em_stock_news 脚本不落地 raw，10 条 evidence raw_ref=null

- 时间线（.vibe/hooks.log + events.jsonl）：
  - 00:52:44 fetch/em_stock_news.json 生成，status=ok，10 条新闻，但 `extra.raw_files=[]`、每条 evidence `raw_ref=null`（fetch_ledger 记 raw_files=0）。
  - 00:54:23 risk attempt 1 validator：`ev-xxx 缺 raw_ref:每条证据必须指向本次 raw/ 内的原始响应(AGENTS.md §4 / §5)` ×10，ok=false。
  - 00:54:42 模型自己 grep 全部 fetch/*.json 定位到这 10 条都在 em_stock_news.json——定位正确，但修不了：raw 数据是取数脚本该落盘的东西，模型不能事后伪造。
  - 00:55:00 / 00:55:30 attempt 2/3 同样拦截，stop 钩子 2 次提醒后终止，stages/risk.json 未写出 → risk failed。
  - report 阶段继承同样的 10 条坏证据，3 次 attempt 全部被拦。
- 根因定位：`.agents/skills/data-access/scripts/sources/eastmoney.py` 的 em_stock_news 在 commit 0518293（09-03，"改走 akshare"）中，`_stock_news_via_akshare` 路径成功返回数据但**没有把原始响应写入 raw/**（`raw_binding=single` 却 `raw_files=[]`）。akshare 返回的是 DataFrame，脚本直接映射成 evidence 字段，跳过了 raw 落盘。
- 影响面：任何走 akshare 路径成功的 run，risk/report 阶段必挂（validator 的 raw_ref 硬校验）。JSONP 兜底路径（原始实现）会写 raw，所以"恒空→failed"和"有数据→raw_ref 拦截"是同一脚本的两个互斥失败模式——修了取数、漏了证据链。
- 修复方向（未执行，供决策）：`_stock_news_via_akshare` 成功后把 akshare 原始返回序列化为 `raw/em_stock_news_*.json` 并回填 `raw_ref`；或 em() 层对 akshare 分支做统一的 raw 绑定。改完用 600519 单端点验证 10 条 evidence 全带 raw_ref。

### 问题 B（次因，误杀残留）：数字忠实度把区间 "69.14-83.83" 拆出负数 "-83.83"

- report.md 第 72 行：`| 前瞻 CAGR(机构数·区间) | 5.47%(44-48家,FY2028 69.14-83.83) | calc-e45da36a4090a6a7 / ev-af5149d3d078 / ev-99d76cd09f28 / ev-c23232a2bdec |`
- 69.14 = ev-99d76cd09f28 (eps_consensus_min)，83.83 = ev-c23232a2bdec (eps_consensus_max)，**两个数都在 evidence 里有真实 raw_ref**（同一条 raw/ths_basic.10jqka.com.cn_worth.html...）。报告本身没错。
- 但 number_fidelity 的数值抽取把区间分隔符 `-` 当成负号，提取出 **-83.83**，而 calc-e45da36a4090a6a7（forward_cagr=5.47%）与证据里都没有 "-83.83" 这个负值 → 判 1/89 对不上。
- 注意 PR #42（HEAD f41e7dc，09-04）修的是"可溯源引用被误杀"，本例是**同文件的另一个抽取 bug**：范围/区间模式 `a-b`（无空格、a/b 都是正数）应识别为两个数而非一个负数。本次 88/89 通过，只这一处卡壳。
- 修复方向（未执行）：number_fidelity 数值抽取正则对 `[0-9]+(?:\.[0-9]+)?-[0-9]+(?:\.[0-9]+)?` 形态按区间拆成两个正数处理（或要求负号前后有运算上下文才算负数）。fixture 加 "69.14-83.83" 用例。
- 依赖关系：问题 B 只有在报告写了区间且该区间上界恰好被引用行校验时才触发；问题 A 修好后 report 会重跑，B 大概率仍会命中（该行写法是宪法 §3 标准产出列要求的格式），两个都修才能真正闭环。

### 待办（老板决策）

1. 修 eastmoney.py em_stock_news akshare 分支的 raw 落盘（问题 A，P0，不修则全市场 6 阶段 run 的 risk/report 必挂）。
2. 修 number_fidelity 区间抽取（问题 B，P1，单独看是误杀残留）。
3. 原事故 validator 崩溃修复（merge.ts loadCalcs 过滤 + validator.ts output 防御）仍在工作区未提交、未进 PR——本次重跑虽未复现 TypeError，但修复本身是正确且必要的（000937 run 有完整崩溃证据），建议照常走 fix/validator-calcs-args-crash 分支提 PR。18/18 测试已过。
4. 三个修复落地后重跑 600519 全 6 阶段验证闭环。
