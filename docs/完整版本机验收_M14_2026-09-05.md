# M14 本机功能闭环与验收（外部条件待补，未提交）

唯一活动源码仍为 `vibe-research-agent-Codex`，分支 `feat/dual-engine`，HEAD `2ed80ac`。
安装验收临时副本不是另一条开发线。没有 push、tag 或 Release。
2026-09-05 后继经 Simon 明确授权追加 M14 记忆更新条目，并同步父目录配置与详细开发日志；
只整理已有证据，没有新增代码提交或重新运行六阶段验收。

## 已完成

1. Codex 对话同会话在途锁：第二条请求在模型调用前被拒绝，不同会话可用；正常返回、抛错、
   `turn.failed` 与流式取消后均可重试。定向回归及独立 Codex 复审通过。
2. 持仓截图／表格人工确认入口：真实浏览器、生产后端、WorkBuddy 与合成截图走完最后保存，
   不自动写入、不重复叠加。详细范围见 [跨来源验收](跨来源业务验收_M12_2026-09-05.md)。
3. 安装依赖补齐 `mootdx==0.11.7`，体检实际导入 `mootdx.quotes`；干净 Python 3.12 环境
   全部依赖安装成功。版本与依赖核对来自 [PyPI 元数据](https://pypi.org/pypi/mootdx/json)。
4. 大资料库先排名再提取最终片段；同步入口保留，HTTP 对话用每八份资料让出事件循环的异步入口，
   取消在模型调用前可终止检索。同一实现保留全部授权范围、版本核验、排名和页码；两轮独立复审通过。
5. 真实 WorkBuddy 研究取消：独立 core / profile 测试等待研究 worker、订阅 CLI、实际受控 MCP
   三个进程启动，再请求中止；1,856 ms 后显示 cancelled，三个已观察进程均退出，五份取数文件哈希未变，
   没有最终报告。不是服务端停止计费的证明，也不是 Windows 取消验收。
6. 根路由中文恢复页：复用 [React Router 的错误边界机制](https://reactrouter.com/how-to/error-boundary)，
   区分 404 与页面异常，提供重新加载/返回首页，不回显任意错误正文、不清空配置，并提示未保存输入风险。
   真实路由渲染覆盖 404、500、Error 与任意抛值；Chrome 验证两项操作、配置哈希不变、标题焦点和 375px 无溢出。
   一轮独立 Codex 复审无可行动回归。此项改善原有默认错误页，不声称此前必然白屏，不替代启动连接或表单错误处理。
7. 计算工具引用前置校验：复用落盘计算契约的证据/计算 id 格式，在访问文件或启动计算器前拒绝错误格式，
   MCP 与直连的工具 schema 同步生成。只验证格式，不声称 id 已存在；省略/空数组及真实计算失败记录的原有行为不变。
   新用例修前失败、修后通过；一轮独立 Codex 复审无可行动回归。
8. 报告放行：最终结束时间、状态/退出码一致且无已记录校验错误时，才经 HTTP/MCP 返回报告、附录及内嵌
   报告的 viewer；运行中、失败、取消和损坏终态均不展示草稿。草稿字节原样保留在本地供排查。
   兼容旧 manifest 的可选字段；这是编排器终态记录，不是防本地篡改的签名。资料不完整的合格报告仍开放并提示缺口。
   回归覆盖状态矩阵、真实 HTTP/stdio MCP、原件保留、前端渲染；Chrome 实际点开失败/不完整归档验证隐藏与可读。
9. 变化提醒数据隔离：浏览器独立数据根曾混入默认目录的旧研究，并在默认目录生成比较缓存。修复为显式传递
   当前 dataRoot，HTTP 查询只读，CLI 仍可归档；选中输入和输出路径检查符号链接与穿越。红绿测试及浏览器复验
   证明不再混入旧目录。验收没有修改原研究文件；此前误生成的比较缓存未擅自删除。
   第 8 项独立审查与第 9 项后继增量复审均为 `No actionable regressions`。

## 规模与回归

- 合成资料 500 份，每份约 46,275 字符；五条命中结果逐字段与改前完全一致。
- 旧同步扫描初测 1.6–2 秒。后继对照受本机其他任务负载影响，不能据此承诺固定倍数提速。
- 修后异步扫描一次 1,227 ms，其间 10 ms 心跳执行 62 次、最长间隔 39 ms；证明本次不再整段独占事件循环，
  不证明任意规模、单份异常巨文件或多请求并发均有该延迟上限。
- 后端串行 795 项：794 通过、1 Windows ACL 跳过；前端 51/51；两端类型检查和前端构建通过。
  Python 最新全量 699/699，本批后继只改安装依赖声明，未改 Python 计算实现。
- 前端主包约 1.30 MB，构建体积警告保留，不冒充无警告。
- Gitleaks 对当前公开文件快照扫描约 5.2 MB，20 个命中均与已提交基线相同行；本轮无新增命中。
  干净安装产品体检的私密内容扫描通过。此结论不是重新审计完整 Git 历史或外部依赖。

## 干净安装与首次使用

从当前源码清单复制到无依赖、无产品数据的临时目录，运行实际 setup，重新安装 Python/npm 依赖并初始化。
实际 start 启动 8765 / 5930，就绪检查成功；真实 Chrome 首次进入 AI 接入页。
产品 Codex 未登录、本机 Claude / WorkBuddy 可用；选择 WorkBuddy → 测试并保存成功 → Agent 默认开启。
订阅来源不能关闭 Agent，页面说明直连需要经验证的 API。未复制用户 Codex 凭据。
离线体检 17 ok、1 warn（产品 Codex 未登录）、0 fail、2 skip（未运行联网检查）。
关停后两端口无监听。最终检索与辩论修复已同步进同一临时验收副本，重新启动后 API、UI、UI 代理健康请求
均为 200；随后关停，8765 / 5930 / 独立测试页 5971 均无监听。
报告访问及提醒隔离补丁也已同步到该干净副本，再次实际 start；API/UI/UI 代理/新报告组件均返回 200，
不存在运行的报告接口返回 missing 且正文/附录为 null。完成检查后关停，不复制个人凭据。

## 117 端点全量体检

本次逐项实际执行 117 个注册端点：91 ok、9 partial、17 failed。不是 117 项全绿。

- 五个 mootdx 端点缺包：安装遗漏已修；干净依赖环境复测为三项网络失败、两项超时，不能声称行情已可达。
- 两个问财、六个 SEC 端点缺用户授权或 SEC 联系身份配置；不从个人私有环境偷带配置。
- 百度访问拒绝、申万证书链错误：保留失败，未关闭 TLS 校验。
- 交易日历一次连接状态失败，串行复测正常，未基于一次瞬断更改取数逻辑。
- Nasdaq 财报日页面返回无法正向识别的内容，继续明确失败，不从不明文案猜日期。
- 九个部分可用含非交易日、未挂产业标签、源数据范围限制等，原始原因保留。

运行产物 `.local/health/m14-full-20260905`、`m14-targeted-r2`、`m14-mootdx-r2`。
全端点诊断和单标的 `endpoint_scope=full` 是两种不同验收，不相互替代。

## 修后六阶段与最终集成审查

- Claude 六阶段、full 范围运行 `m14-claude-full-1` 运行 34 分 50 秒，在风险阶段因 `agent_quota` 失败，
  manifest 为 failed，未生成最终报告。39 份端点信封有 1,026 个不同证据 id，23 个落盘计算记录为 ok；
  这些计数不代表整场完成，也不含被修正覆盖的尝试记录，失败尝试另在事件流中保留。
- 公司画像、一致预期和估值阶段 complete，财务 incomplete（缺历史累计资料期）；风险和最终报告未完成。
- 新开独立 `m14-workbuddy-full-1`，仍使用 full 范围、六阶段，且使用含 mootdx 的干净依赖环境。
  这不是修改 Claude 失败记录或同一任务的静默回退；该运行已于 17:56:28 完成，总耗时 112 分 11 秒。
  六阶段全部 complete、validator_ok=true，最终 complete / exit 0；画像经三次尝试才通过；
  风险第一次尝试用时 22 分 51 秒，有两份计算变更但缺少 `stages/risk.json`；第二次 28 分 10 秒，产出风险文件，
  但一份计算引用格式及两处表述被校验拒绝；第三次 2 分 50 秒修正并通过。报告前两次数字绑定被拒，第三次通过，
  没有放宽 validator 或删掉失败事件。现有证据不足以断定第一次缺文件的根因。
  最终 1,011 条证据、23 条计算、0 冲突，final_errors=[]、gate.ok=true / hits=[]、quote_decision=normal，
  test_scenario=false；14 节正式报告、附录、查看器及知识档案均存在；温度计追加 24 条，跳过 1 条。
  新报告接口实读该结果为 ready，正文 16,487 字符、附录 224,590 字符。此结果不是所有外部数据源全可达，
  也不是模型语义零错误的证明；报告中的缺口保留。约 112 分钟与多轮修正是本轮真实用户成本，未声称已提速。
  上述引用问题另促成工具前置校验补丁，发生在本次六阶段运行已经开始之后；该补丁单独通过红绿测试和独立复审，
  不把先前阶段实跑宣传成补丁后的完整重跑，也不据此声称本轮耗时已改善。
  报告访问与提醒隔离补丁在本次实跑结束后新增，分别经回归、真实浏览器和独立复审；没有再耗时两小时重跑模型。
- 跨模块集成审查确认一个 P2：停止未确认被取消信号覆盖。已补回归和修复；复审又发现前端将该结果误报
  成全场失败，已同步保留错误、已完成内容并禁止失败终态自动归档。最后实际 Codex 复审返回
  `No actionable regressions`，792 项后端与当时 49 项前端全量回归覆盖该修复，新增恢复页后前端再跑为 50/50。

## 仍需外部验收条件

- Windows 原生实机无可用环境；本机的脚本/探针测试不替代 Windows 真机。
- 不同 API 厂商的真实兼容矩阵仍依赖各自授权，不把某个订阅路径成功推广为所有 API 都通过。
- Claude 最新 full 验收仍为额度失败，待额度恢复；旧成功与本轮失败并列保留。

外部条件不是全部待办：112 分钟研究耗时尚未改善，最新全部补丁后的六阶段端到端复跑也尚未进行。
以上两项应单独跟踪，不能把“本机功能闭环”写成客户完整版已全部验收。

只有以上边界逐项核对后才能定义对外验收范围；本地开发、提交、推送与发布分别授权。

### 验收装置本身的修正

真实取消首轮只按命令行包含 MCP 路径判“工具已启动”，会误认 CLI 配置字符串；那轮只能证明 worker 与
CLI 退出，不计入三进程结论。第二轮改为精确匹配实际 MCP 启动命令且要求三个进程，实跑通过，产物
`.local/reviews/m14-live-research-cancel-r2.json`。首轮保留，不用它证明 MCP 退出。
