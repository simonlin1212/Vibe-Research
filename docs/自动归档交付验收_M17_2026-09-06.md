# M17 自动归档与页面交付验收

本批继续 M16 的开发优先策略：Spark 优先，不可用时 GPT-5.6；不使用 Claude Code / WorkBuddy 做模型实跑。
活动源码仍为 `vibe-research-agent-Codex`，`feat/dual-engine`，HEAD `2ed80ac`；未提交、未推送、未发布。

## 范围与隔离

- 独立数据根 `.local/m17-spark-delivery`，保留两次真实运行；没有读取 M16 的产物冒充新运行。
- 同一产品自己的 `.local/codex-home` 登录，捆绑 Codex 0.149.0；不复制个人凭据，不修改客户默认模型。
- `300308.SZ`、六阶段、`core` 端点范围、`controlled_mcp`、medium、每阶段最多一次重试。
- `--knowledge off` 仅关闭召回；本批没有 `--no-archive`，必须由生产收尾流程生成查看器、附录和知识归档。
- 独立 React + 生产 API 测试服务位于 `127.0.0.1:5973`；正常 5930 / 8765 服务不在本批控制范围内。
- 该独立浏览器 origin 预置无密钥 `cli-codex` 配置，仅用于打开报告页，不代表设置页登录/测试保存已验收。

## Spark 失败保留

`m17-spark-delivery-1`：08:48:55.803 至 08:50:26.733，画像通过，后续阶段收到明确的
`You've hit your usage limit for GPT-5.3-Codex-Spark`，最终 failed / exit 3。
这不是模型不存在、资料源失败或结构校验失败；按 Simon 已授权备用方案启动新的 GPT-5.6 Sol 运行。
事件原文保存在 `.local/m17-spark-delivery/runs/m17-spark-delivery-1/events.jsonl`。

该失败运行虽然生成了诊断用查看器/附录，但没有知识归档。正式报告 HTTP 接口返回
`availability=missing`、`run_status=failed`，正文和附录均为 null。真实浏览器显示无报告提示，
已校验报告 article 数为 0，没有把失败运行包装成正式报告。

## GPT-5.6 与交付链

运行 `m17-gpt56-delivery-1`；实际引擎 session 的 `turn_context.model` 已核对为 `gpt-5.6-sol`。
08:52:00.926 至 09:02:07.116，共 606.190 秒（10 分 6 秒），complete / exit 0。
120 条证据、19 项计算（全部 output.status=ok），六阶段 validator_ok=true，最终错误为空、gate 通过。
仅估值阶段补跑一次：首次 extra_findings 中引用的计算未登记进顶层 calculation_ids，既有校验拦住，
模型修正后通过；未降低校验要求。其他五阶段各一次通过。

自动产物（本批未手动调用渲染器补文件）：

| 文件 | 字节数 |
| --- | ---: |
| 运行目录 viewer.html | 113608 |
| 运行目录 report_appendix.md | 31388 |
| knowledge/companies/SZ_300308/latest.md | 11447 |
| knowledge/companies/SZ_300308/runs/m17-gpt56-delivery-1.md | 11447 |

manifest.viewer、manifest.knowledge_archived 均在位，知识归档 gate_removed=0。
详细断言结果：`.local/reviews/m17-delivery-artifacts.json`。

真实 React 页面验收：

- 从归档点击本次运行，报告和证据附录均显示；HTTP 200 / ready，正文 5738 字符、附录 28080 字符。
- 正文 84 处可点击引用逐个核实目标存在，缺失目标为 0；点击计算引用可展开附录并聚焦对应行。
- 重新聚焦真实证据链接后，通过 CDP 原生 Enter 键事件验收，附录展开且 document.activeElement
  为同一次运行对应证据行。早期测试错误使用 `ENTER`，另一次未重新确认焦点，均未计入通过；
  最终测试在同一调用中明确选中链接后发送 rawKeyDown/keyUp，日志 `m17-browser-keyboard-final.log`。
- 刷新页面后回归档重新点击，可再次看到同一报告和附录，不代表刷新后自动恢复选中项。
- 深色 1254 px 和浅色 1440 px 截图已人工检查；1440 px 为报告/附录双栏，390 px 为窄屏布局，
  document 横向溢出均为 false。未进行完整 WCAG 或全部浏览器审计。
- 当前页面观察窗口内没有捕获到 error / unhandledrejection；没有 Vite 错误遮罩。

证据日志：`m17-browser-citations.log`、`m17-browser-keyboard-final.log`、
`m17-browser-responsive.log`、`m17-browser-reload.log`，均在 `.local/reviews/`。
截图 `m17-report-dark.png`、`m17-report-light-wide.png`、`m17-report-citation.png` 同目录。
测试结束移除该 origin 的无密钥 AI 测试配置，关闭本批创建的 Chrome 标签页和测试服务器；真实研究进程已 exit 0。

## 页面兼容与本地回归

浏览器发现同一标的两行都只显示名称和代码，不便区分运行。曾试作状态/时间补充与组件测试，
但现有 `ui_boundary` 明确约束只显示名称和代码，因此撤回本批全部产品改动与新增组件测试，
没有放宽旧边界测试。已向 Simon 询问是否允许补充状态和时间，未确认前维持既定界面。
临时改动的 61 项测试及独立 Codex 审查记录只对应撤回方案，不是最终交付内容。

撤回后最终状态：前端 60/60；界面边界 12/12；知识归档、查看器与取消测试 24/24；
前端类型检查与构建成功。既有大于 500 kB 的构建警告仍存在。
本批未重跑全部后端和 Python，不能把 M16 历史数字当成本批全量回归。

日志：`.local/reviews/m17-desktop-tests-final.log`、`m17-ui-boundary-final.log`、
`m17-archive-regression.log`、`m17-build-final.log`。测试服务复用 M14 辅助脚本结构，
认证随机生成、只留服务端；该脚本经实际 Codex 范围审查未报可行动回归。

## 尚未覆盖

全 117 端点、Windows 原生运行、其他模型适配器矩阵、跨日研究质量与收益均不在本批通过范围。
Spark 限额后编排器仍尝试后续阶段，可作为后续体验改进候选，不能宣称已经修复。

## 自评（本批范围，非发布判定）

准确性 4：真实产物、自动归档与页面链路有证据；不覆盖研究内容的独立财务审计。
完整性 4：本批成功和失败交付路径均验到；首次接入没有重新实跑，full / Windows 保留外部验收项。
清晰度 4：明确区分真实模型、测试页面配置和已撤回的试作；底层日志仍偏长。
可行动性 4：产物、日志与截图定位完整；归档元数据展示等待 Simon 选择。
简洁性 4：结论集中在本批；试作撤回和键盘工具错误留痕使技术记录较长。
平均 4.0/5，无低于 3 分项。后续优先模型限额后的错误提示/停止策略，其次按确认意见改归档可辨识性。
自查：本批证明核心数据范围交付链，不等于完整跨平台、多来源版本已经发布，预计符合 Simon 对验收口径的要求。
