# 界面 V2 正式仓库迁移验收

日期：2026-09-05。授权：Simon「就按这版吧，你开始更新仓库」。

## 范围与状态

- 活动仓库 `vibe-research-agent-Codex`，分支 `feat/dual-engine`，HEAD 仍为 `2ed80ac`。
- 仅本地修改；未暂存、未提交、未推送、未打标签、未发布。保留原有 M9–M14 及上游同步未提交改动。
- V2 独立原型仅作为视觉参考，原型里的合成数字、公司、曲线、假交互未移入产品源码。
- 未修改后端、数据契约、登录实现、模型路由、用户台账或金融研究宪法；未修改历史、Global、AStock、39 号项目副本。

## 实现

1. 全站改为炭黑侧栏、暖白/炭黑阅读面、朱红重点色与少量暖金；收紧圆角、阴影和字阶。保留语义令牌名，下降色用蓝灰并保留数值正负号，不使用绿色作为品牌色。
2. 原 13 个主栏目与 7 个子栏目名称、顺序、路由、展开存储键保留。桌面侧栏 228px / 收起 56px；手机改为抽屉导航，具备关闭、键盘焦点约束、返回焦点与跳到内容入口。
3. 首页新增后端行情快照和最近正式研究归档。只读现有接口，不启动模型任务；快照标取数时间、单位与证据，空值显示“未获取”，partial 保留有效字段，failed 不展示其数字，归档请求失败不伪装成没有记录。
4. 研究页用六阶段索引呈现真实阶段状态。报告从纯文本改为安全 Markdown 排版，旁列同一次运行的后端证据附录；仍按 availability 放行，未校验正文与附录均不渲染。HTML 不执行、外部图片不加载、危险链接不放行。
5. 我的研报阅读面、全局页标题、面板和首页 Agent 使用统一样式。AI 订阅/API 两个选择卡改为原生按钮，选择与“默认开启 Agent”的行为保持不变。

报告附录按真实 id 手动核对，不宣称本轮已实现逐条引用弹窗或 PDF 页内定位。首页没有生成虚构历史曲线。

## 验证证据

- 前端完整串行测试 **57/57**，修复前后各跑一轮；新增 3 项界面契约测试，扩展报告 SSR 测试覆盖 Markdown 表格、加粗、附录隔离、外部图片及危险链接。
- `npm run build` 通过，包含 TypeScript 检查。仍有主 JS 包约 1.31 MB 的构建体积提醒，本轮未做代码拆分。
- `git diff --check` 通过；暂存区为空。
- 与改前 `/tmp/vra-ui-v2-before.FuOSVw` 的逐文件比对：只改变 13 个 src 文件（含 1 个新增 HomeOverview）及 Tailwind 配置；测试改动 2 个文件。没有用 git HEAD 覆盖预先存在的修改。
- 实际 Codex CLI 独立审查两轮：第一轮 1 条 P2（移动导航关闭时，opener 尚在 inert 容器内，立即 focus 失效），核实后改为下一帧恢复焦点；第二轮 `No actionable P1/P2 findings`。原始结论在 `.local/reviews/ui-v2-review-r1.md` 与 `ui-v2-review-r2.md`。

### 浏览器验证

隔离地址 `127.0.0.1:5972` 使用正式 React 源码 + 合成验收 HTTP 数据，没有反向代理真实服务，不读用户资料或登录态，不调用真实模型。夹具位于忽略目录 `.local/reviews/ui-v2-browser.ts`，没有进入生产接口。

已验证：

- 通过页面“测试并保存”完成模拟接入，首页任务和原侧栏显示正常。
- 深浅主题均渲染；桌面 1440px 与手机 390px 的首页/研究报告无主容器横向溢出。
- partial 显示已有数字与缺失项；failed 隐藏数字并提示不能判断市场；空快照不填零。
- 研究归档按钮打开六阶段状态、正文表格、同次运行证据附录。
- 手机打开导航后背景 inert；关闭后实际 activeElement 为“打开导航”，inert=false；点击首页后焦点到 workspace-main。
- 原资讯子栏目正常展开；收起侧栏后宽度 56px，20 个主/子链接仍在，主容器无横向溢出。

正式服务 `127.0.0.1:8765` 与前端 `127.0.0.1:5930` 已启动。正式接入页正常打开，浏览器 error/warn 日志为空；真实检测显示 Codex、Claude Code、WorkBuddy / CodeBuddy 可用。此浏览器来源尚未保存 AI 选择，因此按既有门控显示首次接入页；未替用户选择或运行连接探针。

这不是全部业务页真实数据 E2E、六阶段模型重跑、Windows 实机或 LAN 验收；上述旧发布门槛不因 UI 验证而消失。

## 交付自检

按 agent-self-evaluation 的五轴口径：准确性 4/5（测试与双轮审查通过，但未复跑真实模型）；完整性 4/5（本轮视觉范围完成，仍需用户审美验收）；清晰度 4/5（保留熟悉栏目，AI 接入页原有说明仍较长）；可操作性 4/5（正式服务可打开，首次浏览器仍需用户选择 AI）；简洁度 4/5（14 个产品文件的局部变更，无新依赖，首页仍保留原有完整功能入口）。平均 4.0/5，无需因本轮范围暂停交付。

后续优先：用户看正式页定稿；另行授权后做包体拆分与更广的真实业务回归。自查：用户应能认可“原板块保留、无绿色主视觉、正式页面可看”的事实；是否足够有质感仍以用户验收为准。
